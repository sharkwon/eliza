[
  {
    "domain": "Built-in code-writing tools the agent uses directly (file/edit/bash/build-validation), not via sub-agents",
    "summary": "The direct code-writing surface is real and fairly mature. `@elizaos/plugin-coding-tools` exposes FILE (read/write/edit/grep/glob/ls), SHELL (run via /bin/bash -c with cloud/local-safe/local-yolo/capability-router dispatch + history), and WORKTREE (enter/exit) actions, gated to code/terminal/automation contexts with role gates, backed by a SandboxService path blocklist, FileStateService read-before-write mtime gating, RipgrepService, and SessionCwdService. Build/quality validation is NOT in coding-tools — it lives in `@elizaos/plugin-app-control`'s AppVerificationService (typecheck/lint/test/build/launch/browser-screenshot/structured-proof) which the APP create/edit flow wires into the coding sub-agent via a validator contract. Unit + deterministic-scenario coverage is strong (4000+ test LOC in coding-tools, real-tsc integration test for verification), but the end-to-end create→coding-agent→verify→screenshot loop is never exercised with a live model (scenarios deliberately omit START_CODING_TASK), and there are zero committed evidence artifacts (no trajectories, video, screenshots, or timelines) for this domain. The stale NFT-drop plugin scope entry has since been removed from this domain.",
    "maturity": "partial",
    "existing": [
      {
        "component": "codingToolsPlugin (FILE/SHELL/WORKTREE actions)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/index.ts",
        "whatItDoes": "Registers the three direct coding actions plus SandboxService, FileStateService, SessionCwdService, RipgrepService. Auto-enables on features.codingTools and terminal-capable env (off on store/iOS, Android only in local-yolo).",
        "maturity": "solid"
      },
      {
        "component": "SHELL action (shellAction)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/bash.ts",
        "whatItDoes": "action=run executes via /bin/bash -c with timeout clamp [100,600000], cwd validation, history view/clear, and heavy NL-driven command rewriting (disk/memory/health/source-search/crypto endpoint canonicalization). Surfaces exit code, cwd, took, timeout, signal, sandbox backend; truncates streams at 30k chars.",
        "maturity": "solid"
      },
      {
        "component": "FILE edit handler (editFileHandler)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/edit.ts",
        "whatItDoes": "String-replace edit with sandbox path validation, assertWritable stale-read gate, ambiguity check (multi-match requires replace_all), no_match failure, detectSecrets guard before write, recordWrite after. Returns replacement count + first line.",
        "maturity": "solid"
      },
      {
        "component": "FILE write handler (writeFileHandler)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/write.ts",
        "whatItDoes": "Validates path, gates on assertWritable, detectSecrets guard, mkdir -p + writeFile, recordWrite. Non-atomic (direct writeFile, no temp+rename).",
        "maturity": "solid"
      },
      {
        "component": "runShell execution chokepoint",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/lib/run-shell.ts",
        "whatItDoes": "Dispatches by runtime mode: capability-router PTY first, then cloud (throws), local-safe (SandboxManager.exec, refuses outside workspace), local-yolo (host /bin/bash -c). Handles bash/zsh startup flags, process-group kill on timeout (SIGTERM then SIGKILL), zsh->bash fallback, missing-tool detection.",
        "maturity": "solid"
      },
      {
        "component": "SandboxService (path policy)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/services/sandbox-service.ts",
        "whatItDoes": "Blocks user-private + per-OS system paths by default; optional allow-roots via CODING_TOOLS_WORKSPACE_ROOTS; realpath-resolves and rejects traversal. Validated before every fs access.",
        "maturity": "solid"
      },
      {
        "component": "AppVerificationService (build validation)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-verification.ts",
        "whatItDoes": "Structured verification pipeline: runs package scripts typecheck/lint/test/build, app launch poll, headless puppeteer browser check with screenshot + vision description, and a structured-proof check that cross-validates the agent's claimed APP_CREATE_DONE/PLUGIN_CREATE_DONE counts against actual test output. Persists per-check .log files under <stateDir>/app-verifications/<runId>/, returns verdict + retryablePromptForChild.",
        "maturity": "solid"
      },
      {
        "component": "verification-helpers (diagnostics + screenshot capture)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/verification-helpers.ts",
        "whatItDoes": "parseTscOutput/parseEslintOutput/parseVitestOutput diagnostic parsers, detectPackageManager, ensureVerificationDir, captureScreenshotViaDevApi (/api/dev/cursor-screenshot), describeScreenshotWithVision (IMAGE_DESCRIPTION model).",
        "maturity": "solid"
      },
      {
        "component": "APP create flow (runCreate / dispatchCodingAgent)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/app-create.ts",
        "whatItDoes": "Multi-turn scaffold flow: fuzzy-match installed apps, render [CHOICE] block, copy min-project template with token rewrite, then dispatch a coding sub-agent via START_CODING_TASK with the app-verification validator (onVerificationFail=retry) using spawnWithTrajectoryLink. Errors surface as failure ActionResults with reason text.",
        "maturity": "solid"
      },
      {
        "component": "CodingTaskExecutor",
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/services/coding-task-executor.ts",
        "whatItDoes": "TaskExecutor that maps coding-verb task specs to START_CODING_TASK/CREATE_TASK action, rewrites output in character voice. Bridges direct tools to the orchestrator contract.",
        "maturity": "partial"
      },
      {
        "component": "plugin-shell ShellService",
        "path": "/Users/shawwalters/eliza/plugins/plugin-shell/services/shellService.ts",
        "whatItDoes": "Lower-level sandboxed shell: SHELL_ALLOWED_DIRECTORY restriction, PTY, background named sessions (poll/send-keys/kill), per-conversation history, ExecApprovalService. The coding-tools SHELL action consumes its history service.",
        "maturity": "solid"
      },
      {
        "component": "AppWorkerHostService (app reload/isolation)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-worker-host-service.ts",
        "whatItDoes": "Spawns one worker_threads Worker per isolation=worker app with typed RPC invoke(); auto-starts persisted worker apps on boot. Relevant to modify/reload of generated apps.",
        "maturity": "solid"
      },
      {
        "component": "DeviceFilesystemBridge",
        "path": "/Users/shawwalters/eliza/plugins/plugin-device-filesystem/src/services/device-filesystem-bridge.ts",
        "whatItDoes": "Mobile-safe read/write/list bridge (Capacitor on iOS/Android, node:fs on desktop/AOSP) that coding-tools FILE target=device delegates to. normalizeDevicePath rejects absolute/.. /NUL.",
        "maturity": "solid"
      },
      {
        "component": "ElizaBunRuntime (native-bun-runtime)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-native-bun-runtime/src/index.ts",
        "whatItDoes": "Capacitor plugin hosting the agent runtime on-device (iOS xcframework/JSCore, Android foreground service). Infrastructure that enables the agent (and thus its coding tools) to run on mobile; not itself a code-writing tool.",
        "maturity": "partial"
      },
      {
        "component": "plugin-app-manager (AppManager + apps routes)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-manager/src/services/app-manager.ts",
        "whatItDoes": "App discovery/launch/run-state library + /api/apps/* routes. Supports the launch step of verification; not a code-writing tool itself.",
        "maturity": "solid"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/bash.test.ts",
        "kind": "unit",
        "covers": "43 tests: capability-router preference, foreground exec, empty-stream marking, blocklist cwd rejection, timeout + orphaned-child kill, cwd grounding/rewrite logic, URL quoting, all NL command-canonicalization rewrites, user-facing-text projection, command_failed paths, history view/clear.",
        "gaps": "All via injected fakes/mocks; no real long-running build (bun run build) validation, no concurrent shell sessions, no real PTY/background session through coding-tools, no cloud-mode refusal asserted end-to-end."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/edit.test.ts",
        "kind": "unit",
        "covers": "edit ambiguity/no_match/identical-string/secret-guard/stale-read paths and replacement counting.",
        "gaps": "No concurrent-edit race, no binary-file edit, no very-large-file edit."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/services/sandbox-service.test.ts",
        "kind": "unit",
        "covers": "Per-OS default blocklist (darwin/linux/win32), credential-dir blocking, /etc realpath resolution, allow-roots.",
        "gaps": "Symlink-escape into blocked path and worktree-root mutation interactions only lightly covered."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/lib/run-shell.test.ts",
        "kind": "unit",
        "covers": "Mode dispatch (cloud throw, local-safe SandboxManager refusal/exec, local-yolo host), capability-router routing.",
        "gaps": "No real docker/apple-container backend exercised; backendForManager engine paths not run against real engines."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-coding-tools/__tests__/plugin-integration.test.ts",
        "kind": "integration",
        "covers": "Plugin wiring: services registered, actions present, autoEnable gating across env/variant/platform.",
        "gaps": "No live runtime action dispatch; plugin loaded in isolation."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
        "kind": "scenario",
        "covers": "End-to-end FILE write/read, SHELL run, WORKTREE enter/exit against a real seeded git repo + real SandboxService/SessionCwdService; asserts exact action order, args, filesystem side effects, worktree cleanup. Captures CapturedAction ledger.",
        "gaps": "Deterministic LLM proxy with strict pre-registered fixtures (registerStrictActionRouteFixtures) — the model never actually chooses tools; no live-model trajectory. No multi-file edit task, no build/test invocation, no error-recovery or rollback turn."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/app-verification.integration.test.ts",
        "kind": "integration",
        "covers": "Real tsc typecheck pass/fail verdict, lint/test via local shims, structured-proof mismatch diagnostics (claimed vs actual test count, kind/name-field mismatch), retry-prompt content. Gated on bun/npm availability.",
        "gaps": "build, launch, and browser/screenshot checks are NOT integration-tested with real puppeteer/launch; only unit-mocked in app-verification.test.ts (which exists but was not opened). No vision-description path tested live."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/test/scenarios/app-create-no-existing.scenario.ts",
        "kind": "scenario",
        "covers": "live-only scenario: APP create first-turn scaffold acknowledgement, judged by LLM rubric.",
        "gaps": "Explicitly does NOT register START_CODING_TASK, so the actual scaffold->coding-agent->verify->screenshot loop never runs. Accepts a 'could not dispatch' failure as pass. No real generated-app artifact, no verification verdict, no screenshot."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/app-worker-host.test.ts",
        "kind": "unit",
        "covers": "21 tests for worker spawn/RPC/lifecycle of isolation=worker apps.",
        "gaps": "No test reloading a freshly-coded app, no concurrency to many simultaneous app workers."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-shell/__tests__/shell.real.test.ts",
        "kind": "integration",
        "covers": "2 tests: real command in allowed dir surfaced via provider; fail-closed on directory escape.",
        "gaps": "No background-session, PTY, send-keys, approval-flow, or history-TTL real tests."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-device-filesystem/src/__tests__/round-trip.test.ts",
        "kind": "integration",
        "covers": "Node-backend write/read/list/overwrite round-trip + path-validation + plugin-registration tests.",
        "gaps": "Capacitor (iOS/Android) backend never exercised; only Node path tested."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-native-bun-runtime/src/bridge-contract.test.ts",
        "kind": "unit",
        "covers": "Web-fallback returns (start/getStatus/stop/sendMessage/call/TTS unavailable), StartOptions shape, hostile-input rejection.",
        "gaps": "No real on-device iOS/Android engine; native code path untested in JS."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/__tests__/deterministic-action-coverage.test.ts",
        "kind": "unit",
        "covers": "Ratchet gate: coding-tools + app-control + device-filesystem action surfaces must match manifest and be referenced by a running deterministic scenario; count only grows.",
        "gaps": "Guards existence/wiring of coverage, not correctness or live behavior; tolerates deterministic-only coverage."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": true,
        "where": "Deterministic CapturedAction ledger via scenario-runner (packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts) — but proxy-LLM, not live. Real-LLM trajectory JSONL/run-viewer for coding/app-create: NOT present on disk."
      },
      {
        "type": "log",
        "exists": true,
        "where": "AppVerificationService persists per-check .log files at runtime under <ELIZA_STATE_DIR>/app-verifications/<runId>/{typecheck,lint,test,build,launch,browser,structured-proof}.log; structured [ClassName] logs throughout coding-tools and app-create. No committed sample logs in repo."
      },
      {
        "type": "screenshot",
        "exists": true,
        "where": "Generated at runtime only: AppVerificationService browser check writes screenshot.png and captureScreenshotViaDevApi writes desktop.png into the verification dir. No committed before/after screenshots for coding tools or app-create in .github/issue-evidence/."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough or test:e2e:record artifact for the create-app / file-edit / build-validation flow anywhere in the domain."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline capture for a coding/build run; durationMs is recorded per-check inside VerificationResult but never assembled into a committed timeline artifact."
      },
      {
        "type": "report",
        "exists": true,
        "where": "AppVerificationService returns a structured VerificationResult (verdict/checks/screenshot/retryablePromptForChild/durationMs); scenario-runner can emit JSON reports. No checked-in report for this domain."
      }
    ],
    "gaps": [
      {
        "title": "No live-LLM end-to-end create-app loop (scaffold -> coding agent -> verify -> screenshot)",
        "severity": "critical",
        "description": "The full direct-coding pipeline is never proven with a real model. plugin-app-control/test/scenarios/app-create-no-existing.scenario.ts is live-only but deliberately omits START_CODING_TASK and accepts a 'could not dispatch' result as pass, so no real app is ever generated, verified, or screenshotted. deterministic-coding-tools-actions.scenario.ts uses strict pre-registered fixtures, so the model never selects a tool. There is zero real-LLM trajectory evidence for direct code writing.",
        "suggestedWork": "Add a live-only scenario under plugins/plugin-app-control/test/scenarios that registers START_CODING_TASK + AppVerificationService and runs APP create against a live model end-to-end, asserting: template copied, child coding agent edited files, AppVerificationService verdict=pass, screenshot.png + per-check logs produced. Run via eliza-scenarios run with --report and --run-dir to capture a real JSONL trajectory + run viewer, and commit the artifacts to .github/issue-evidence/."
      },
      {
        "title": "build / launch / browser-screenshot verification checks lack integration coverage",
        "severity": "high",
        "description": "AppVerificationService.runBuild, runLaunchCheck, and runBrowserCheck (real puppeteer + screenshot + vision) are only unit-mocked. The integration test (app-verification.integration.test.ts) covers typecheck/lint/test/structured-proof with real tsc but stops there. The browser path that produces the only real screenshot evidence is never exercised against a real launched app + real chromium in CI.",
        "suggestedWork": "Add an integration test that scaffolds a tiny real app, runs profile=full with a launched dev server (or a static fixture server) and ELIZA_CHROME_PATH set, and asserts a non-empty screenshot.png is written and the browser check passes/fails on injected console errors. Gate on chromium availability with itIf, similar to the existing bun/npm gate."
      },
      {
        "title": "No committed evidence artifacts (trajectory/screenshot/video/timeline) for the coding domain",
        "severity": "high",
        "description": "Per the repo Definition of Done, agent/action/tool changes require real-LLM trajectories, before/after screenshots, and a video walkthrough. .github/issue-evidence/ contains only cloud UI PNGs; nothing for FILE/SHELL/WORKTREE or app-create. No .webm/.mp4 and no test:e2e:record output exists for this domain.",
        "suggestedWork": "Create a recorded walkthrough harness: drive a real agent through 'create an app that does X', capture the chat UI + generated files + verification verdict via bun run test:e2e:record (video) plus the scenario-runner JSONL trajectory and the AppVerificationService screenshot/logs. Land them under .github/issue-evidence/<issue#>-coding-create-app/."
      },
      {
        "title": "No concurrency / many-projects coverage",
        "severity": "high",
        "description": "Phase 3 goal of 10 concurrent coding projects has no test. AppWorkerHostService spawns workers but is unit-tested per-app; there is no test of multiple simultaneous app-create dispatches, multiple verification runs writing to distinct runIds, or SessionCwdService/SandboxService isolation across many concurrent conversations.",
        "suggestedWork": "Add a stress/scenario test that fires N (>=10) concurrent APP create dispatches (or N concurrent SHELL/FILE sessions in distinct rooms) and asserts: per-conversation cwd/sandbox isolation holds, verification runIds do not collide, and no cross-room file leakage. Capture a timeline of start/finish per run."
      },
      {
        "title": "No rollback / undo for direct code edits",
        "severity": "medium",
        "description": "On verification failure the flow only retries with retryablePromptForChild; there is no git-revert/restore-backup path and no test for rolling back a bad generated app or a failed multi-file edit. FileStateService tracks mtime for stale-read protection but does not snapshot prior contents.",
        "suggestedWork": "Add a backup/rollback capability (e.g. snapshot edited files or use a worktree/branch per task with git reset on failure) and a scenario asserting that a failed verification leaves the workdir restorable. At minimum, test the WORKTREE exit cleanup path as the rollback boundary for create flows."
      },
      {
        "title": "SHELL action has no real build-failure -> diagnostic surfacing test",
        "severity": "medium",
        "description": "bash.test.ts asserts command_failed on non-zero exit, but there is no test where the agent runs a real build/typecheck via SHELL, the build fails, and the failure (exit code + truncated stderr) is surfaced to the planner in a way a follow-up action can act on. Build validation only happens in app-control's separate verification service, not via the direct SHELL tool the agent uses for ad-hoc builds.",
        "suggestedWork": "Add a deterministic scenario where SHELL runs a failing 'bun run build'/'tsc' in a seeded project and assert the ActionResult carries exit_code + stderr tail, and that a subsequent FILE edit + re-run SHELL produces exit 0. Capture the two-step trajectory."
      },
      {
        "title": "Mobile/device backends (Capacitor iOS/Android) untested for file writes",
        "severity": "medium",
        "description": "DeviceFilesystemBridge only has Node-backend round-trip tests; the Capacitor iOS/Android path (FILE target=device) is never exercised. native-bun-runtime native engine paths are only tested via web-fallback stubs. Platform coverage (AOSP/iOS/Play) for the file-write tool is effectively unproven.",
        "suggestedWork": "Add a device-backed integration harness (or at minimum a contract test with a mocked Capacitor Filesystem) that exercises read/write/list through the Capacitor backend, and document the manual on-device evidence step (screenshot + log) for AOSP/iOS in PR_EVIDENCE flow."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/index.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/bash.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/edit.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/actions/write.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/lib/run-shell.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/services/coding-task-executor.ts",
      "/Users/shawwalters/eliza/plugins/plugin-coding-tools/src/services/sandbox-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-verification.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/verification-helpers.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/app-create.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/app-verification.integration.test.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/test/scenarios/app-create-no-existing.scenario.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/__tests__/deterministic-action-coverage.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-shell/services/shellService.ts",
      "/Users/shawwalters/eliza/plugins/plugin-device-filesystem/src/services/device-filesystem-bridge.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-worker-host-service.ts"
    ]
  },
  {
    "domain": "Sub-agent orchestration via ACP (plugins/plugin-agent-orchestrator, plugin-sub-agent-claude-code, packages/sweagent)",
    "summary": "plugin-agent-orchestrator is the canonical, mature ACP orchestration plugin: it spawns local coding agents (elizaos, pi-agent, opencode, codex, claude) as subprocesses via either an embedded native ACP JSON-RPC client (NativeAcpClient in acp-native-transport.ts, default) or a legacy acpx CLI wrapper, routes their terminal events back into the runtime as synthetic memories (SubAgentRouter), and layers durable tasks (OrchestratorTaskService), a structured provision→turn→submit smithers runner, workspace/git/GitHub lifecycle, multi-account credential injection, trajectory linkage, and an interruption decider. Unit coverage is genuinely deep and green (acp-service 73 cases, acp-native-transport 21, sub-agent-router large, multi-account 6+, interruption-decider 9, plus ~60 unit files). The agent-spawning/routing/credential logic is solid-to-partial. However the LIVE end-to-end evidence layer is broken and stale: the offline composed multi-account E2E works, but the headline live smoke (task-agent-live-smoke.ts) is referenced by the e2e test at a non-existent path (packages/app-core/test/scripts vs the real packages/core/test/live), imports a PTYService the plugin does not export, only supports framework claude/codex and modes sequential/web (the e2e calls a counter-app mode that the smoke rejects), and verifies served apps via raw net/HTTP reachability + sentinel strings rather than screenshots or video. There are no saved trajectory/screenshot/video/timeline artifacts for this domain anywhere in the repo, and no scenario-runner scenario exercises the orchestrator/sub-agent path. plugin-sub-agent-claude-code is a separate, smaller sandboxed claude-CLI driver (not ACP), and packages/sweagent is security-patched vendor source not wired into runtime.",
    "maturity": "partial",
    "existing": [
      {
        "component": "AcpService (subprocess lifecycle, transport selection, spawn/send/cancel/stop, session reconcile + acpx-orphan GC, baseline-sha capture, account selection)",
        "path": "plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
        "whatItDoes": "2941-line core: spawnSession() normalizes agentType, resolves workdir, selects a coding account + injects credentials, then branches to native (spawnNativeSession + NativeAcpClient) or cli (runAcpx) transport; nativeAgentCommand() resolves per-agent commands (ELIZA_<AGENT>_ACP_COMMAND overrides, codex/claude default to pinned npx, opencode prefers vendored shim); sendPrompt/cancel/stop/getChangedPaths; emits typed SessionEvents.",
        "maturity": "solid"
      },
      {
        "component": "NativeAcpClient (embedded ACP JSON-RPC over stdio)",
        "path": "plugins/plugin-agent-orchestrator/src/services/acp-native-transport.ts",
        "whatItDoes": "927-line ACP client: initialize/session.new/session.prompt/cancel/close, handles agent->client requests (session/update, request_permission with approvalPreset gating, fs read/write scoped to cwd via realpath + symlink rejection, terminal create/output/wait/kill/release with process-group signaling), parses ELIZA_ACP_MCP_SERVERS for MCP parity, 300s default timeout.",
        "maturity": "solid"
      },
      {
        "component": "SubAgentRouter (terminal-event -> synthetic memory routing)",
        "path": "plugins/plugin-agent-orchestrator/src/services/sub-agent-router.ts",
        "whatItDoes": "2606-line service: subscribes to AcpService.onSessionEvent, posts task_complete/error/blocked as synthetic inbound memories to runtime.messageService.handleMessage with origin routing keys (roomId/userId/messageId), per-session round-trip cap (default 32) with lineage reset on task_complete, cross-session completion dedupe, changeset/verified-URL gating, dead-URL suppression.",
        "maturity": "solid"
      },
      {
        "component": "OrchestratorTaskService (durable task store + ACP event bridge)",
        "path": "plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
        "whatItDoes": "2115-line durable task lifecycle: spawns via acp.spawnSession (L1710), bridges ACP events to task records, drives follow-ups (sendToSession), stop/archive; backs the /api/orchestrator routes.",
        "maturity": "solid"
      },
      {
        "component": "task-agent-routing (adapter + workdir resolution)",
        "path": "plugins/plugin-agent-orchestrator/src/services/task-agent-routing.ts",
        "whatItDoes": "normalizeTaskAgentAdapter maps aliases to elizaos/pi-agent/claude/codex/opencode; resolvePinnedAdapter (fixed strategy reads ELIZA_ACP_DEFAULT_AGENT/BENCHMARK_TASK_AGENT); resolveSpawnWorkdir via TASK_AGENT_WORKDIR_ROUTES JSON rules + convention auto-detect under TASK_AGENT_WORKDIR_ROOTS.",
        "maturity": "solid"
      },
      {
        "component": "smithers-task-runner / executor / integration (default structured execution path)",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
        "whatItDoes": "Default-on (ELIZA_ORCHESTRATOR_SMITHERS!=0) provision->turn(per-agent, parallel fan-out)->approval->submit workflow graph; each step delegates to ACP turns; executor + integration + db-backend covered by unit tests.",
        "maturity": "solid"
      },
      {
        "component": "interruption-decider + sub-agent-inbox",
        "path": "plugins/plugin-agent-orchestrator/src/services/interruption-decider.ts",
        "whatItDoes": "Pure decideInterruption() gives coding sub-agents a shouldRespond-equivalent: deliver/queue/interrupt/ignore based on stop/redirect/additive patterns, addressing, multiParty, sessionBusy; SubAgentInbox queues QUEUE/INTERRUPT text and drains on idle.",
        "maturity": "solid"
      },
      {
        "component": "coding-account-selection + multi-account credential injection",
        "path": "plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
        "whatItDoes": "selectCodingAccount picks least-used pooled account per agent type; injects per-account creds (claude->CLAUDE_CODE_OAUTH_TOKEN, codex->per-account CODEX_HOME/auth.json, opencode->pooled key) and drops parent ANTHROPIC_API_KEY/OPENAI_API_KEY; verified by multi-account-spawn unit test + compose-multi-account-e2e.",
        "maturity": "solid"
      },
      {
        "component": "spawn-trajectory (parent<->child trajectory linkage)",
        "path": "plugins/plugin-agent-orchestrator/src/services/spawn-trajectory.ts",
        "whatItDoes": "withLinkedSpawn wraps spawnWithTrajectoryLink from core: stamps parentTrajectoryStepId into child metadata + ELIZA_PARENT_TRAJECTORY_STEP_ID env and calls handle.linkChild(childId). Wires the data model for cross-agent trajectories; unit-tested. No evidence it is actually exercised end-to-end with a real captured trajectory.",
        "maturity": "partial"
      },
      {
        "component": "plugin-sub-agent-claude-code (sandboxed claude CLI driver, NOT ACP)",
        "path": "packages/plugin-remote-manifest/src/sub-agent-claude-code/sub-agent-service.ts",
        "whatItDoes": "Separate plugin: wraps the claude CLI in an OS-sandboxed (sandbox-exec/bwrap) Bun subprocess with env allowlist/blocklist, cwd realpath validation, binary allowlisting, redacted session transcripts + audit hash. Has unit tests (sandbox, sub-agent-service, session-recorder, plugin). Not part of the ACP transport path.",
        "maturity": "partial"
      },
      {
        "component": "packages/sweagent (security-patched SWE-agent vendor source)",
        "path": "packages/sweagent/security",
        "whatItDoes": "Security-remediated copies of SWE-agent python/typescript inspector + tools (path-traversal/SSRF/command-injection fixes). Explicitly NOT a runtime-imported plugin; only safe-path/safe-url unit tests. Not wired to the ACP orchestrator.",
        "maturity": "partial"
      },
      {
        "component": "benchmarks/orchestrator (SWE-bench-style trajectory harness + viewer)",
        "path": "(elizaOS/benchmarks) orchestrator",
        "whatItDoes": "Python benchmark orchestrator that runs elizaos/opencode (and other) adapters across a matrix, normalizes results into SQLite/JSON, writes per-cell trajectories/ dirs, and serves a browser viewer; supports trajectory-review enforcement. A real trajectory-evidence system but a separate benchmark harness, not wired to the ACP plugin live path.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/ (60+ files: acp-service.test.ts 73, acp-native-transport.test.ts 21, sub-agent-router.test.ts, multi-account-spawn.test.ts 6, spawn-agent.test.ts 13, send-to-agent.test.ts, create-task.test.ts, session-store.test.ts 12, interruption-decider.test.ts 9, smithers-*.test.ts, coding-account-selection.test.ts, sub-agent-identity.test.ts, claude-oauth-token.test.ts, etc.)",
        "kind": "unit",
        "covers": "Spawn argument/command resolution, transport selection, ACP JSON-RPC framing + permission/fs/terminal handlers, synthetic-memory routing keys + round-trip cap, multi-account credential injection + parent-key drop + least-used rotation, session persistence tiers, interruption decisions, smithers graph, route handlers, SSRF guard. Verified green (ran a 36-case subset: all pass).",
        "gaps": "All mock the subprocess/runtime; none spawn a real agent or assert a real produced artifact. No assertion that a captured trajectory file is emitted for a spawn."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/src/__tests__/spawn-trajectory.test.ts",
        "kind": "unit",
        "covers": "Parent-step metadata stamping + linkChild call via withLinkedSpawn under runWithTrajectoryContext.",
        "gaps": "Mocked trajectory logger only; does not prove a real parent/child trajectory is recorded or viewable end-to-end."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
        "kind": "live",
        "covers": "Gated (RUN_LIVE_ACPX=1 + acpx on PATH) real codex subprocess -> task_complete -> SubAgentRouter -> fake messageService.handleMessage with all routing keys intact.",
        "gaps": "Requires legacy acpx CLI (not the default native transport) and an authenticated codex; skips by default; no artifact capture; does not cover native transport, claude, opencode, elizaos, or pi-agent."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/native-acp-smoke.live.test.ts + tests/e2e/live-native-acp-smoke.mjs",
        "kind": "live",
        "covers": "Gated (RUN_LIVE_NATIVE_ACP=1) native ACP adapter math prompt -> stopReason end_turn, final text contains 15; optional claude/opencode via explicit commands.",
        "gaps": "Trivial math prompt only; no real coding task, no file/app artifact, no trajectory/screenshot; skips when unset/unconfigured."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/src/__tests__/task-agent-live.e2e.test.ts",
        "kind": "e2e",
        "covers": "Intends to drive live claude/codex through sequential, web, and counter-app modes (keep-alive across tasks, serve a webpage, build a counter app loaded through APP).",
        "gaps": "BROKEN/STALE: resolves liveSmokeScript to packages/app-core/test/scripts/task-agent-live-smoke.ts which does NOT exist (real file is packages/core/test/live/); and the actual smoke imports PTYService (not exported by the plugin) and only supports modes sequential|web (rejects the counter-app mode 3 of these tests pass in). Even when authenticated these would fail."
      },
      {
        "path": "packages/core/test/live/task-agent-live-smoke.ts",
        "kind": "live",
        "covers": "sequential (file create + sentinel, session reuse) and web (serve a page, reference-page research) for claude/codex using PTYService + spawnAgentAction.",
        "gaps": "Imports PTYService from @elizaos/plugin-agent-orchestrator which is NOT exported anywhere (only an unrelated substring hit exists), so import fails; verifies served apps via net/HTTP reachability + sentinel strings, never screenshots/video; no counter-app mode; framework limited to claude/codex (no opencode/elizaos/pi-agent)."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
        "kind": "e2e",
        "covers": "Offline composed pipeline: real app-core AccountPool + selector bridge -> AcpService.spawnSession (cli transport) -> a fake acpx subprocess records injected env; asserts distinct least-used accounts, parent-key drop, per-account CODEX_HOME/auth.json. Self-checking with PASS/FAIL summary.",
        "gaps": "Uses a fake acpx binary (no real coding agent); cli transport only; no trajectory/screenshot."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/scripts/live-codex-spawn-e2e.ts + live-opencode-build-e2e.ts",
        "kind": "live",
        "covers": "Real codex (machine ~/.codex login) / real opencode (CEREBRAS_API_KEY) spawned via native AcpService to build LIVE_PROOF.txt; verifies real usage probe, real account selected/injected, file built. Honest console outcome report.",
        "gaps": "Standalone bun scripts, not in CI/vitest; skip without creds; output is console text only, no saved trajectory/screenshot/video/timeline artifact; only codex + opencode (no claude/elizaos/pi-agent live build)."
      },
      {
        "path": "packages/plugin-remote-manifest/src/sub-agent-claude-code/*.test.ts (sandbox, sub-agent-service, session-recorder, plugin)",
        "kind": "unit",
        "covers": "Env filtering, cwd/symlink validation, binary allowlist, OS sandbox fallback, redacted transcripts + audit hash for the claude-CLI driver.",
        "gaps": "Separate from ACP path; no live claude-CLI spawn test, no artifact evidence."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/scenario-runner (none)",
        "kind": "none",
        "covers": "Nothing — no scenario-runner scenario exercises TASKS/spawn_agent/sub-agent routing. deterministic-coding-tools-actions.scenario.ts covers built-in coding tools, not the orchestrator.",
        "gaps": "No real-LLM scenario trajectory for the orchestrator domain (the PR_EVIDENCE-mandated artifact type is entirely absent here)."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No saved real-LLM scenario trajectory for spawn/route exists. spawn-trajectory.ts wires parent/child linkage and benchmarks/orchestrator can emit trajectories/ dirs for elizaos/opencode, but neither produces a checked-in or CI-captured trajectory for the ACP orchestrator live path. No scenario-runner scenario targets TASKS/sub-agents."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "No screenshots for sub-agent/orchestrator flows. The 'build a counter app, load through APP' / 'serve a webpage' live smoke verifies via net + sentinel strings only; no .png capture. .github/issue-evidence has only unrelated cloud images."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of any spawn/route/build flow exists for this domain."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured [ClassName] logs throughout (acp-service.ts, sub-agent-router.ts, task-agent-routing.ts [workdir-routes]); live scripts (live-codex-spawn-e2e.ts, compose-multi-account-e2e.ts) emit PASS/FAIL + outcome console logs. But these are ephemeral run output, not captured/checked-in evidence."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline artifact. SessionEvents (ready/message/tool_running/task_complete/...) and durable task event-log exist in code and could feed one, but no timeline is rendered/captured for this domain."
      },
      {
        "type": "report",
        "exists": true,
        "where": "benchmarks/orchestrator produces normalized SQLite/JSON results + browser viewer (benchmarks/benchmark_results/) and code_agent matrix/publishability reports — a real report system, but for the benchmark harness, not the ACP plugin's live orchestration path."
      }
    ],
    "gaps": [
      {
        "title": "Live task-agent E2E harness is broken on three independent axes",
        "severity": "critical",
        "description": "task-agent-live.e2e.test.ts resolves the smoke at packages/app-core/test/scripts/task-agent-live-smoke.ts (does not exist; real file is packages/core/test/live/task-agent-live-smoke.ts), the smoke imports PTYService which the plugin no longer exports (PTYService exists nowhere in source), and the smoke only accepts modes sequential|web while the e2e calls --mode counter-app for 3 of its 6 tests (the smoke prints Usage and exits non-zero). Net result: the flagship 'create a counter app / serve a webpage / keep a session alive' live coverage cannot run even with valid credentials.",
        "suggestedWork": "Repoint the e2e test path to packages/core/test/live/task-agent-live-smoke.ts (or move the smoke under app-core), replace the PTYService import with AcpService + tasksAction (the current exported surface), and add a counter-app mode to the smoke (or drop those e2e cases). Add a CI/local check that imports the smoke module to catch import-shape drift."
      },
      {
        "title": "No real-LLM trajectory evidence for spawn/route, despite PR_EVIDENCE mandate",
        "severity": "high",
        "description": "spawn-trajectory.ts builds the parent/child trajectory linkage data model and is unit-tested with mocks, but no end-to-end run captures a real trajectory showing parent runtime -> spawned claude/codex/opencode -> task_complete -> SubAgentRouter synthetic memory -> planner reply. PR_EVIDENCE requires real-LLM trajectories for agent/action changes; this domain has none saved.",
        "suggestedWork": "Author a scenario-runner scenario (packages/scenario-runner/test/scenarios/orchestrator-spawn-route.scenario.ts) that spawns a sub-agent via TASKS_CREATE against a live model, asserts the synthetic task_complete memory routes back, and emits a JSON report + native jsonl + run-viewer artifact into .github/issue-evidence/. Assert handle.linkChild produced a linked child step in the recorded trajectory."
      },
      {
        "title": "Live coverage limited to codex/opencode build + claude/codex sequential; elizaos, pi-agent, and native claude builds unproven live",
        "severity": "high",
        "description": "Live build scripts only exercise codex (live-codex-spawn-e2e) and opencode/Cerebras (live-opencode-build-e2e). The router live test needs legacy acpx (not the default native transport). elizaos and pi-agent (the documented DEFAULT agent types) and a live native claude build have no live test that produces a real artifact.",
        "suggestedWork": "Add gated live build E2Es for elizaos, pi-agent, and native claude that each build a real file/app via the native transport and assert the artifact + task_complete event, mirroring live-codex-spawn-e2e.ts. Wire them into a single gated lane (e.g. ORCHESTRATOR_LIVE) with per-agent skip-on-missing-auth."
      },
      {
        "title": "Served-app verification uses net reachability + sentinel strings, not screenshot/video",
        "severity": "high",
        "description": "The web/counter-app smoke confirms a served app via raw TCP/HTTP reachability and sentinel strings in output. The domain spec wants screenshots + video of the actual rendered app/UI. There is zero visual capture for any orchestration build flow.",
        "suggestedWork": "After the sub-agent serves the app, drive a headless browser (the repo already has Playwright in packages/app-core/core) to load the served URL, capture before/after full-page screenshots and a short video, and write them to .github/issue-evidence/. Assert on rendered DOM content, not just port-open."
      },
      {
        "title": "No scenario/test for concurrency, error/rollback, or the 10-concurrent-projects target",
        "severity": "medium",
        "description": "reserveSessionSlot enforces ELIZA_ACP_MAX_SESSIONS (default 8) and the round-trip cap exists, but there is no test that drives many concurrent spawns (let alone 10 projects), no test for spawn-error recovery/respawn cascade beyond unit-level lineage logic, and no rollback/cancel-mid-build scenario with a real agent.",
        "suggestedWork": "Add an integration test that spawns N concurrent sessions (cap behavior + distinct workdirs), one that injects a failing agent command and asserts errored status + clean client teardown (spawnNativeSession catch path), and one that cancels a mid-turn session and asserts cooperative session/cancel + inbox drain. Raise ELIZA_ACP_MAX_SESSIONS to 10+ in the concurrency test."
      },
      {
        "title": "Multi-account model switching (z.ai/GLM/Kimi, gpt-oss-120b) only documented, not live-validated",
        "severity": "medium",
        "description": "Multi-account selection is well unit-tested for claude/codex/opencode, and docs describe ln/Kimi/GLM/gpt-oss-120b routing, but there is no test that actually spawns an opencode/elizaos sub-agent backed by a gpt-oss-120b / z.ai / GLM account and verifies the model answered. Docs explicitly call live OpenCode/ln validation 'credential-gated follow-up'.",
        "suggestedWork": "Add gated live tests that link a pooled ln/z.ai/GLM account, spawn opencode (and elizaos where applicable) on gpt-oss-120b, and assert a real model completion + correct injected key. Capture the spawn env + completion as evidence."
      },
      {
        "title": "Standalone live scripts produce no checked-in evidence and are outside CI",
        "severity": "medium",
        "description": "live-codex-spawn-e2e.ts / live-opencode-build-e2e.ts / compose-multi-account-e2e.ts emit only console PASS/FAIL and are run manually; nothing persists a trajectory/screenshot/log/timeline artifact, so a reviewer cannot confirm a run happened without re-running with credentials.",
        "suggestedWork": "Have each live script write a structured run report (JSON + the produced artifact + a session-event timeline) under .github/issue-evidence/<issue>-<slug>/ when a --report flag is passed, and document the exact command + expected artifact in the package README."
      },
      {
        "title": "sweagent vendor source unused at runtime; SWE-bench coding capability not connected to ACP orchestrator",
        "severity": "low",
        "description": "packages/sweagent is explicitly not imported by the runtime and only has safe-path/safe-url unit tests; the benchmarks/orchestrator SWE harness runs adapters out-of-band. The product claim of orchestrating SWE-style coding work has no in-runtime test linking sweagent or the benchmark trajectories to the ACP plugin.",
        "suggestedWork": "Either document sweagent as benchmark-only (clarify scope) or add an integration path + test that drives a SWE-bench-style task through the ACP orchestrator and reconciles its trajectory with the benchmarks/orchestrator viewer format."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-native-transport.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/sub-agent-router.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/task-agent-routing.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/spawn-trajectory.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/interruption-decider.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/sub-agent-inbox.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/session-store.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/live-codex-spawn-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/live-opencode-build-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/__tests__/task-agent-live.e2e.test.ts",
      "/Users/shawwalters/eliza/packages/core/test/live/task-agent-live-smoke.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/multi-account-spawn.test.ts",
      "/Users/shawwalters/eliza/packages/plugin-remote-manifest/src/sub-agent-claude-code/sub-agent-service.ts",
      "/Users/shawwalters/eliza/packages/sweagent/README.md",
      "/Users/shawwalters/eliza/(elizaOS/benchmarks) orchestrator/README.md"
    ]
  },
  {
    "domain": "Smithers workflow engine — in-process workflow execution (plugins/plugin-workflow) and durable coding-task orchestration (plugins/plugin-agent-orchestrator smithers-task-*)",
    "summary": "There are two distinct Smithers integrations. (1) plugin-workflow's in-process engine (smithers-runtime.ts + embedded-workflow-service.ts) spawns a fresh Bun subprocess per run, compiles an n8n-style node DAG into native Smithers control flow (dependency-depth levels → parallel groups joined in sequence), delegates each node's execution back to the parent over a line-delimited stdin/stdout protocol, honors n8n retry/continueOnFail, reports per-run engine metrics, and persists durable state to sqlite/postgres/pglite. It is fed by a RAG generation pipeline (keywords → catalog search → match → feasibility → LLM generate → validateAndRepair → fixWorkflowErrors). (2) plugin-agent-orchestrator's durable coding-task path (smithers-task-runner.ts) models a coding task as provision → durable per-agent turn loop (parallel fan-out) → approval gate → submit, crash-resumable via a stable runId + force:false; SmithersTaskExecutor drives ACP coding-agent turns. Both engines are genuinely mature in code and have real (subprocess-spawning) unit/integration tests that pass green. The critical gap: the orchestrator's rich multi-step graph (provision/submit/approval/parallel/multi-turn) is NOT wired into production — runCreate calls runDurableTask single-turn (maxTurns:1, no provision/submit/approval/parallel), so it is only a behavior-preserving durable wrapper around a single prompt. There are also no real-LLM trajectory / screenshot / video / timeline evidence artifacts and no scenario-runner scenarios for either Smithers path; RAG generation is tested only with mocked useModel.",
    "maturity": "partial",
    "existing": [
      {
        "component": "In-process Smithers workflow engine (DAG→parallel/sequence, stdin/stdout node-execution protocol, n8n retry/continueOnFail, per-run metrics)",
        "path": "plugins/plugin-workflow/src/services/smithers-runtime.ts",
        "whatItDoes": "runWorkflowWithSmithers spawns a per-run Bun subprocess running smithers-orchestrator; createSmithersScript builds the node graph with computeLevels (dependency-depth → parallel groups joined by workflow.sequence), delegates each node back to the parent via newline-delimited JSON requests, honors retryOnFail/maxTries/waitBetweenTries/continueOnFail, emits engine metrics (nodes/levels/maxConcurrency/started/finished/failed/skipped/retries). resolveSmithersDbConfig selects sqlite/postgres/pglite with feature-detect fallback.",
        "maturity": "solid"
      },
      {
        "component": "EmbeddedWorkflowService production caller of the in-process engine",
        "path": "plugins/plugin-workflow/src/services/embedded-workflow-service.ts",
        "whatItDoes": "resolveExecutionPlan (lines ~2360) topologically orders enabled nodes + builds incoming-connection map into a SmithersExecutionPlan; runWorkflow (lines ~2423) persists a pending execution then calls runWorkflowWithSmithers with runNode=executeNode, saves the completed execution, and records engine metrics. This is the real in-process execution runtime (no HTTP sidecar).",
        "maturity": "solid"
      },
      {
        "component": "RAG workflow generation pipeline",
        "path": "plugins/plugin-workflow/src/utils/generation.ts",
        "whatItDoes": "extractKeywords → matchWorkflow → assessFeasibility → generateWorkflow → fixWorkflowErrors; structured-output LLM calls (schemas in src/schemas) with provider routing (Cerebras→openai mapping). WorkflowService.generateWorkflowDraft (workflow-service.ts ~335) chains keywords→node search→generation→validateAndRepair→positioning with up to 3 repair passes.",
        "maturity": "solid"
      },
      {
        "component": "Durable Smithers coding-task runner (provision→turn-loop→approval→submit, parallel fan-out, crash-resume)",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
        "whatItDoes": "runTaskWithSmithers spawns a per-task Bun + smithers-orchestrator subprocess; createTaskScript builds provision step + per-agent wf.loop (until done / maxIterations onMaxReached return-last) optionally wf.parallel across agents + approval + submit; delegates each step to the host TaskStepExecutor over stdin/stdout; assembles TaskRunResult (status completed/incomplete/denied, turns, agentsDone, workspace, submit, metrics). force:false + stable runId gives crash-resume. resolveSmithersDbConfig mirrors the workflow engine.",
        "maturity": "solid"
      },
      {
        "component": "SmithersTaskExecutor (ACP-backed step executor) + detectTurnDone",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-executor.ts",
        "whatItDoes": "Implements TaskStepExecutor against a minimal AcpLike (spawnSession/sendPrompt/findResumableSessionByLabel); lazily spawns or reattaches a session by label for durable resume, sends initial vs continue prompt per turn, detectTurnDone classifies stopReason (max/length/interrupt → not done), records lastResponse/lastError, pluggable provision/approval/submit callbacks.",
        "maturity": "solid"
      },
      {
        "component": "Smithers task integration seam + production wiring",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-integration.ts",
        "whatItDoes": "shouldUseSmithersTaskRunner (default on, ELIZA_ORCHESTRATOR_SMITHERS=0 to disable); acpServiceToAcpLike adapts AcpService; runDurableTask drives an already-spawned session through runTaskWithSmithers at maxTurns:1 (single-turn drop-in) and rethrows executor.lastError. Wired in actions/tasks.ts runCreate→runPromptViaSmithers (line ~591/410).",
        "maturity": "partial"
      },
      {
        "component": "Smithers task type contracts",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-types.ts",
        "whatItDoes": "Defines TaskRunSpec (taskId/runId/provision/submit/approvalBeforeSubmit/maxTurns/parallelAgents), TaskStepContext, TaskStepExecutor seam, TaskRunResult/TaskRunMetrics. Documents the full provision→loop→approval→submit→summarize model.",
        "maturity": "solid"
      },
      {
        "component": "Trajectory link service for spawned sub-agents",
        "path": "plugins/plugin-agent-orchestrator/src/services/spawn-trajectory.ts",
        "whatItDoes": "buildLinkedSpawnMetadata/Env + spawnWithTrajectoryLink threading parent/child trajectory step IDs through metadata + env so a spawned coding sub-agent's trajectory links to the parent step. This is the trajectory-capture plumbing for the orchestration path (not Smithers-specific).",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-runner.test.ts",
        "kind": "integration",
        "covers": "REAL Smithers subprocess (8 cases, 60s timeouts, verified passing in 25.8s): single-turn complete, multi-turn loop until done, maxTurns→incomplete, provision+approval(approved)+submit, approval-denied→denied+submit skipped, parallel fan-out (2 agents×2 turns), fatal turn failure propagation, durable crash-resume (SIGKILL mid-turn-3 then resume same runId skips completed turns).",
        "gaps": "Uses a FakeExecutor, not the real ACP coding agent. No assertion on metrics.durationMs/retries accuracy beyond shape. Does not test >2 parallel agents or interaction with the workspace/submit GitHub path."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-executor.test.ts",
        "kind": "unit",
        "covers": "detectTurnDone classification; SmithersTaskExecutor lazy spawn + initial prompt, session reuse + continue prompt, reattach-by-label (durable resume), turn-error throw, injected approval/submit callbacks. Uses a FakeAcp.",
        "gaps": "No test against the real AcpService/NativeAcpClient. continuePrompt default behavior across many turns not stressed."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-integration.test.ts",
        "kind": "integration",
        "covers": "REAL Smithers subprocess (3 cases, 60s): runDurableTask completes+captures response (sendPrompt), fallback to sendToSession, prompt-error propagation as run failure; shouldUseSmithersTaskRunner env gating.",
        "gaps": "Only single-turn (matches production wiring) — does not exercise the integration layer driving provision/submit/parallel/multi-turn, because runDurableTask hardcodes maxTurns:1."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/smithers-db-backend.test.ts",
        "kind": "unit",
        "covers": "resolveSmithersDbConfig (sqlite default / postgres / pglite / unknown→sqlite) and a re-implemented copy of the inline subprocess layer-selection branch incl. feature-detect fallback when Smithers.postgres/pglite absent.",
        "gaps": "Tests a copy of the inline script logic, not the actual string executed in the subprocess; no live postgres/pglite backend run."
      },
      {
        "path": "plugins/plugin-workflow/__tests__/unit/smithers-runtime.test.ts",
        "kind": "integration",
        "covers": "REAL Smithers subprocess via fixtures/smithers-runtime-case.ts (4 cases, verified passing 9.8s): parallel-level fan-out + data routing through DAG + engine metrics (nodes/levels/maxConcurrency), n8n retry, continueOnFail error item, fatal-fail run rejection.",
        "gaps": "No trigger-mode/schedule-mode plan tested here; no postgres/pglite backend; no large/deep graph stress."
      },
      {
        "path": "plugins/plugin-workflow/__tests__/unit/embeddedWorkflowService.test.ts",
        "kind": "integration",
        "covers": "9 cases incl. PGlite-backed CRUD + executeWorkflow end-to-end through Smithers, AND reads back the persisted Smithers sqlite tables (smithers_0000_manual, smithers_0001_set, smithers_eliza_workflow_result) to verify durable per-node state + engine provider/metrics.",
        "gaps": "Single linear Set-node workflow; does not cover real connector nodes (gmail/stripe) executing, multi-trigger, or webhook-driven runs."
      },
      {
        "path": "plugins/plugin-workflow/__tests__/unit/smithers-db-backend.test.ts",
        "kind": "unit",
        "covers": "Same shape as the orchestrator db-backend test: resolveSmithersDbConfig + re-implemented layer-selection branch with feature-detect fallback.",
        "gaps": "Tests a copy of the inline logic, not the executed subprocess string; no live non-sqlite backend."
      },
      {
        "path": "plugins/plugin-workflow/__tests__/unit/generation.test.ts",
        "kind": "unit",
        "covers": "33 cases of the RAG generation pipeline (extractKeywords/matchWorkflow/etc.) with MOCKED useModel returning canned structured outputs.",
        "gaps": "No real-LLM call anywhere — generation correctness against an actual model (gpt-oss-120b/claude) is unverified; no trajectory captured."
      },
      {
        "path": "plugins/plugin-workflow/__tests__/unit/workflow-dispatch.test.ts",
        "kind": "unit",
        "covers": "WORKFLOW_DISPATCH service: blank-id reject, missing-service error, idempotency-key stripping, dedup row, concurrent-collapse, failure wrapping, stoppable registration (7 cases).",
        "gaps": "Does not run an actual workflow through dispatch→Smithers; executeWorkflow is mocked."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts, __tests__/live/native-acp-smoke.live.test.ts, tests/e2e/acp-codex-smoke.mjs",
        "kind": "live",
        "covers": "Live ACP loop gated by RUN_LIVE_ACPX=1 / RUN_LIVE_NATIVE_ACP=1 against real acpx+codex (spawn→task_complete→SubAgentRouter→synthetic memory). Real coding-agent execution.",
        "gaps": "These exercise the ACP/router path, NOT the Smithers durable runner. No live test drives runTaskWithSmithers/runDurableTask against a real coding agent end-to-end; all default-skipped without env+binary."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No real-LLM scenario-runner trajectory exists for either Smithers path. No scenarios under packages/scenario-runner/test/scenarios or packages/test/scenarios drive TASKS_CREATE→runDurableTask or WORKFLOW→Smithers. orchestrator_lifecycle benchmark is keyword-scored conversation only (no real execution)."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured logger.info({src:'plugin:workflow:smithers',...}) in smithers-runtime.ts ~515 emits per-run engine metrics; subprocess stderr is surfaced on failure. Tests assert engine metrics in execution result. No captured/attached log artifact files for evidence though."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "TaskRunResult.metrics has durationMs/turns/agents/retries (a numeric summary), and the persisted Smithers sqlite stores per-node/per-iteration rows, but no rendered timeline/run-viewer artifact is produced or captured for this domain."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "None. No UI surface for Smithers run state is screenshotted; no audit:cloud or e2e:record artifacts for workflow/orchestrator Smithers runs."
      },
      {
        "type": "video",
        "exists": false,
        "where": "None. No video walkthrough of a Smithers-driven multi-step coding task or workflow run."
      },
      {
        "type": "report",
        "exists": true,
        "where": "plugins/plugin-agent-orchestrator/docs/ (SUBAGENT_FLOW_AND_PARITY.md, sub-agent-routing.md, multi-account-orchestration.md, orchestrator-buildout-followups.md) describe orchestration design; package CLAUDE.md/AGENTS.md document the Smithers task path. These are design docs, not run evidence."
      }
    ],
    "gaps": [
      {
        "title": "Orchestrator Smithers graph capability is built + unit-tested but NOT wired into production",
        "severity": "high",
        "description": "runTaskWithSmithers supports provision→multi-turn-loop→approval→submit with parallel agent fan-out, and the unit tests prove all of it works against the real Smithers subprocess. But the only production caller, runDurableTask (smithers-task-integration.ts) hardcodes maxTurns:1 with no provision/submit/approval/parallelAgents, and runCreate (actions/tasks.ts ~591) just uses it as a durable wrapper around a single prompt. Workspace provision/submit are separate TASKS sub-actions, not driven through the Smithers graph. So 'smithers workflows drive multi-step coding tasks' is true of the engine but false of the shipped product path.",
        "suggestedWork": "Either (a) wire a real multi-step config through to runDurableTask (pass maxTurns from the round-trip cap, provision:true when a workspace task, submit/approvalBeforeSubmit when PR submission is requested, parallelAgents for swarm tasks) and add a wiring test in actions/tasks.ts that asserts the spec passed to runTaskWithSmithers; or (b) explicitly document that the production path is intentionally single-turn and the graph is future capability. Add an integration test that runs the full provision→turns→approval→submit graph against the FakeAcp via the production runCreate path."
      },
      {
        "title": "No real-LLM trajectory / run-viewer evidence for any Smithers path",
        "severity": "high",
        "description": "All generation tests mock useModel; all runner tests use FakeExecutor/FakeAcp. No scenario-runner scenario exercises WORKFLOW generation+execution or TASKS_CREATE durable coding against a live model, so there is no JSON report + run viewer + native jsonl trajectory (the repo's stated Definition-of-Done evidence) for this domain.",
        "suggestedWork": "Add scenario-runner scenarios: (1) NL prompt → WORKFLOW create → activate → execute, asserting the Smithers engine metrics + a real node firing; (2) a coding task via TASKS_CREATE that runs through runDurableTask against a real ACP coding agent, capturing the trajectory jsonl + report. Run against gpt-oss-120b and claude. Land artifacts under .github/issue-evidence/."
      },
      {
        "title": "Non-sqlite Smithers backends (postgres/pglite) never run live",
        "severity": "medium",
        "description": "resolveSmithersDbConfig and the layer-selection branch are unit-tested, but the actual subprocess script's postgres/pglite path is feature-detected and the bundled smithers-orchestrator@0.22.0 reportedly lacks Smithers.postgres/pglite, so in practice it always degrades to sqlite. There is no test that a postgres/pglite-backed run actually completes and persists durable state.",
        "suggestedWork": "Add an integration test (gated, like the live ACP tests) that runs runTaskWithSmithers / runWorkflowWithSmithers with SMITHERS_DB_PROVIDER=pglite against a real pglite-capable smithers build and verifies durable resume from that backend, OR assert+document that non-sqlite is currently unsupported by the pinned version so the dead branch is intentional."
      },
      {
        "title": "db-backend tests assert a re-implemented copy of the inline subprocess script, not the executed string",
        "severity": "medium",
        "description": "Both smithers-db-backend.test.ts files re-declare selectSmithersLayer that 'replicates the inline branch from createTaskScript'. If the real String.raw script in smithers-runtime.ts/smithers-task-runner.ts drifts from the copy, the tests still pass while production breaks. The layer-selection logic in the actual executed subprocess is untested.",
        "suggestedWork": "Refactor the inline backend-selection into an exported pure function injected into the script (or export the script string and snapshot/grep it), so the test exercises the same code the subprocess runs. Add a live pglite/postgres subprocess run to close the loop."
      },
      {
        "title": "No end-to-end live test of runTaskWithSmithers against a real coding agent",
        "severity": "medium",
        "description": "The live tests (sub-agent-router.live, native-acp-smoke, acp-codex-smoke.mjs) exercise the ACP/router path directly, bypassing the Smithers runner. There is no live test that drives the durable Smithers task runner against a real codex/claude/opencode session and verifies a real code change + crash-resume on a real workspace.",
        "suggestedWork": "Add a gated live e2e (RUN_LIVE_ACPX=1) that calls runDurableTask/runTaskWithSmithers with a real AcpService, has a coding agent make a trivial file edit across 2 turns, kills the subprocess mid-run, and verifies resume continues the same session/workspace. Capture stdout protocol + final diff as evidence."
      },
      {
        "title": "RAG generation correctness unverified against real models; no concurrency/many-workflows stress",
        "severity": "medium",
        "description": "generation.ts (keywords→catalog→match→feasibility→generate→repair) is only exercised with canned mock outputs. There is no test of generation quality, typeVersion-hallucination repair, or credential-block insertion against an actual model, and no stress test of many concurrent workflow executions through the per-run subprocess engine (resource/file-handle pressure of N sqlite subprocesses).",
        "suggestedWork": "Add a live generation eval (the plugin already exposes WORKFLOW eval_samples + a GEPA optimizer case file) wired to gpt-oss-120b/claude, asserting deploy-ready output. Add a concurrency test that executes 10 workflows simultaneously through EmbeddedWorkflowService and asserts isolation + no subprocess leak."
      },
      {
        "title": "No timeline/screenshot/video evidence for Smithers-driven runs",
        "severity": "low",
        "description": "TaskRunResult.metrics and the persisted Smithers sqlite give a numeric/structural record, but the orchestrator/task UI views are not screenshotted and no run timeline or video walkthrough is captured for a Smithers multi-step run, which the repo's evidence standard requires for agent-flow changes.",
        "suggestedWork": "When wiring the multi-step path, capture a before/after of the orchestrator task widget + a run timeline (steps with durations from the metrics) and a short video of a Smithers-driven coding task, stored under .github/issue-evidence/."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-workflow/src/services/smithers-runtime.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/src/services/embedded-workflow-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/src/services/workflow-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/src/utils/generation.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-executor.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-integration.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-types.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/actions/tasks.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-runner.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-executor.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-integration.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/smithers-db-backend.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/__tests__/unit/smithers-runtime.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/__tests__/fixtures/smithers-runtime-case.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/__tests__/unit/embeddedWorkflowService.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-workflow/__tests__/unit/generation.test.ts"
    ]
  },
  {
    "domain": "Multi-account quota tracking + switching (codexbar-style) — the AccountPool bridge: per-provider account selection by strategy (priority/round-robin/least-used/quota-aware), live session/weekly usage % probing for Claude + Codex, per-account credential injection on coding-agent spawn, usage attribution, and health-based switching.",
    "summary": "A real, well-architected multi-account pool exists and is wired end-to-end for Claude (anthropic-subscription) and Codex (openai-codex), plus opencode→cerebras-api. The brain is AccountPool (packages/app-core/src/services/account-pool.ts) with four strategies; usage % is genuinely fetched live from provider endpoints via pollAnthropicUsage/pollCodexUsage (packages/app-core/src/services/account-usage.ts) and stored as LinkedAccountUsage.sessionPct/weeklyPct. The orchestrator plugin reads the shared CODING_AGENT_SELECTOR_BRIDGE_SYMBOL bridge installed by coding-account-bridge.ts; on spawn AcpService.spawnSession selects an account, injects per-account env (CLAUDE_CODE_OAUTH_TOKEN / per-account CODEX_HOME+auth.json / CEREBRAS_API_KEY), drops parent keys, and stamps non-secret account meta on the session. Switching is driven by health flags (markRateLimited/markNeedsReauth on 401/403/429 and login_required) + filterEligible exclusion, plus burst-spread least-used. There is NO codexbar integration (the term appears nowhere). z.ai is the major gap: zai-coding/zai-api are tracked as pool accounts and key-validated, but NO sessionPct/weeklyPct is ever fetched for z.ai (no pollZaiUsage) — its usage probe only stamps {refreshedAt}, and z.ai/Kimi/GLM are deliberately not coding-agent selector candidates (no first-party CLI). Coverage is strong at the unit/offline-e2e level; live switching against ≥2 real accounts exists but is gated/opt-in and produces no captured trajectory/screenshot/video evidence in-repo.",
    "maturity": "partial",
    "existing": [
      {
        "component": "AccountPool selection brain (priority/round-robin/least-used/quota-aware, session affinity, burst-spread, health filtering, CRUD, mutations, keep-alive sweep)",
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/account-pool.ts",
        "whatItDoes": "Owns which LinkedAccountConfig serves a request. applyStrategy() implements all 4 strategies (quota-aware skips accounts with sessionPct>=85 via QUOTA_AWARE_SKIP_PCT=85; least-used sorts by usage.sessionPct then recency). filterEligible() excludes disabled/excluded/non-OK accounts and re-admits rate-limited ones past their reset. markRateLimited/markNeedsReauth/markInvalid/markHealthy mutate health. refreshUsage() routes to pollAnthropicUsage/pollCodexUsage; explicitly returns (no probe) for direct-API providers. sweepAccountPoolKeepAlive() refreshes subscription usage every ~5min; startAccountPoolKeepAlive wired in runtime boot.",
        "maturity": "solid"
      },
      {
        "component": "Live usage probes + JSONL day counters",
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/account-usage.ts",
        "whatItDoes": "pollAnthropicUsage() GETs https://api.anthropic.com/api/oauth/usage and parses five_hour (sessionPct) + seven_day (weeklyPct) utilization (handles flat + nested shapes). pollCodexUsage() GETs https://chatgpt.com/backend-api/wham/usage parsing rate_limit.primary_window.used_percent (sessionPct only; Codex has no weekly). recordCall() appends per-(provider,account,day) JSONL; readTodayCounters() aggregates calls/tokens/errors. NO z.ai/Kimi probe exists.",
        "maturity": "solid"
      },
      {
        "component": "Coding-agent selector bridge (app-core side)",
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.ts",
        "whatItDoes": "Installs globalThis bridge. AGENT_PROVIDER_CANDIDATES maps claude→[anthropic-subscription,anthropic-api], codex→[openai-codex,openai-api], opencode→[cerebras-api]; z.ai/Kimi/GLM intentionally absent. select() picks account, resolves token, materializes CODEX_HOME/auth.json+config.toml (TOML-injection guarded), returns envPatch. isAuthFailure() triages 401/403 vs transient. recordUsage/markRateLimited/markNeedsReauth delegate to pool.",
        "maturity": "solid"
      },
      {
        "component": "Orchestrator-side bridge reader (plugin)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
        "whatItDoes": "Reads the globalThis bridge without importing app-core. isMultiAccountAgentType (claude/codex/opencode only). selectCodingAccount() never throws, returns selection+non-secret meta. accountMetaFromSessionMetadata() parses stamped account. MULTI_ACCOUNT_AGENT_TYPES excludes zai/glm/kimi.",
        "maturity": "solid"
      },
      {
        "component": "Spawn-time selection + credential injection",
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
        "whatItDoes": "spawnSession() (around line 566-602) resolves strategy from ELIZA_CODING_ACCOUNT_STRATEGY, calls selectCodingAccount with sessionKey=session id, merges envPatch into customCredentials, stamps account meta, logs the selected account.",
        "maturity": "solid"
      },
      {
        "component": "Usage attribution + health-driven switching + account overview",
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
        "whatItDoes": "recordUsage() (811) sums per-turn tokens into the serving account via bridge.recordUsage. error/login_required handlers (641-690) call markSessionAccountUnhealthy → bridge.markRateLimited/markNeedsReauth (auth on 401/403, rate-limit on 429/quota; 529 deliberately excluded). getAccountOverview()/getRoomRoster() build the dashboard DTOs (strategy + availability + per-session assignments).",
        "maturity": "solid"
      },
      {
        "component": "Accounts REST API incl. z.ai handling + refresh-usage",
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/accounts-routes.ts",
        "whatItDoes": "/api/accounts CRUD; /refresh-usage drives pool.refreshUsage for OAuth subscription providers (canonical %), falling back to inline 1-token probes. probeCodingPlanKey (zai-coding) and probeDirectApiKey (zai-api) only GET /models to validate the key and set usage:{refreshedAt} — no sessionPct/weeklyPct for z.ai. Keeps zai-coding and zai-api in separate credential pools.",
        "maturity": "partial"
      },
      {
        "component": "Accounts dashboard widget (UI)",
        "path": "/Users/shawwalters/eliza/packages/ui/src/components/chat/widgets/agent-orchestrator-accounts-view.tsx",
        "whatItDoes": "Renders per-account 5h (sessionPct) + 7d (weeklyPct) usage bars (color by 60/85 thresholds), health dot, in-use count, strategy badge, availability chips, and per-room roster. UsageBar returns null when pct undefined — so z.ai accounts (no %) show no usage bar.",
        "maturity": "solid"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/account-pool.test.ts",
        "kind": "unit",
        "covers": "Provider-scoped resolution on id collision; scoped health mutations; refreshUsage parsing Codex used_percent→sessionPct; priority/round-robin/session-affinity selection; burst-spread least-used across 3 equal-usage accounts; quota-aware skips sessionPct>=85; least-used picks lowest sessionPct.",
        "gaps": "No weeklyPct selection test; no Anthropic usage-parse test (only Codex); no z.ai; no test that filterEligible re-admits a rate-limited account after its reset window; no concurrency/burst beyond 3."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.test.ts",
        "kind": "unit",
        "covers": "least-used Claude → CLAUDE_CODE_OAUTH_TOKEN; per-account CODEX_HOME/auth.json incl id_token; ELIZA_CODEX_MODEL TOML-injection guard; opencode→cerebras least-used→CEREBRAS_API_KEY; per-account usage delta via recordUsage; null when no accounts; rate-limited account skipped; describe() availability; isAuthFailure triage table.",
        "gaps": "All against in-memory pool with fake fetch — no live provider probe. No z.ai/Kimi path (none exists). No exclude-on-failure re-select test at the bridge level. No quota-aware-strategy test through the bridge."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/multi-account-spawn.test.ts",
        "kind": "unit",
        "covers": "Keystone: bridge consulted on spawn; Claude token injected + ANTHROPIC_API_KEY dropped; Codex per-account CODEX_HOME + OPENAI_API_KEY/OPENAI_MODEL dropped; opencode CEREBRAS_API_KEY; single-account fallback when no bridge; non-multi-account types skip bridge; two consecutive claude spawns land on DISTINCT accounts (rotating mock).",
        "gaps": "Bridge is a vi.fn mock — does not exercise the real pool/credential resolution from the spawn layer. No mid-task switch-on-rate-limit assertion (the markSessionAccountUnhealthy→re-select path is untested at this layer)."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/coding-account-selection.test.ts",
        "kind": "unit",
        "covers": "isMultiAccountAgentType (true claude/codex/opencode; false elizaos/pi-agent/zai/glm/kimi); strategy normalization incl quota-aware; account meta parse/null; selectCodingAccount null-without-bridge, skips non-multi types, returns meta without secrets, never throws on reject.",
        "gaps": "Pure contract-level; no integration with real bridge."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.live.test.ts",
        "kind": "live",
        "covers": "Gated by ORCHESTRATOR_LIVE_MULTI_ACCOUNT=1: least-used rotation hands out DISTINCT real accounts via exclude set; each envPatch secret is real (>8 chars); pool.refreshUsage returns numeric sessionPct for each connected Anthropic/Codex account.",
        "gaps": "Opt-in only (skipped + green in CI); requires ≥2 manually-linked accounts; produces no persisted artifact (just vitest assertions). Covers only anthropic-subscription + openai-codex; no z.ai, no opencode/cerebras live. Does not spawn a real sub-agent or test switch-on-429."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
        "kind": "e2e",
        "covers": "Offline composed E2E (test:e2e:multi-account): real AccountPool + real-format mock creds in temp dir → installed bridge → AcpService.spawnSession → REAL spawned fake-acpx subprocess. Asserts 10 checks: two claude spawns pick distinct accounts, each child gets distinct CLAUDE_CODE_OAUTH_TOKEN, parent ANTHROPIC/OPENAI keys dropped, Codex gets per-account CODEX_HOME with right token+account_id.",
        "gaps": "Subprocess is a fake acpx (no real LLM/coding work). Not run in CI lane automatically (manual bun script). No z.ai. No rate-limit→switch scenario. No timeline/trajectory artifact emitted (prints PASS/FAIL to stdout only)."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/live-codex-spawn-e2e.ts",
        "kind": "live",
        "covers": "Imports real ~/.codex login as pooled account, probes REAL session usage (logs sessionPct + resetsAt), bridge selects it + materializes CODEX_HOME, spawns a REAL Codex sub-agent that builds LIVE_PROOF.txt; reports rate-limit honestly. Spends real quota.",
        "gaps": "Single account (no rotation/switch). No z.ai (live-opencode covers cerebras). Manual only; output is console logs, not a captured/committed trajectory+timeline+screenshot. No assertion harness — informational."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/test/api/accounts-routes.test.ts",
        "kind": "unit",
        "covers": "Provider-scoped account patch on id collision; lists multiple accounts per provider; rejects external/unavailable subscription providers as imported API keys; coding-plan creds only on dedicated providers; z.ai coding-plan vs direct-API kept in separate credential pools (env ZAI_API_KEY/Z_AI_API_KEY set).",
        "gaps": "Does NOT test /refresh-usage producing sessionPct/weeklyPct; does not test z.ai usage % (because none is fetched); no test of the pool-vs-inline-probe fallback branch."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/ui/src/components/chat/widgets/__e2e__/orchestrator-accounts-fixture.tsx",
        "kind": "e2e",
        "covers": "Screenshot fixture (run via test:orchestrator-accounts-e2e → run-orchestrator-accounts-e2e.mjs, headless chromium → orchestrator-accounts.png) rendering 4 states: empty, accounts-with-usage-bars (Claude/Codex sessionPct+weeklyPct), assignments, room roster.",
        "gaps": "Mock props only — never wired to a real backend/pool. No z.ai account in the fixture. Screenshot is generated to a local output dir, not committed as PR evidence."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No committed real-LLM trajectory for multi-account selection/switching. live-codex-spawn-e2e.ts / live-opencode-build-e2e.ts spawn real sub-agents but emit only console logs (events list, OUTCOME block), not a saved scenario-runner JSON report / jsonl trajectory."
      },
      {
        "type": "screenshot",
        "exists": true,
        "where": "packages/ui/src/components/chat/widgets/__e2e__/run-orchestrator-accounts-e2e.mjs writes output/orchestrator-accounts.png of the accounts widget (4 states). Generated on demand, not committed; driven by mock props, not live data."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of account selection or live switching anywhere in repo (no test:e2e:record artifact for this domain)."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured [coding-account-bridge] selection logs (coding-account-bridge.ts:330), [AccountPool] keep-alive logs (account-pool.ts:998), and AcpService 'coding account selected for spawn' log (acp-service.ts:580). live-codex-spawn-e2e.ts prints LIVE usage sessionPct + selected account + materialized auth.json + build outcome. These are emitted at runtime / by live scripts but not captured to .github/issue-evidence."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline artifact (no per-account selection/switch timeline captured; the JSONL day counters in account-usage.ts are aggregates, not an evented timeline, and are not surfaced as evidence)."
      },
      {
        "type": "report",
        "exists": true,
        "where": "plugins/plugin-agent-orchestrator/docs/multi-account-orchestration.md is a thorough design+hitlist report (P0-P4 all checked) but is a design doc, not captured run evidence. .github/issue-evidence has NO multi-account/quota/codex/claude/zai artifacts."
      },
      {
        "type": "none",
        "exists": false,
        "where": "No z.ai-specific evidence of usage% tracking because z.ai usage% is never fetched."
      }
    ],
    "gaps": [
      {
        "title": "z.ai (and Kimi/GLM) usage % is never fetched or tracked — only key validation",
        "severity": "high",
        "description": "For zai-coding (probeCodingPlanKey) and zai-api (probeDirectApiKey) in accounts-routes.ts, the 'refresh usage' path only does GET /models to validate the key and writes usage:{refreshedAt} with NO sessionPct/weeklyPct. AccountPool.refreshUsage explicitly returns without probing for any non-subscription provider, and there is no pollZaiUsage anywhere. So the quota-aware/least-used strategies treat every z.ai account as sessionPct=0 (a.usage?.sessionPct ?? 0), the dashboard shows no usage bars for z.ai, and z.ai cannot be load-balanced by remaining quota. The prompt's question 'is z.ai tracked' answers: accounts are tracked, usage% is NOT.",
        "suggestedWork": "Add pollZaiUsage (and pollMoonshotUsage) to account-usage.ts hitting z.ai's quota/balance endpoint (or parse z.ai rate-limit response headers in probeDirectApiKey/probeCodingPlanKey into sessionPct), wire into AccountPool.refreshUsage for zai-api/zai-coding/moonshot-api, and add a unit test asserting sessionPct is populated. If z.ai exposes no usage endpoint, document that limitation explicitly and have the widget show 'usage unavailable' rather than a silent empty bar."
      },
      {
        "title": "Mid-task live account SWITCHING (rate-limit → re-select a different account) is not tested live or with captured evidence",
        "severity": "high",
        "description": "The exclude-on-failure design (P0 hitlist 'mark account + re-select excluding it') is realized only as health flagging: on 429/quota orchestrator-task-service.markSessionAccountUnhealthy marks the account rate-limited so the NEXT select() skips it (filterEligible). There is no test that an in-flight sub-agent whose account hits 429 actually re-spawns/continues on a different account, and no live trajectory/timeline proving a real switch under real quota exhaustion. The live tests only prove distinct accounts are handed out across fresh spawns via the exclude set.",
        "suggestedWork": "Add an integration test that injects a 429 'error' event for a running session and asserts (a) markRateLimited is called for that account, (b) a subsequent spawn for the same agentType selects a different account, and (c) the original is re-admitted after its reset. Add a gated live scenario that drives one account to its session cap and captures the switch as a scenario-runner trajectory + timeline."
      },
      {
        "title": "No committed end-to-end evidence (trajectory/video/timeline) for the multi-account feature",
        "severity": "medium",
        "description": ".github/issue-evidence has zero multi-account/quota artifacts. The live scripts (live-codex-spawn-e2e, live-opencode-build-e2e, coding-account-bridge.live.test) prove the real path but emit ephemeral console output / vitest assertions, and the accounts-widget screenshot is generated to a local dir from mock props. Per repo Definition of Done (AGENTS.md), agent/model changes need real-LLM trajectories + before/after screenshots + a video walkthrough — none are captured for this domain.",
        "suggestedWork": "Convert live-codex/live-opencode scripts to emit a scenario-runner JSON report + native jsonl, capture a before/after screenshot of the accounts widget populated by the REAL pool (not the mock fixture), and record a short video of two real sub-agents on distinct accounts with diverging usage bars. Land artifacts under .github/issue-evidence/<issue#>-multi-account.*."
      },
      {
        "title": "Offline composed E2E and live scripts are not in any automated CI lane",
        "severity": "medium",
        "description": "test:e2e:multi-account (compose-multi-account-e2e.ts, 10 real checks against a spawned subprocess) and the live scripts are manual bun invocations. They are not in run-all-tests.mjs / test:server / test:e2e, so a regression in the spawn-time credential-injection wiring (the harness already caught one real cli-transport bug) would not be caught by CI.",
        "suggestedWork": "Add compose-multi-account-e2e.ts to the test:e2e lane (it is hermetic — temp dir + fake acpx, no network/quota). Keep the live + real-quota scripts gated but document a periodic/manual schedule for them."
      },
      {
        "title": "Anthropic weekly usage (weeklyPct) parsing is untested and Anthropic probe has no unit test",
        "severity": "low",
        "description": "account-pool.test.ts only tests Codex usage parsing (used_percent→sessionPct). pollAnthropicUsage's dual-shape parsing (flat five_hour_utilization/seven_day_utilization vs nested) and weeklyPct extraction have no direct unit test, despite the UI rendering a 7d bar from weeklyPct. utilizationToPct's 0..1→0..100 multiply and normalizeResetTimestamp's sec/ms heuristic are also untested.",
        "suggestedWork": "Add account-usage.test.ts covering pollAnthropicUsage for both response shapes (asserting sessionPct + weeklyPct + resetsAt), pollCodexUsage, utilizationToPct edge cases, and normalizeResetTimestamp epoch-seconds-vs-ms."
      },
      {
        "title": "quota-aware strategy uses sessionPct only (ignores weeklyPct) and is not the runtime default",
        "severity": "low",
        "description": "applyStrategy's quota-aware branch filters on usage.sessionPct < 85 only; an account fine on its 5h window but near its 7d weekly cap is not deprioritized. The coding default is least-used (also sessionPct-only), so weeklyPct is fetched + displayed but never influences selection.",
        "suggestedWork": "Extend quota-aware (and optionally least-used) to consider weeklyPct (e.g. skip if max(sessionPct,weeklyPct) >= threshold) and add a test. Document the trade-off if intentional."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/packages/app-core/src/services/account-pool.ts",
      "/Users/shawwalters/eliza/packages/app-core/src/services/account-usage.ts",
      "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
      "/Users/shawwalters/eliza/packages/agent/src/api/accounts-routes.ts",
      "/Users/shawwalters/eliza/packages/ui/src/components/chat/widgets/agent-orchestrator-accounts-view.tsx",
      "/Users/shawwalters/eliza/packages/app-core/src/services/account-pool.test.ts",
      "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.test.ts",
      "/Users/shawwalters/eliza/packages/app-core/src/services/coding-account-bridge.live.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/multi-account-spawn.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/coding-account-selection.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/live-codex-spawn-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/live-opencode-build-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/docs/multi-account-orchestration.md",
      "/Users/shawwalters/eliza/packages/ui/src/components/chat/widgets/__e2e__/orchestrator-accounts-fixture.tsx",
      "/Users/shawwalters/eliza/packages/ui/src/components/chat/widgets/__e2e__/run-orchestrator-accounts-e2e.mjs",
      "/Users/shawwalters/eliza/packages/agent/test/api/accounts-routes.test.ts"
    ]
  },
  {
    "domain": "Orchestrator/task UI surfaces (plugins/plugin-task-coordinator) — OrchestratorWorkbench, CodingAgentTasksPanel, PTY consoles, orchestrator-stream/diff/plan/reasoning/markdown chat rendering, TaskCardList, CodingAgentSettingsSection",
    "summary": "plugin-task-coordinator is the display/control layer for coding-agent task threads and PTY sessions; it owns zero server runtime (all state lives in @elizaos/plugin-agent-orchestrator) and renders entirely as views + slot fills. The chat/coding UX is mature and well-built: OrchestratorWorkbench.tsx (3925 lines) renders a Claude-Code/Codex-style flowing conversation (user bubbles, flat agent markdown, structured tool-call cards with diffs/output, collapsible reasoning, plan/acceptance/usage inspector, recovery actions like retry/rerun/restart-with-edited-plan), driven by SSE streaming + debounced tail-refetch + active-poll. CodingAgentTasksPanel is the simpler task-coordinator landing; PtyConsoleBase/Drawer/SidePanel/TerminalPane render live PTY output (raw <pre> buffer cap 200k, plus an xterm.js pane). The conversation-builder transform (orchestrator-stream.helpers) is heavily unit-tested (118 unit tests pass overall). HOWEVER: the live e2e (test/orchestrator-workbench.live.e2e.test.ts) is STALE — it asserts test IDs that no longer exist in source (orchestrator-task-item, orchestrator-composer, orchestrator-new-task, orchestrator-create-dialog), so it is a no-op even when a stack is up. The screenshot harness (run-dashboard-shot.mjs) only shoots static TaskCard fixtures (not the real workbench/PTY/settings), its output is gitignored, and it is wired into no package.json script. The largest behavioral DOM test (TaskInspector terminal-task guards) currently FAILS TO LOAD (broken @elizaos/shared/voice-eot resolution), so `bun run test` exits non-zero and that suite does not run. No committed screenshot/video/trajectory evidence exists for this domain.",
    "maturity": "partial",
    "existing": [
      {
        "component": "OrchestratorWorkbench — multi-agent task room + chat UI",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/OrchestratorWorkbench.tsx",
        "whatItDoes": "3925-line workbench: task card list landing -> full-pane task room. Renders the coding-agent conversation (ConversationBlockView), TaskInspector (plan/acceptance/artifacts/usage/sub-agent cards/add-agent form/edit-plan-restart), OperatorDetailDrawer (input/output/events/usage tabs + retry/rerun recovery), SSE live streaming (client.streamOrchestratorTask) with debounced tail-refetch, active-poll (1.5s) vs idle-poll (5s), Esc-to-stop, stop-active running bar, mobile slide-over inspector via matchMedia. Tasks are started conversationally in chat (no in-view composer/create button by design).",
        "maturity": "solid"
      },
      {
        "component": "orchestrator-stream — conversation/tool-card renderer",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-stream.tsx",
        "whatItDoes": "Turns message/event records into a Claude-Code/Codex-style stream: user bubble (right), flat full-width agent markdown with identity marker, ToolCallCard (action verb, target, diff stat, status badge, exit code, duration, expandable ToolBody with DiffView/output), reasoning cells, system notices. Output clamping keeps both ends of long command output.",
        "maturity": "solid"
      },
      {
        "component": "buildConversation transform",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-stream.helpers.ts",
        "whatItDoes": "Coalesces agent_message_chunk rows into turns, merges tool_running events by session-scoped tool id, settles never-terminal tools after session end, keeps stderr/error turns distinct. The core data->view logic; heavily unit-tested.",
        "maturity": "solid"
      },
      {
        "component": "CodingAgentTasksPanel — task-coordinator landing + slot fill",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/CodingAgentTasksPanel.tsx",
        "whatItDoes": "Task-thread list (cards) -> full-pane detail (sessions, pending decisions, artifacts, coordinator decisions, events, ANSI-stripped transcript messages, delete/reopen). 404-tolerant (treats orchestrator-backend absence as empty, not error).",
        "maturity": "solid"
      },
      {
        "component": "PtyConsoleBase / Drawer / SidePanel + PtyTerminalPane",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/PtyConsoleBase.tsx",
        "whatItDoes": "Live PTY console: hydrates buffered output (client.getPtyBufferedOutput), subscribes to pty-output WS events, sends input/interrupt(Ctrl-C)/stop, 200k-char buffer cap, drawer/side-panel/full variants with a session sidebar. PtyTerminalPane is a richer xterm.js terminal (lazy import, FitAddon, resize observer, themed). Registered as slot fills via register-slots.ts.",
        "maturity": "solid"
      },
      {
        "component": "orchestrator-capabilities + index.ts capability manifest",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-capabilities.ts",
        "whatItDoes": "15 typed capability descriptors (status/list/open/create/pause/resume/pause-all/resume-all/delete/fork/update/validate/add-agent/stop-agent/send-message) with a dispatch map driving the voice/chat-driven GUI surfaces. Manifest<->dispatch parity is unit-asserted.",
        "maturity": "solid"
      },
      {
        "component": "CodingAgentSettingsSection (+ AgentTabsSection/LlmProviderSection/ModelConfigSection/GlobalPrefsSection/GitHubConnectionCard)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/CodingAgentSettingsSection.tsx",
        "whatItDoes": "Per-framework coding-agent settings (elizaos/pi-agent/opencode/claude/codex tabs): model, LLM provider, approval preset, GitHub connection. Filled into the @elizaos/ui slot. No dedicated unit/DOM test.",
        "maturity": "partial"
      },
      {
        "component": "orchestrator-markdown / reasoning / plan / diff renderers",
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-markdown.tsx",
        "whatItDoes": "MarkdownText (marked-based, link-protocol-sanitized), ReasoningCell (collapsible), plan/checklist block, DiffView/DiffStat with lineDiff/countDiff. Markdown safety + diff math are unit-tested; the reasoning/plan React components have no direct test.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/orchestrator-stream.test.ts",
        "kind": "unit",
        "covers": "buildConversation transform (chunk coalescing, tool-call merge by id, cross-session id isolation, stderr/error turns, reasoning delta coalescing, settling non-terminal tools after session end) and MarkdownText link-protocol sanitization. 534 lines, the strongest suite.",
        "gaps": "Pure data->block logic only; does not render ConversationBlockView/ToolCallCard/ReasoningCell as DOM or assert their visual/interactive behavior (expand/collapse, inspect)."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/orchestrator-inspector-terminal-task.test.tsx",
        "kind": "unit",
        "covers": "TaskInspector DOM behavior: terminal-status action-bar guards (done/failed/archived hide Edit group + priority), Pause/Resume/Fork/Restart/Validate/Archive/Reopen/Delete/CopyLink/SetPriority/AddAgent/inspect/stop callbacks, populated data display (sub-agent/plan/acceptance/artifact/usage).",
        "gaps": "CURRENTLY BROKEN: suite fails to load (Vite cannot resolve @elizaos/shared/voice-eot, transitively imported by @elizaos/ui) because vitest.config.ts aliases @elizaos/ui to source but not @elizaos/shared. This makes `bun run test` exit non-zero and the entire inspector behavioral suite does not run."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/CodingAgentTasksPanel.test.tsx",
        "kind": "unit",
        "covers": "CodingAgentTasksPanel DOM: list render with count chips, initial fetch params, empty state, search re-fetch, show-archived toggle, card-click detail pane with populated regions, back chip, delete/reopen via client calls.",
        "gaps": "Does not cover the orchestrator-backend-404 -> empty-state branch explicitly, nor the polling reconcile path."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/run-orchestrator-capability.test.ts",
        "kind": "unit",
        "covers": "All 15 orchestrator capability dispatch handlers (param coercion, requireTaskId, result wrapping, unknown-capability rejection). Plus capability-parity, params, interact-dispatch, diff, view-format suites.",
        "gaps": "Capability handlers tested against a mocked client; no end-to-end capability->backend->UI round trip."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/test/orchestrator-workbench.live.e2e.test.ts",
        "kind": "e2e",
        "covers": "INTENDED: drive real dev stack with Playwright, assert no page/console errors, no raw ACP JSON leak, no horizontal overflow, user message appears after send, create-task dialog open/close.",
        "gaps": "STALE / effectively dead: asserts test IDs that do not exist in current source (orchestrator-task-item, orchestrator-composer, orchestrator-new-task, orchestrator-create-dialog — workbench uses orchestrator-conversation-block and has no composer or create dialog by design). Gated on a running stack (skipped in CI), and the send/dialog tests early-return when those IDs/counts are absent, so it provides near-zero real coverage."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/test/coding-agent-codex-artifact.live.e2e.test.ts",
        "kind": "live",
        "covers": "INTENDED: real Codex CLI end-to-end artifact production.",
        "gaps": "Skipped unless `codex` binary in PATH and ~/.codex/auth.json exists; not run in CI; not a UI-surface test (validates backend artifact, not the coordinator views)."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs",
        "kind": "scenario",
        "covers": "esbuild+Playwright screenshot of TaskCard primitives (5 cards desktop/mobile + empty state) from dashboard-fixture.tsx with brand palette; asserts card count/titles + no page errors.",
        "gaps": "Shoots only static shared card primitives, NOT the real OrchestratorWorkbench, TaskInspector, conversation stream, PTY console, or settings. Output (dashboard-shots/) is gitignored. Wired into NO package.json script, so it never runs automatically and produces no committed evidence."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No scenario-runner trajectory exercises the orchestrator/task UI. The only live trajectory hook (coding-agent-codex-artifact.live.e2e) targets backend artifact production, not the views, and is skipped without a local Codex CLI."
      },
      {
        "type": "screenshot",
        "exists": true,
        "where": "Partial only: /Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs captures TaskCard fixtures, but output dir dashboard-shots/ is gitignored (src/__e2e__/.gitignore) and not committed. The only committed image is assets/hero.png (marketing). No screenshot of the real workbench, inspector, PTY console, or settings."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough for this domain. Root test:e2e:record (scripts/e2e-recordings/run-all.mjs) does not cover plugin-task-coordinator routes; nothing committed under .github/issue-evidence for task/orchestrator/pty."
      },
      {
        "type": "log",
        "exists": false,
        "where": "No captured structured [ClassName] backend logs or frontend console/network logs attached for the orchestrator/PTY code paths. The live e2e collects console/page errors at runtime but discards them (asserts empty), and is skipped without a stack."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline capture of a task lifecycle (open->active->tool calls->validate->done) as evidence. The UI renders a timeline live, but no recorded timeline artifact exists."
      },
      {
        "type": "report",
        "exists": false,
        "where": "No scenario JSON report / run viewer output for orchestrator UI flows."
      }
    ],
    "gaps": [
      {
        "title": "TaskInspector behavioral suite is broken and silently not running",
        "severity": "high",
        "description": "__tests__/unit/orchestrator-inspector-terminal-task.test.tsx fails to load: Vite cannot resolve @elizaos/shared/voice-eot (imported transitively via @elizaos/ui) because vitest.config.ts aliases @elizaos/ui to source but not @elizaos/shared (which is unbuilt: no dist/voice-eot). `bun run --cwd plugins/plugin-task-coordinator test` exits code 1 (1 failed suite | 11 passed; 118 tests pass) and the largest inspector DOM suite (action-bar guards + all control callbacks) does not execute. This is a regression-blind spot in the most action-rich part of the UI.",
        "suggestedWork": "Add an @elizaos/shared (and @elizaos/shared/* subpath) src alias to plugins/plugin-task-coordinator/vitest.config.ts mirroring the @elizaos/ui aliasing, or ensure shared is built before this suite. Then confirm the inspector suite runs green and wire it into CI so the failure cannot recur silently."
      },
      {
        "title": "Live workbench e2e is stale and provides ~zero real coverage",
        "severity": "high",
        "description": "test/orchestrator-workbench.live.e2e.test.ts asserts test IDs absent from current source (orchestrator-task-item, orchestrator-composer, orchestrator-new-task, orchestrator-create-dialog). The workbench renders orchestrator-conversation-block / orchestrator-task ... cards and has no in-view composer or create-task dialog (tasks are started conversationally in chat by design). So even with a running stack, openTask/send/dialog tests early-return or never find elements. It is also stack-gated (skipped in CI).",
        "suggestedWork": "Rewrite the e2e against the real DOM contract: open a TaskCard, assert orchestrator-message-list renders user/agent/tool blocks (orchestrator-user-message, orchestrator-agent-message, orchestrator-tool-call), exercise inspect-block -> OperatorDetailDrawer tabs, stop-active/Esc, inspector pause/resume/validate, and load-older pagination. Run it against bun run cloud:mock so it executes in CI, not only manually."
      },
      {
        "title": "No committed visual/trajectory/video/log evidence for the coding chat UX",
        "severity": "high",
        "description": "Per AGENTS.md, agent/UI changes require real-LLM trajectories, before/after full-page screenshots, a video walkthrough, and backend+frontend logs. For the orchestrator/task surfaces none of these are committed: dashboard-shots are gitignored and shoot only static cards; there is no scenario report, no run viewer, no video, no log capture of a real task lifecycle.",
        "suggestedWork": "Add a scenario-runner scenario that drives a real coding task end-to-end against a live model and capture: (1) JSON report + native jsonl trajectory, (2) full-page before/after screenshots of the workbench/inspector/PTY console (real components, not fixtures), (3) a recorded video walkthrough, (4) structured backend [Orchestrator...] logs + frontend console/network logs. Land artifacts under .github/issue-evidence/."
      },
      {
        "title": "PTY console UX (the live terminal coding experience) is entirely untested",
        "severity": "high",
        "description": "PtyConsoleBase/Drawer/SidePanel and PtyTerminalPane (xterm.js) have no unit, DOM, or e2e tests. Critical behaviors are unverified: buffered-output hydration, pty-output WS event handling, input/Enter/Ctrl-C interrupt/stop, 200k buffer trim, session-switch resubscribe, resize, and the xterm lazy-mount/dispose lifecycle. These are the surfaces that render the actual sub-agent terminal rooms.",
        "suggestedWork": "Add DOM tests with a mocked client (getPtyBufferedOutput, onWsEvent('pty-output'), sendPtyInput, subscribe/unsubscribe, resizePty, stopCodingAgent) asserting hydrate-on-mount, append-on-event, trimBuffer at the cap, sendLine/interrupt/stop dispatch, and unsubscribe-on-unmount. Add an e2e that opens a real PTY session and asserts streamed output + interrupt against the running stack."
      },
      {
        "title": "CodingAgentSettingsSection and its sub-sections are untested",
        "severity": "medium",
        "description": "CodingAgentSettingsSection, AgentTabsSection, LlmProviderSection, ModelConfigSection, GlobalPrefsSection, GitHubConnectionCard (per-framework model/provider/approval-preset/GitHub config, multi-account switching) have no unit/DOM tests. This is the surface that selects models and accounts (relevant to multi-model / multi-account switching coverage).",
        "suggestedWork": "Add DOM tests for tab switching across elizaos/pi-agent/opencode/claude/codex, model selection persisting via the prefs client, approval-preset changes, and GitHub connection card states (connected/disconnected/error). Cover the api/coding-agents-auth-sanitize and preflight-normalize helpers in an integration test of the settings flow (the helpers are unit-tested in isolation)."
      },
      {
        "title": "No concurrency / scale coverage for many simultaneous task rooms",
        "severity": "medium",
        "description": "The workbench polls status + per-task timeline, runs SSE streams, and active-polls at 1.5s. There is no test for many concurrent active tasks/sub-agents (e.g. 10 projects): cross-task contamination guards (detailReqRef token + selectedIdRef checks in fetchDetail/loadOlderTimeline) and the debounced refetch storm-guard are only covered indirectly, not under concurrent load.",
        "suggestedWork": "Add a test that rapidly switches selectedId across many tasks mid-flight and asserts no cross-task transcript merge (the token/selection guards hold), plus a load test of buildConversation with thousands of merged chunk/tool events for render-cost regressions."
      },
      {
        "title": "Reasoning/plan React renderers and TaskCardList lack direct tests",
        "severity": "low",
        "description": "orchestrator-reasoning (ReasoningCell collapse/streaming), orchestrator-plan (plan/checklist block), and TaskCardList primitives (TaskCard/TaskStatusChip/TaskMetaChip/SparseWatermark) are only exercised indirectly (via the stream test for markdown/diff, and the gitignored card screenshot fixture). No assertions on collapse behavior, streaming indicator, or status medallion mapping.",
        "suggestedWork": "Add focused DOM tests: ReasoningCell expand/collapse + streaming spinner, plan-step glyph/status mapping, and TaskStatusChip/TaskStatusMedallion icon+tone per status. Optionally commit the run-dashboard-shot output (or a curated subset) as visual baseline evidence."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/OrchestratorWorkbench.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-stream.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-stream.helpers.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/CodingAgentTasksPanel.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/PtyConsoleBase.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/PtyTerminalPane.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/PtyConsoleDrawer.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/CodingAgentSettingsSection.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/orchestrator-capabilities.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/index.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/register-slots.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/test/orchestrator-workbench.live.e2e.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/orchestrator-inspector-terminal-task.test.tsx",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/__tests__/unit/orchestrator-stream.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/vitest.config.ts"
    ]
  },
  {
    "domain": "Scenario-runner test harness (packages/scenario-runner) — executor, interceptor, judge, reporter/run-viewer, native-export, final-checks, schema; coding-task/sub-agent capability testing",
    "summary": "packages/scenario-runner is a mature, well-structured integration harness: it boots a real PGLite-backed AgentRuntime (live or deterministic-LLM-proxy), drives four turn kinds (message/action/api/tick, plus voice), intercepts every action handler into CapturedAction[] (params/result/error/artifacts/state-transitions/connector-dispatches/memory-writes/approvals), runs ~30 typed finalCheck handlers plus an LLM-as-judge (Cerebras gpt-oss-120b or runtime TEXT_LARGE), and emits a JSON aggregate + per-scenario bundle + a self-contained HTML run-viewer + eliza_native_v1 training-corpus JSONL derived from recorded trajectory files. Trajectory capture works (runtime auto-writes per-stage JSON when ELIZA_TRAJECTORY_DIR is set; reporter summarizes stages/tokens/cache/tool calls into the viewer). For CODING capability it is shallow: exactly ONE coding scenario exists (deterministic-coding-tools-actions: FILE/SHELL/WORKTREE direct action handlers in a seeded git repo), and it explicitly does NOT spawn sub-agents. The whole sub-agent/orchestration surface (plugin-agent-orchestrator TASKS spawning claude-code/codex/opencode over PTY, smithers workflows, sweagent) is excluded from the harness by design (LIVE_ONLY_REMAINDER) and only unit-tested. The harness captures NO real screenshots/video/timeline — the \"screenshot\"/\"frontendScreenshot\" artifact fields are just string lengths from mocked computer-use, browser-screenshot scenarios assert a canned string, and real visual capture lives in a separate Playwright lane (packages/app/test/ui-smoke, cloud-frontend e2e) that scenario-pr-workflow.test.ts only checks is wired. No build-validation, no multi-project concurrency, no app-create-and-reload, no rollback, and no multi-model/account-switching scenarios exist.",
    "maturity": "partial",
    "existing": [
      {
        "component": "runScenario executor (turn loop, seeds, requires-gate, finalChecks, judge)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/executor.ts",
        "whatItDoes": "Core execution: resolves rooms, runs seed steps (custom/advanceClock/seed-dispatch), gates on requires.plugins, runs message/action/api/tick/voice turns with per-turn timeout, runs per-turn assertions (assertResponse/assertTurn/responseIncludesAny/forbiddenActions/responseJudge) and finalChecks, synthesizes an implicit non-success REPLY when a message turn emits text but the LLM selected no action, mounts a fake computeruse service, starts an in-process loopback HTTP server for api turns, resolves {{now}}/{{definitionId}}/{{occurrenceId}} template tokens.",
        "maturity": "solid"
      },
      {
        "component": "attachInterceptor (CapturedAction capture)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/interceptor.ts",
        "whatItDoes": "Idempotently wraps every runtime action handler to capture actionName/parameters/result/error; wraps createMemory (memoryWrites) and createTask (approval-request inference); derives connector dispatches (MESSAGE/VOICE_CALL), state transitions (browser_task completed/needs_human, intervention), and artifacts (screenshot/frontendScreenshot/path/attachments) — but only as strings/lengths, never real binaries.",
        "maturity": "solid"
      },
      {
        "component": "final-checks registry",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/final-checks/index.ts",
        "whatItDoes": "~30 typed handlers: actionCalled, selectedAction, selectedActionArguments, memoryWriteOccurred, approvalRequestExists, approvalStateTransition, connectorDispatchOccurred, messageDelivered, draftExists, uploadedAssetExists, browserTaskCompleted/NeedsHuman, noSideEffectOnReject, push*, clarificationRequested, interventionRequestExists, workflowDispatchOccurred, gmail* (mock-ledger backed), custom (predicate), judgeRubric (deferred to executor). Unknown type or unknown field fails loudly via FINAL_CHECK_KEYS strict-key validation.",
        "maturity": "solid"
      },
      {
        "component": "LLM-as-judge",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/judge.ts",
        "whatItDoes": "judgeTextWithLlm scores a candidate (response text + structured action trace) against a rubric 0..1 using Cerebras gpt-oss-120b when enabled else runtime TEXT_LARGE; retry-on-unparseable (2 retries), no heuristic fallback — throws if neither model works. Transport in cerebras-judge.ts.",
        "maturity": "solid"
      },
      {
        "component": "reporter (buildAggregate/writeReport/writeReportBundle/writeScenarioRunViewer)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/reporter.ts",
        "whatItDoes": "Builds the aggregate report, writes matrix.json + per-scenario JSON + a self-contained HTML run-viewer (index.html + data.js) that renders scenarios, turn tables, call-by-call trajectory stage summaries (latency/tokens/cache%/tool name+success/preview), and eliza_native_v1 rows. Pure text/JSON; embeds no images or video.",
        "maturity": "solid"
      },
      {
        "component": "native-export (trajectory → training corpus)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/native-export.ts",
        "whatItDoes": "exportScenarioNativeJsonl reads recorded per-stage trajectory JSON under <runDir>/trajectories/, converts each model-boundary stage to an eliza_native_v1 row, stamps each row with the scenario's pass/fail/skip outcome (so failed runs aren't exported as gold), and writes a manifest with counts.",
        "maturity": "solid"
      },
      {
        "component": "schema (turn kinds, finalCheck types, lanes, scenario())",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/schema/index.d.ts",
        "whatItDoes": "ScenarioDefinition/ScenarioTurn/CapturedAction types; FINAL_CHECK_KEYS strict-key map; scenario() validator enforcing finalCheck field whitelist + lane validity. ScenarioTurn.kind is an open string but executor only supports message|action|api|tick|voice.",
        "maturity": "solid"
      },
      {
        "component": "CLI (eliza-scenarios run/list)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/cli.ts",
        "whatItDoes": "Discovers/loads/filters by lane, single shared runtime per process (PGLite cannot be recreated), sets ELIZA_TRAJECTORY_DIR/ELIZA_LIFEOPS_RUN_ID/ELIZA_LIFEOPS_SCENARIO_ID so the recorder tags trajectories, writes report + run-viewer + native jsonl, enforces SKIP_REASON loud-skip policy and exit codes.",
        "maturity": "solid"
      },
      {
        "component": "runtime-factory",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/runtime-factory.ts",
        "whatItDoes": "createScenarioRuntime boots a PGLite runtime with mocked test environment, deterministic-LLM-proxy or a live provider, embedding fallback, disabled background schedulers. Does NOT itself instantiate a trajectory recorder — that happens in packages/core/src/services/message.ts via ELIZA_TRAJECTORY_DIR.",
        "maturity": "solid"
      },
      {
        "component": "deterministic-coding-tools-actions scenario (the ONLY coding scenario in this harness)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
        "whatItDoes": "Seeds an isolated git repo, registers plugin-coding-tools, and drives 5 message turns through strict-LLM-proxy fixtures to exercise FILE write/read, SHELL run, WORKTREE enter/exit; asserts exact handler params, success, byte counts, exit codes, and real filesystem side-effects + cleanup. Tests built-in coding TOOLS, not sub-agent orchestration or build validation.",
        "maturity": "solid"
      },
      {
        "component": "deterministic-action-coverage gate",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/__tests__/deterministic-action-coverage.test.ts",
        "whatItDoes": "Ratchets keyless action coverage against live plugin action surfaces; explicitly documents plugin-agent-orchestrator's TASKS (spawns ACP coding sub-agents over PTY) as LIVE_ONLY_REMAINDER excluded from the keyless harness and only unit-covered — the authoritative statement that sub-agent orchestration is not scenario-tested here.",
        "maturity": "solid"
      },
      {
        "component": "app-create scenario (orchestration NOT exercised)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/test/scenarios/app-create-no-existing.scenario.ts",
        "whatItDoes": "Asserts the APP action selects create mode for a scaffold intent, but the file comment states the orchestrator is not exercised and START_CODING_TASK is not registered in the test runtime, so a 'could not dispatch' outcome is accepted — i.e. the create→spawn-coding-agent path is unverified.",
        "maturity": "stub"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/executor.test.ts",
        "kind": "unit",
        "covers": "Executor turn handling, assertions, finalCheck dispatch, synthesized-REPLY behavior, template resolution.",
        "gaps": "No coding-task / sub-agent spawn paths; no build-validation; no multi-project concurrency."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/interceptor.test.ts",
        "kind": "unit",
        "covers": "Action/memory/task wrapping, connector-dispatch and artifact derivation.",
        "gaps": "Artifact capture only validated for string fields (screenshot length, path), never a real binary/image."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/reporter.test.ts",
        "kind": "unit",
        "covers": "buildAggregate + writeScenarioRunViewer producing a self-contained viewer with reports, trajectory stage summaries, and native rows.",
        "gaps": "Viewer is text/JSON only — no test for embedding screenshots/video/timeline because the harness cannot capture them."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/native-export.test.ts",
        "kind": "unit",
        "covers": "recordedTrajectoryToNativeRows + exportScenarioNativeJsonl outcome stamping and manifest counts.",
        "gaps": "None for its scope; only covers model-boundary rows, not sub-agent/coding trajectories (none are produced)."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
        "kind": "scenario",
        "covers": "Built-in FILE/SHELL/WORKTREE coding-tool actions with real git-repo side effects under the deterministic proxy.",
        "gaps": "No sub-agent spawn (claude-code/codex/opencode), no smithers workflow, no app/plugin create+reload, no build/typecheck/test validation of generated code, no error/rollback, no concurrency."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/src/scenario-pr-workflow.test.ts",
        "kind": "unit",
        "covers": "Asserts the separate Playwright/visual CI lanes (packages/app ui-smoke, cloud-frontend e2e) and screenshot-quality guards are wired in the PR workflow.",
        "gaps": "Confirms by negative space that real screenshot/video capture lives OUTSIDE scenario-runner; the scenario harness produces none."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__",
        "kind": "unit",
        "covers": "~80 unit test files for the sub-agent orchestrator (TASKS/ACP/PTY) — the actual code-orchestration engine.",
        "gaps": "Unit-only; never driven through the scenario-runner, so no end-to-end coding trajectory/judge/report/native-export evidence for spawning real coding sub-agents."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": true,
        "where": "Per-stage RecordedTrajectory JSON under <runDir>/trajectories/<agentId>/<id>.json (auto-written by packages/core/src/services/message.ts when ELIZA_TRAJECTORY_DIR is set by the CLI), summarized into the viewer and converted to eliza_native_v1 JSONL by native-export.ts. Covers in-process model/tool/toolSearch stages only — not sub-agent processes."
      },
      {
        "type": "report",
        "exists": true,
        "where": "matrix.json + per-scenario JSON bundle (reporter.writeReport/writeReportBundle) and aggregate totals; printStdoutSummary markdown table."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured [scenario-runner]/[eliza-scenarios]/[scenario-judge] logger output throughout executor.ts, cli.ts, judge.ts, native-export.ts."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "NOT captured by this harness. CapturedAction.screenshot/frontendScreenshot are only string-length records from a mocked computer-use service (executor.ts createScenarioComputerUseService) and a canned BROWSER_SCREENSHOT string; real screenshots live in the separate Playwright lane (packages/app/test/ui-smoke, packages/cloud-frontend/tests/e2e)."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video capture anywhere in packages/scenario-runner; no Playwright/ffmpeg/webm/mp4 path. Video walkthroughs are a separate test:e2e:record lane outside this domain."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "Only coarse per-turn durationMs and per-stage latencyMs/timestamp in the trajectory summary; no first-class timeline artifact (no Gantt/event timeline) and no cross-process orchestration timeline."
      }
    ],
    "gaps": [
      {
        "title": "No sub-agent / coding-agent orchestration scenarios in the harness",
        "severity": "critical",
        "description": "The product's headline capability — orchestrating claude-code/codex/opencode/eliza sub-agents and smithers workflows via plugin-agent-orchestrator TASKS over PTY/ACP — has ZERO scenario-runner coverage. It is explicitly excluded as LIVE_ONLY_REMAINDER in deterministic-action-coverage.test.ts and only unit-tested in plugins/plugin-agent-orchestrator/__tests__. app-create-no-existing.scenario.ts confirms the create→spawn-coding-agent path is unverified (START_CODING_TASK not registered, 'could not dispatch' accepted).",
        "suggestedWork": "Add a new turn kind or seed-backed harness that can spawn a real (or recorded-replay) coding sub-agent: a scenario that issues a coding TASK, asserts a sub-agent room is created, captures the sub-agent's trajectory/tool calls, and verifies the produced diff. Add finalCheck types like subAgentSpawned, taskRoomCreated, diffProduced, buildPassed. Provide a deterministic ACP/PTY replay fixture (analogous to strict-llm-action-fixtures) so it can run keyless in CI, plus a live-only variant against real claude-code/codex."
      },
      {
        "title": "No build/typecheck/test validation of generated code",
        "severity": "critical",
        "description": "There is no finalCheck or turn that compiles, typechecks, or runs tests on code the agent writes. deterministic-coding-tools-actions only checks file bytes/exit codes; there is no proof that a created app/plugin/view actually builds or runs.",
        "suggestedWork": "Add a buildValidation final-check (run bun run build/typecheck/test in the seeded workspace and assert exit 0) and a coding scenario that scaffolds an app, builds it, and asserts the build artifact exists. Capture build logs as an artifact in the run-viewer."
      },
      {
        "title": "No real screenshot/video/UI capture for coding/orchestration UI surfaces",
        "severity": "high",
        "description": "The harness captures no real screenshots or video; CapturedAction artifacts are string lengths from mocked services. Orchestrator/task views, sub-agent rooms, generated app/view UIs, widgets, and notifications get no visual evidence from scenario-runner — visual proof only exists in a disconnected Playwright lane that does not see scenario state.",
        "suggestedWork": "Either (a) bridge scenario-runner to the Playwright/app ui-smoke lane so a scenario can drive the runtime and capture a real dashboard/orchestrator/sub-agent-room screenshot+video as a first-class artifact in the run-viewer, or (b) add a computer-use/browser capture step that writes real PNG/webm files into <runDir>/artifacts and renders them inline in writeScenarioRunViewer (currently text/JSON only)."
      },
      {
        "title": "No multi-project concurrency / scale scenarios",
        "severity": "high",
        "description": "Nothing exercises concurrent coding projects (the brief calls for up to 10 concurrent projects). The CLI uses a single shared runtime per process and PGLite cannot be recreated, so concurrency isn't even expressible in the current harness.",
        "suggestedWork": "Add an orchestration scenario (or a multi-process driver in scripts/run-scenarios-isolated.mjs style) that launches N concurrent coding tasks, asserts isolation between task rooms/worktrees, and produces a per-project timeline in the run-viewer."
      },
      {
        "title": "No create→modify→reload→rollback lifecycle scenarios",
        "severity": "high",
        "description": "No scenario covers creating an app/plugin/view, modifying it, hot-reloading it, hitting an error, and rolling back. app-create only asserts action selection; there is no edit/reload/error/rollback flow.",
        "suggestedWork": "Author lifecycle scenarios: APP create → VIEWS edit → reload assertion → inject a build error → assert error surfaced → rollback → assert prior state restored. Add finalCheck types for reloadOccurred and rollbackRestored."
      },
      {
        "title": "No multi-model / multi-account switching coverage",
        "severity": "medium",
        "description": "The judge can use Cerebras gpt-oss-120b and runtime TEXT_LARGE, but no scenario validates the agent running tasks across models (gpt-oss-120b, claude, codex, z.ai) or switching accounts mid-run. Provider selection is per-process only.",
        "suggestedWork": "Add a live-only scenario matrix that runs the same coding task under each provider and an account-switch scenario asserting the orchestrator routes to the selected model/account and records provider per stage (the native rows already carry provider, so add a finalCheck/report grouping by provider)."
      },
      {
        "title": "No first-class timeline artifact",
        "severity": "medium",
        "description": "Only durationMs/latencyMs/timestamp exist; there is no orchestration timeline (sub-agent spawn → tool calls → completion across processes), which the brief requires for evidence.",
        "suggestedWork": "Emit a timeline.json (ordered events: turn start/end, action start/end, sub-agent spawn/exit, build start/end) and render it as a horizontal timeline in writeScenarioRunViewer."
      },
      {
        "title": "No platform-targeted scenarios (cloud/desktop/AOSP/Debian/Play/iOS)",
        "severity": "medium",
        "description": "Scenarios run against an in-process PGLite runtime with no notion of target platform/device tier; nothing exercises coding/orchestration on remote containers per platform.",
        "suggestedWork": "Parameterize scenarios by target platform and add a remote-container driver; at minimum tag scenarios with target platform and add platform-specific finalChecks (e.g. native bridge availability), wiring to the os/ and remote-container infrastructure."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/packages/scenario-runner/src/executor.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/interceptor.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/final-checks/index.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/judge.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/reporter.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/native-export.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/cli.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/runtime-factory.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/schema/index.d.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/schema/index.js",
      "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/__tests__/deterministic-action-coverage.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/test/scenarios/app-create-no-existing.scenario.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/scenario-pr-workflow.test.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/reporter.test.ts"
    ]
  },
  {
    "domain": "E2E recording: screenshots, video, timelines (across cloud/desktop/AOSP/Debian/Play/iOS), with focus on recording coding/agent-orchestration flows end-to-end",
    "summary": "The repo has a mature, Playwright-based screenshot+video+trace recording stack and a custom contact-sheet/viewer pipeline, but it is built for UI visual auditing, not for recording a real coding/agent-orchestration flow end-to-end. The root `test:e2e:record` (scripts/e2e-recordings/run-all.mjs) runs 9 Playwright suites with E2E_RECORD=1 (video:on, trace:on, screenshot:on), extracts trace frames into per-test contact sheets, and builds an index.html viewer with per-test video links — this is solid for web/cloud/app UI. cloud-frontend's `audit:cloud` (aesthetic-audit.spec.ts) is the most polished surface: full-page desktop+mobile screenshots, hover screenshots, console/network-error capture, palette/radius linting, screenshot-quality gates, and auto-stubbed manual-review markdown per route. The app package has the broadest Playwright matrix (web, mobile-Pixel7, voice-mic, audit-app, electrobun-packaged desktop, android-cdp). The critical gap for THIS audit: there is NO harness that records a real coding session (claude-code/codex/opencode/smithers spawning, file edits, plugin reload, errors, rollback) as video+timeline. The closest spec, orchestrator-gui-workbench.spec.ts, is 100% mocked API; scenario-runner emits JSON reports + native JSONL trajectories but produces zero screenshots/video/timeline. There is also no recording for AOSP/Debian/Play/iOS remote-container flows (only a USB-installer wizard Playwright suite and an electrobun desktop screenshot assertion). All recorded output is gitignored (e2e-recordings/, test-results/, aesthetic-audit-output/), so no committed evidence exists for coding flows; .github/issue-evidence holds only two ad-hoc PNGs.",
    "maturity": "partial",
    "existing": [
      {
        "component": "Root e2e recording orchestrator (test:e2e:record)",
        "path": "/Users/shawwalters/eliza/scripts/e2e-recordings/run-all.mjs",
        "whatItDoes": "Runs the 9 UI_E2E_SUITES (suites.mjs) with E2E_RECORD=1, writing Playwright output into e2e-recordings/<pkg>/test-results, then invokes contact-sheet + viewer generators. Supports --packages, --skip-tests/sheets/viewer. This is the canonical full screenshot+video+trace capture entry point.",
        "maturity": "solid"
      },
      {
        "component": "E2E suite registry",
        "path": "/Users/shawwalters/eliza/scripts/e2e-recordings/suites.mjs",
        "whatItDoes": "Declares the recordable suites (app, cloud-frontend, cloud-e2e, homepage, os-homepage, os-usb-installer, ui-agent-surface, feed-dag-visualizer) plus covered-by/skipped lists. Each entry has configDir/script/recordEnv. Notably the app suite is forced into stub mode (ELIZA_UI_SMOKE_FORCE_STUB=1) so recordings are deterministic, not real.",
        "maturity": "solid"
      },
      {
        "component": "Contact-sheet + manifest generator",
        "path": "/Users/shawwalters/eliza/scripts/e2e-recordings/generate-contact-sheets.mjs",
        "whatItDoes": "Scans test-results for Playwright trace.zip files, extracts *-trace.trace, pulls non-blank screenshot frames into contact-sheets/<pkg>/<test>/frames/, discovers .webm video files, and writes e2e-recordings/manifest.json with {video, frames} per test. Skips tests with no trace or video.",
        "maturity": "solid"
      },
      {
        "component": "Viewer index generator",
        "path": "/Users/shawwalters/eliza/scripts/e2e-recordings/generate-viewer.mjs",
        "whatItDoes": "Reads manifest.json and builds e2e-recordings/index.html — a per-test card grid with a Video link (or 'no video') and frame thumbnails. This is the closest thing to a 'run viewer' for UI recordings; it has no notion of an agent/coding timeline.",
        "maturity": "partial"
      },
      {
        "component": "Cloud aesthetic audit (audit:cloud)",
        "path": "/Users/shawwalters/eliza/packages/cloud-frontend/tests/e2e/aesthetic-audit.spec.ts",
        "whatItDoes": "Walks ~60 cloud routes at desktop(1440x900)+mobile(390x844): full-page screenshot, --hover.png after hovering primary button, live :hover color sampling, palette(blue-ban, orange<->black)+border-radius linting, console-error + failed-request capture, sharp-based screenshot-quality gate, and auto-stubs manual-review/<slug>.md. Outputs report.json + contact-sheet.html. Auth via synthetic injected JWT + full /api/* route mocking.",
        "maturity": "solid"
      },
      {
        "component": "App UI-smoke Playwright config (web+mobile+voice+desktop+audit)",
        "path": "/Users/shawwalters/eliza/packages/app/playwright.ui-smoke.config.ts",
        "whatItDoes": "Defines projects: chromium, chromium-voice-mic (real WAV fed as fake mic), mobile-chromium (Pixel 7), audit-app (all-views aesthetic audit). Honors E2E_RECORD to flip trace/video/screenshot to 'on' and redirect outputDir into e2e-recordings/app. Boots the live app stack via app-core/scripts/playwright-ui-live-stack.ts.",
        "maturity": "solid"
      },
      {
        "component": "Orchestrator GUI workbench spec (coding-task UI)",
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/orchestrator-gui-workbench.spec.ts",
        "whatItDoes": "Drives the coding-task orchestrator surface: renders build-room with codex/cerebras sessions, inspector controls, retry/stop session, orchestrator-timeline assertions. Records video/trace via the ui-smoke config. BUT every /api/orchestrator/* call is mocked (taskSummary/taskDetail/timelinePage fixtures) — it captures the UI chrome of a coding flow, never a real sub-agent run.",
        "maturity": "partial"
      },
      {
        "component": "All-views aesthetic audit (audit:app)",
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/all-views-aesthetic-audit.spec.ts",
        "whatItDoes": "App-side analogue of the cloud aesthetic audit: walks ~50 app views x 2 viewports capturing screenshots; run via the dedicated audit-app project. Covers the app shell visually but not coding-flow dynamics.",
        "maturity": "solid"
      },
      {
        "component": "AI-QA capture spec + route catalog",
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/ai-qa-capture.spec.ts",
        "whatItDoes": "Captures per-route screenshots (desktop/mobile, light/dark) with a quality-retry helper and a button inventory, writing captures/<route>/ + a report under reports/ai-qa. trace/video are explicitly OFF here (screenshot-only inventory).",
        "maturity": "partial"
      },
      {
        "component": "Electrobun packaged desktop e2e",
        "path": "/Users/shawwalters/eliza/packages/app/playwright.electrobun.packaged.config.ts",
        "whatItDoes": "Drives the built desktop app binary (no webServer). trace/video/screenshot are retain-on-failure only. desktop-launch-render.e2e.spec.ts takes a real window screenshot via the harness bridge (/main-window/screenshot) and asserts non-blank. This is the only desktop-shell recording, and it is a single render assertion, not a flow video.",
        "maturity": "partial"
      },
      {
        "component": "Android CDP Playwright config + driver",
        "path": "/Users/shawwalters/eliza/packages/app/playwright.android.config.ts",
        "whatItDoes": "Runs Playwright over the Android WebView CDP socket (screenshot/trace retain-on-failure; notes they are slow over CDP). android-e2e.mjs handles emulator boot, APK install, voice-model staging via adb. No screenrecord/scrcpy video capture; recording on Android is effectively absent.",
        "maturity": "partial"
      },
      {
        "component": "Cloud full-stack mock e2e config",
        "path": "/Users/shawwalters/eliza/packages/cloud/e2e/playwright.config.ts",
        "whatItDoes": "Boots local cloud API + frontend with auth-cookie login and provisioning flows; honors recording flag to set trace/screenshot/video to 'on'. Feeds the cloud-e2e suite in the recording orchestrator.",
        "maturity": "solid"
      },
      {
        "component": "Live-test artifact-coverage gate",
        "path": "/Users/shawwalters/eliza/packages/scripts/check-live-test-artifact-coverage.mjs",
        "whatItDoes": "Enumerates every live/real/e2e package script and flags LLM-shaped ones lacking artifact evidence (recognizing scenario/e2e:record/remote-capabilities artifact patterns). Encodes the policy that UI suites yield screenshots/traces while model calls yield trajectory artifacts — but does not itself require coding-flow video/timeline.",
        "maturity": "partial"
      },
      {
        "component": "Scenario runner (real-LLM trajectory tool)",
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/bin/eliza-scenarios",
        "whatItDoes": "Runs .scenario.ts against a real AgentRuntime, emits JSON aggregate report (--report/--report-dir) and native JSONL trajectory (--export-native). This is the mandated real-LLM trajectory evidence per CLAUDE.md, but it produces NO screenshots, video, or visual timeline — purely textual turn/assertion data.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/packages/cloud-frontend/tests/e2e/aesthetic-audit.spec.ts",
        "kind": "e2e",
        "covers": "~60 cloud routes x desktop+mobile: full-page + hover screenshots, console/network errors, palette/radius lint, screenshot-quality gate, manual-review stubs",
        "gaps": "All /api/* mocked (deterministic empty/fixture states); no video, no real data, no multi-step interaction flow, no coding/orchestration surface"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/orchestrator-gui-workbench.spec.ts",
        "kind": "e2e",
        "covers": "Coding-task orchestrator UI: build-room sessions (codex/cerebras), inspector, retry/stop, orchestrator-timeline element; video/trace via E2E_RECORD",
        "gaps": "Fully mocked API — no real sub-agent spawn (claude-code/codex/opencode/smithers), no real file edits/plugin reload/error/rollback; no explicit screenshot assertions; timeline here is an in-UI widget, not a recording timeline"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/all-views-aesthetic-audit.spec.ts",
        "kind": "e2e",
        "covers": "~50 app views x 2 viewports screenshot audit (run via audit-app project)",
        "gaps": "Static per-view capture only; no flow video, no dynamic coding interaction"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/ai-qa-capture.spec.ts",
        "kind": "e2e",
        "covers": "Per-route screenshot inventory (desktop/mobile x light/dark) + button inventory + quality retry",
        "gaps": "trace/video explicitly OFF; screenshots only; no flow capture"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/electrobun-packaged/desktop-launch-render.e2e.spec.ts",
        "kind": "e2e",
        "covers": "Real packaged desktop window screenshot via bridge, asserts non-blank render",
        "gaps": "Single render screenshot; no walkthrough video of a coding flow on desktop; video only retain-on-failure"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
        "kind": "scenario",
        "covers": "Coding-tools actions exercised deterministically through scenario-runner (JSON report + JSONL trajectory)",
        "gaps": "Deterministic (no real model), and produces no screenshots/video/timeline; not wired into any visual recording"
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/playwright.android.config.ts",
        "kind": "e2e",
        "covers": "Android WebView via CDP; screenshot/trace retain-on-failure",
        "gaps": "No video, no adb screenrecord/scrcpy; recording effectively off on Android/AOSP"
      },
      {
        "path": "elizaOS/os:packages/os/usb-installer/tests/visual.spec.ts",
        "kind": "e2e",
        "covers": "USB installer wizard UI visual pages (desktop+mobile screenshots)",
        "gaps": "Only OS-image surface with Playwright; no Debian/AOSP runtime coding-flow recording; the OS-linux tree's recording hits are vendored Tails docs, not a harness"
      }
    ],
    "evidence": [
      {
        "type": "screenshot",
        "exists": true,
        "where": "aesthetic-audit.spec.ts / all-views-aesthetic-audit.spec.ts / ai-qa-capture.spec.ts write PNGs to packages/cloud-frontend/aesthetic-audit-output/{desktop,mobile}/ and e2e-recordings/<pkg>/, but output dirs are gitignored; only /Users/shawwalters/eliza/.github/issue-evidence/8810-*.png and 8812-*.png are committed (two ad-hoc UI PNGs, not coding flows)"
      },
      {
        "type": "video",
        "exists": true,
        "where": "Playwright video:on under E2E_RECORD writes .webm into e2e-recordings/<pkg>/test-results/<test>/; generate-viewer.mjs links them. Capability exists but is gitignored and only for mocked UI suites — no real coding-flow video captured anywhere"
      },
      {
        "type": "trajectory",
        "exists": true,
        "where": "scenario-runner emits JSON report (--report) + native JSONL (--export-native); a deterministic coding scenario exists (packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts) but no committed real-LLM coding trajectory under .github/issue-evidence"
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No recording/run timeline generator exists. 'timeline' in orchestrator-gui-workbench.spec.ts and the /api/orchestrator/.../timeline mock is an in-product UI activity feed, not a captured evidence timeline. generate-viewer.mjs has no timeline view"
      },
      {
        "type": "log",
        "exists": true,
        "where": "aesthetic-audit + cloud-e2e capture console errors + failed network requests into report.json; Playwright traces hold network/console. No structured backend [ClassName] log capture tied to a coding-flow recording"
      },
      {
        "type": "report",
        "exists": true,
        "where": "report.json / contact-sheet.html (aesthetic audits), e2e-recordings/manifest.json + index.html (viewer), reports/ai-qa/ (ai-qa). All gitignored; none cover a real coding/orchestration run"
      }
    ],
    "gaps": [
      {
        "title": "No end-to-end recording of a REAL coding flow (video + timeline)",
        "severity": "critical",
        "description": "Nothing records a real coding/agent-orchestration session as video+timeline. orchestrator-gui-workbench.spec.ts mocks every /api/orchestrator/* call; scenario-runner produces only JSON/JSONL with no visuals. There is no artifact that shows a sub-agent (claude-code/codex/opencode/smithers) actually being spawned, editing files, reloading a plugin/view, and finishing — synchronized with a video walkthrough.",
        "suggestedWork": "Build a coding-flow recording harness: a Playwright spec (or a wrapper around the orchestrator UI) that runs against a REAL local model + real sub-agent spawn, records video:on + trace:on + full-page screenshots at each milestone, and emits a per-step timeline.json (spawn, plan, edit, build, error, rollback, done) joined to the scenario-runner JSONL turn ids. Add a generate-viewer 'coding timeline' view that aligns video timestamp -> trajectory turn -> screenshot."
      },
      {
        "title": "Coding/orchestration suites not part of the recording orchestrator in REAL mode",
        "severity": "high",
        "description": "suites.mjs forces the app suite into ELIZA_UI_SMOKE_FORCE_STUB=1 and the only coding-surface spec (orchestrator-gui-workbench) is mocked. So even when test:e2e:record runs, it records a stubbed orchestrator, giving false confidence that coding flows are 'recorded'.",
        "suggestedWork": "Add a real-mode suite entry (e.g. 'orchestrator-live') that boots the live app stack + a real provider key + real coding-tools/agent-orchestrator services, records video, and is gated to run when a model key is present. Keep the stubbed run for determinism but clearly separate it in the viewer."
      },
      {
        "title": "No video/timeline capture on Android/AOSP, Debian, Play, or iOS remote-container coding flows",
        "severity": "high",
        "description": "Android Playwright (playwright.android.config.ts) is screenshot/trace retain-on-failure only with no adb screenrecord/scrcpy; iOS/Play remote-container coding has no Playwright/video harness; Debian/AOSP runtime has no recording (OS-linux 'recording' hits are vendored Tails docs). The only OS Playwright is the USB-installer wizard. Cross-platform coding-flow evidence is therefore impossible to produce today.",
        "suggestedWork": "Add device-level capture: adb screenrecord (Android/AOSP) and xcrun simctl io recordVideo (iOS) wrappers that wrap a coding-flow run and stitch the device video into the e2e-recordings viewer; add an Xvfb+ffmpeg screen-record wrapper for Debian/AOSP container runs. Register these as suites so test:e2e:record covers each platform."
      },
      {
        "title": "No committed coding-flow evidence; all recording output is gitignored",
        "severity": "medium",
        "description": "e2e-recordings/, test-results/, aesthetic-audit-output/, reports/ai-qa/ are all gitignored, so PRs cannot attach a coding-flow video/timeline as required by AGENTS.md. .github/issue-evidence/ has only two static UI PNGs and no coding trajectory/video.",
        "suggestedWork": "Define a small committed-evidence convention for coding flows: a compressed video + timeline.json + key screenshots + scenario JSONL bundle under .github/issue-evidence/<issue#>-coding-flow/, and a CI step (extending check-live-test-artifact-coverage.mjs) that fails coding/orchestrator PRs lacking the bundle."
      },
      {
        "title": "Recording timeline does not align video <-> trajectory <-> screenshots",
        "severity": "medium",
        "description": "generate-viewer.mjs produces a per-test card with a video link and trace frames but no synchronized timeline, and scenario-runner JSONL is entirely disconnected from the Playwright video. A reviewer cannot scrub a coding video to the moment a specific tool call/file edit/error happened.",
        "suggestedWork": "Emit a normalized timeline (Playwright video start ts + per-action ts from trace + scenario-runner turn timestamps) and render a scrubbable timeline in the viewer that maps video time to trajectory turn and to the screenshot taken at that step."
      },
      {
        "title": "Error/rollback/concurrency coding states are not visually recorded",
        "severity": "medium",
        "description": "orchestrator-gui-workbench covers retry/stop on mocked sessions, but there is no recorded capture of a real build error surfacing, a rollback being applied, or N concurrent coding projects (the Phase-3 goal of up to 10) running and being switched between, with video.",
        "suggestedWork": "Add coding scenarios that force a compile/test failure, a rollback, and 10 concurrent project rooms, each recorded with video + timeline + per-room screenshots, and assert the UI states (error banner, rollback diff, concurrent room rail) in the same spec so the recording is meaningful, not just chrome."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/scripts/e2e-recordings/run-all.mjs",
      "/Users/shawwalters/eliza/scripts/e2e-recordings/suites.mjs",
      "/Users/shawwalters/eliza/scripts/e2e-recordings/generate-contact-sheets.mjs",
      "/Users/shawwalters/eliza/scripts/e2e-recordings/generate-viewer.mjs",
      "/Users/shawwalters/eliza/packages/cloud-frontend/tests/e2e/aesthetic-audit.spec.ts",
      "/Users/shawwalters/eliza/packages/cloud-frontend/scripts/run-e2e.mjs",
      "/Users/shawwalters/eliza/packages/app/playwright.ui-smoke.config.ts",
      "/Users/shawwalters/eliza/packages/app/test/ui-smoke/orchestrator-gui-workbench.spec.ts",
      "/Users/shawwalters/eliza/packages/app/test/ui-smoke/all-views-aesthetic-audit.spec.ts",
      "/Users/shawwalters/eliza/packages/app/test/ui-smoke/ai-qa-capture.spec.ts",
      "/Users/shawwalters/eliza/packages/app/playwright.electrobun.packaged.config.ts",
      "/Users/shawwalters/eliza/packages/app/playwright.android.config.ts",
      "/Users/shawwalters/eliza/packages/app/scripts/android-e2e.mjs",
      "/Users/shawwalters/eliza/packages/cloud/e2e/playwright.config.ts",
      "/Users/shawwalters/eliza/packages/scripts/check-live-test-artifact-coverage.mjs",
      "/Users/shawwalters/eliza/packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
      "/Users/shawwalters/eliza/packages/app/test/electrobun-packaged/desktop-launch-render.e2e.spec.ts"
    ]
  },
  {
    "domain": "Dynamic plugin/view reload, build validation, undo/rollback, broken-reload recovery",
    "summary": "elizaOS has a solid build-validation pipeline (AppVerificationService: real typecheck/lint/test/build/launch/browser, structured-proof gating, per-check logs + screenshot) and a partial in-process runtime reload path. Reload is two-layered: (1) dev-only process bounce in packages/app-core/scripts/dev-ui.mjs (a source watcher that restarts the API child, not in-process HMR), and (2) a genuine in-process lifecycle: loadPluginFromDirectory cache-busts the ESM import and calls runtime.registerPlugin (which the plugin-lifecycle wrapper extends to register/unregister Plugin.views), exposed at POST /api/plugins/load-from-directory and invoked by VerificationRoomBridgeService after a passing verdict; plus applyPluginRuntimeMutation (config_apply -> plugin_reload -> runtime_reload -> restart_required tiers) and a runtime-ops manager with hot/warm/cold strategies. View bundles are served from disk (GET /api/views/:id/bundle.js) with mtime+size ETag and content-hash cache-busting, so a rebuilt bundle is picked up on next browser fetch. The big gaps: there is NO rollback/undo for code or runtime state — a broken live-load is surfaced honestly ('built and verified but live-load failed; reload the agent') but never reverted; applyPluginRuntimeMutation can unload plugins then fail mid-reload and fall back to a full restart without restoring the prior in-memory graph; and the cold strategy swaps the runtime before the health check with the manager explicitly noting that restoring the previous runtime would require a 'two-phase restart contract' that does not exist. There is no git-revert/snapshot of edited source — the only 'undo' in the codebase is the homescreen canvas history (a client-side scene-document op stack), which is unrelated to plugin/view code rollback. Evidence capture (trajectory/screenshot/video/timeline) specific to reload/rollback is essentially absent.",
    "maturity": "partial",
    "existing": [
      {
        "component": "AppVerificationService (build validation pipeline)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-verification.ts",
        "whatItDoes": "Runs a structured verification pipeline (typecheck/lint/test/build/launch/browser/structured-proof) against an app/plugin workdir via execFile, persists each check's full log + a Puppeteer screenshot to a verification dir, parses tsc/eslint/vitest output into diagnostics, validates a structured completion proof against actual check results, and returns a pass/fail verdict + retryablePromptForChild for the orchestrator to retry the coding agent.",
        "maturity": "solid"
      },
      {
        "component": "loadPluginFromDirectory / unloadPluginFromDirectory (in-process live reload)",
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/load-plugin-from-directory.ts",
        "whatItDoes": "Resolves a plugin's built JS entry (package.json module/main/exports -> dist/index.js), dynamically imports it with a ?t=Date.now() cache-buster so a rebuilt module re-evaluates, and calls runtime.registerPlugin (lifecycle wrapper also registers Plugin.views) so an edited+rebuilt plugin's actions/providers/views appear without an agent restart. unload delegates to runtime.unloadPlugin. Path-traversal/symlink-escape guarded. NOTE: it is explicitly NOT a build step — caller must have compiled first.",
        "maturity": "solid"
      },
      {
        "component": "/api/plugins/load-from-directory + unload-from-directory routes",
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/server.ts",
        "whatItDoes": "HTTP entry (lines ~2496-2580) that gates on isLocalCodeExecutionAllowed (store-variant block), validates an absolute directory, and delegates to loadPluginFromDirectory/unloadPluginFromDirectory. Returns 422 with the import error on failure. This is the wire the VerificationRoomBridge calls over loopback.",
        "maturity": "solid"
      },
      {
        "component": "VerificationRoomBridgeService (verify -> live-load -> chat verdict)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/verification-room-bridge.ts",
        "whatItDoes": "Subscribes to the SwarmCoordinator broadcast bus; on a passing PLUGIN_CREATE verdict it POSTs the workdir to /api/plugins/load-from-directory (loadPluginFromWorkdir) and posts a chat message: 'built, verified, and loaded live' on success, or 'built and verified ... but live-load failed: <err>. Reload the agent to pick it up.' on failure. Honest failure surfacing but no rollback.",
        "maturity": "solid"
      },
      {
        "component": "applyPluginRuntimeMutation (tiered reload-apply pipeline)",
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/plugin-runtime-apply.ts",
        "whatItDoes": "Diffs previous vs next resolved plugin graphs and applies the lightest change: in-place applyPluginConfig (config_apply), else unload+registerPlugin (plugin_reload), else full runtime restart (runtime_reload/restart_required) when a plugin has a DB adapter or lifecycle is unsupported. On a mid-reload throw it logs and falls back to restart — but does NOT restore plugins it already unloaded (no rollback of the in-memory graph).",
        "maturity": "partial"
      },
      {
        "component": "Runtime-ops manager + hot/cold strategies + classifier/health",
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/operations/manager.ts",
        "whatItDoes": "DefaultRuntimeOperationManager: single-flight, idempotency-keyed, phase-logged operation runner. Classifies intents into hot/warm/cold tiers, runs the strategy, then a health-check gate. createHotStrategy (reload-hot.ts) mutates env + best-effort plugin.applyConfig (~100ms, no reload). createColdStrategy delegates to an injected restartRuntime closure. CRITICAL: on a failed post-swap health check the manager comments it cannot restore the previous runtime ('requires a two-phase restart contract') — so cold reload has no rollback.",
        "maturity": "partial"
      },
      {
        "component": "View bundle build + disk serving with cache-busting",
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/views-routes.ts",
        "whatItDoes": "GET /api/views/:id/bundle.js serves the compiled view bundle from disk (getBundleDiskPath) with an mtime+size-derived ETag, 304 support, content-hash (?v=bundleHash) immutable caching when versioned else no-cache so a rebuilt bundle is revalidated/picked up. View bundles themselves are built by per-plugin Vite configs (vite.config.views.ts) producing dist/views/bundle.js; the runtime does not rebuild them — the coding agent's build step does.",
        "maturity": "solid"
      },
      {
        "component": "Dev source watcher (process-level hot-reload)",
        "path": "/Users/shawwalters/eliza/packages/app-core/scripts/dev-ui.mjs",
        "whatItDoes": "startAgentSourceWatcher watches */src dirs and bounces the API child via the supervisor when source changes, only when the agent is healthy, skipping bulk changes (>HOT_RELOAD_BULK_CHANGE_LIMIT, default 4) that look like a git reset/build. This is a full process restart (dev convenience), NOT in-process HMR, and is not the runtime reload path used in production.",
        "maturity": "solid"
      },
      {
        "component": "Plugin import staging (ESM module-graph cache busting on re-import)",
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/plugin-resolver.ts",
        "whatItDoes": "importPluginModuleFromPath stages a plugin into <stateDir>/plugins/.runtime-imports/<pkg>/<mkdtemp> on any re-import of an already-imported package name, so a mutated plugin gets a fresh staged URL that busts ESM's un-evictable module graph. Prunes stale staging dirs (keep newest N). Cold fast-path imports in place on first import. Underpins hot-reload correctness.",
        "maturity": "solid"
      },
      {
        "component": "VIEWS/APP create+edit dispatch -> coding agent + verification validator",
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/views-create.ts",
        "whatItDoes": "Multi-turn flow: scaffolds from min-plugin template (or locates existing source for edit), dispatches START_CODING_TASK with validator {service:'app-verification',method:'verifyPlugin',profile:'full'} and onVerificationFail:'retry', links a trajectory, and stamps originRoomId so the bridge can post the verdict. Ties together build-validate-reload but does not itself trigger the live reload (that is the bridge).",
        "maturity": "solid"
      },
      {
        "component": "Plugin lifecycle teardown (closest thing to per-plugin rollback)",
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/plugin-lifecycle.ts",
        "whatItDoes": "installRuntimePluginLifecycle wraps registerPlugin/unloadPlugin to track owned actions/providers/evaluators/services/routes/events/models/views and tear them down cleanly. restoreAdapterIfNeeded restores a prior DB adapter on unload. This is component-level cleanup, NOT a snapshot/rollback of a failed reload — there is no save-before / restore-after of the whole plugin graph.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/load-plugin-from-directory.test.ts",
        "kind": "unit",
        "covers": "load+register+unload of a built dir plugin; entry resolution (package.json main -> dist/index.js fallback); views-bearing plugin registers without throwing; path-traversal and symlink-escape rejection; clear error when no built entry; error when export is not a plugin.",
        "gaps": "Does NOT exercise the actual hot-reload semantic (load same dir twice after editing the file and assert the NEW code is live via ?t= cache-bust). No broken-reload recovery (load a plugin whose import throws mid-evaluation and assert the runtime is left in a consistent prior state). No concurrency (two reloads racing). No rollback."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/verification-room-bridge.test.ts",
        "kind": "unit",
        "covers": "verify->live-load->chat-verdict path: posts 'loaded live' on pass (asserts POST to /api/plugins/load-from-directory, never 'reinject'); 'build-passed-but-load-failed' honest verdict when load route returns 422 'import threw: bad export'; fail verdict with summary; boot-order attach retries; dedupe; stop() teardown.",
        "gaps": "Mocks fetch — never drives a REAL broken plugin through the real load route. Asserts only the chat TEXT on broken load, not that the runtime graph is unchanged/recovered. No assertion that a failed load did not partially register actions/views. No rollback verification."
      },
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/app-verification.integration.test.ts",
        "kind": "integration",
        "covers": "AppVerificationService.verifyApp against a real temp project using real tsc for typecheck; pass and fail TS fixtures; lint/test wired to local shims; fast profile verdict. Gated on bun/npm availability.",
        "gaps": "No real launch/browser/screenshot step (those are unit-mocked elsewhere). No build-then-reload end-to-end. No structured-proof-against-checks integration. Does not feed a verified plugin into loadPluginFromDirectory."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/runtime/plugin-resolver-cold-import.test.ts",
        "kind": "unit",
        "covers": "Cold fast-path vs staging: flag on + same package name imported twice -> 2nd import goes through staging (the ESM module-graph cache-bust that makes hot-reload pick up mutated code).",
        "gaps": "Tests the staging decision, not an actual mutate-then-reimport-then-assert-new-behavior reload. No failure/rollback path."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/test/runtime/operations/classify-ordering.test.ts",
        "kind": "unit",
        "covers": "Manager classifies provider-switch from the pre-prepare provider snapshot (ensures an unloaded provider plugin gets a cold restart rather than a hot no-op).",
        "gaps": "Only the classifier ordering. No test of a failed cold reload + health-check failure + (missing) rollback. No plugin-enable/disable reload tiering test."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/test/runtime/operations/vault-integration.test.ts",
        "kind": "integration",
        "covers": "vault x runtime-ops: accepted op persists apiKeyRef not plaintext; hot reload resolves apiKeyRef into process.env; idempotency does not double-write; pruning preserves vault entries; legacy op migration.",
        "gaps": "Focused on secret handling during a hot reload, not on reload correctness/rollback, plugin graph mutation, or broken-reload recovery."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/plugin-runtime-apply.ts",
        "kind": "none",
        "covers": "Nothing — there is no plugin-runtime-apply.test.ts. The core tiered reload-apply pipeline (config_apply/plugin_reload/runtime_reload/restart_required, plus the mid-reload failure fallback) is UNTESTED.",
        "gaps": "No test that a mid-reload throw leaves the runtime consistent or that it falls back to restart correctly; no test of unload-then-register ordering; no test that a force-reload of an adapter plugin escalates to runtime reload; no rollback-on-failure test (and the code has no rollback to test)."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/api/views-routes.hero.test.ts",
        "kind": "unit",
        "covers": "View routes: hero image serving, search, navigate-broadcast, interact-result/coverage.",
        "gaps": "No test for /api/views/:id/bundle.js serving a REBUILT bundle (mtime ETag change -> client revalidation), no content-hash cache-bust test, and no end-to-end edited-view-bundle reload pickup."
      }
    ],
    "evidence": [
      {
        "type": "log",
        "exists": true,
        "where": "AppVerificationService persists per-check logs (typecheck.log, lint.log, test.log, build.log, launch.log, browser.log, structured-proof.log) + screenshot.png/desktop.png into a verification dir (ensureVerificationDir(runId)) in /Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-verification.ts and verification-helpers.ts. Structured [ClassName] logs throughout the reload path (runtime-ops, eliza-api, VerificationRoomBridge)."
      },
      {
        "type": "screenshot",
        "exists": true,
        "where": "AppVerificationService.runBrowserCheck captures page.screenshot to <dir>/screenshot.png and falls back to captureScreenshotViaDevApi -> desktop.png, optionally described via vision. This is per-verification, NOT a before/after reload screenshot pair, and is not retained as committed evidence."
      },
      {
        "type": "trajectory",
        "exists": false,
        "where": "views-create.ts uses spawnWithTrajectoryLink to link the coding-agent child trajectory, but there is no captured/committed real-LLM trajectory for the reload/rollback domain (no scenario under packages/scenario-runner exercises create->verify->reload->broken-reload). No .github/issue-evidence artifacts for this domain were found."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of the modify->build->reload or broken-reload-recovery flow. No e2e:record harness output for plugin/view reload."
      },
      {
        "type": "timeline",
        "exists": true,
        "where": "DefaultRuntimeOperationManager appends phase records (validate/apply-env/notify-plugins/cold-restart/health-check) with startedAt/finishedAt timestamps to the operation repository (manager.ts + repository.ts), giving a per-operation reload timeline — but only for runtime-ops provider-switch/config-reload, not for the loadPluginFromDirectory live-load path, and not captured as test/PR evidence."
      },
      {
        "type": "report",
        "exists": false,
        "where": "No scenario-runner JSON report, run viewer, or native jsonl captured for the reload/build-validation/rollback domain."
      }
    ],
    "gaps": [
      {
        "title": "No rollback/recovery on a broken plugin reload (in-memory graph can be left half-torn-down)",
        "severity": "critical",
        "description": "applyPluginRuntimeMutation (plugin-runtime-apply.ts:286-334) unloads removed/reload packages, then registers new ones; if registerPlugin throws mid-loop it logs and falls back to a full restart but never restores the plugins it already unloaded. loadPluginFromDirectory has no snapshot/restore either. The runtime-ops cold strategy (manager.ts:269-278) swaps the runtime BEFORE the health check and the code explicitly states it cannot restore the previous runtime ('requires a two-phase restart contract'). A broken reload of an edited plugin/view can therefore leave the live agent with missing actions/providers/views until a manual full restart.",
        "suggestedWork": "Implement a two-phase reload: snapshot the plugin ownership graph (and adapter) before unload/register, and on any failure (register throw OR post-reload health-check failure) restore the snapshot (re-register the previous module, restore adapter, re-register prior views). Add the two-phase restart contract the manager comment references so cold reload can revert to the prior runtime. Add unit tests in packages/agent/src/api/plugin-runtime-apply.test.ts and packages/agent/src/runtime/operations covering mid-reload throw and failed-health-check rollback."
      },
      {
        "title": "plugin-runtime-apply.ts (the core tiered reload pipeline) has zero tests",
        "severity": "high",
        "description": "The central function deciding config_apply vs plugin_reload vs runtime_reload vs restart_required — including the adapter-requires-restart escalation and the mid-reload failure fallback — is completely untested. There is no plugin-runtime-apply.test.ts anywhere.",
        "suggestedWork": "Add a unit suite that: applies a config-only change (config_apply), adds/removes a plugin (plugin_reload via unload+register), forces an adapter plugin (runtime_reload escalation), and injects a registerPlugin throw to assert the restart fallback and (once implemented) rollback. Use a fake AgentRuntime with the lifecycle wrapper installed."
      },
      {
        "title": "No end-to-end edit -> rebuild -> live-reload -> assert-new-behavior test",
        "severity": "high",
        "description": "load-plugin-from-directory.test.ts proves load/unload and security, and plugin-resolver-cold-import proves staging decisions, but nothing edits a built plugin on disk, re-imports it, and asserts the NEW code path is live (the whole point of the ?t= cache-bust + staging). The hot-reload semantic is asserted only by comments.",
        "suggestedWork": "Add a test that scaffolds a built plugin returning v1, loads it, rewrites dist/index.js to return v2, calls loadPluginFromDirectory again, and asserts the runtime now serves v2 (action handler / views metadata changed). Add a sibling for a re-import whose module evaluation throws and assert the prior v1 plugin is still registered and functional."
      },
      {
        "title": "Broken-reload recovery is only asserted via mocked chat text, never a real broken plugin",
        "severity": "high",
        "description": "verification-room-bridge.test.ts mocks fetch to return a 422 and asserts the chat message says 'live-load failed'. No test drives a genuinely broken built plugin (e.g. import that throws, missing export, bad view bundle) through the real /api/plugins/load-from-directory route and asserts (a) the runtime graph is unchanged and (b) the view registry didn't half-register the broken view.",
        "suggestedWork": "Add an integration test that POSTs a real broken plugin dir to the load route and asserts HTTP 422 + no partial registration (actions/providers/views unchanged). Pair with the rollback work above."
      },
      {
        "title": "View-bundle rebuild-and-reserve is untested at the route level",
        "severity": "medium",
        "description": "A modified view's bundle is served from disk with an mtime+size ETag and content-hash cache-bust, but no test verifies that rebuilding dist/views/bundle.js changes the ETag/bundleHash and that a client revalidation returns the new bytes (vs a stale 304). For dynamic view editing this is the browser-side reload contract.",
        "suggestedWork": "Add a views-routes bundle test: serve a bundle, capture ETag, rewrite the bundle file (new mtime/size), and assert a fresh request returns 200 with new bytes and a new ETag, and that ?v=<oldHash> vs ?v=<newHash> cache headers differ (immutable vs no-cache)."
      },
      {
        "title": "No real-LLM trajectory / video / before-after screenshot evidence for the reload+rollback flow",
        "severity": "medium",
        "description": "Per the repo Definition of Done, agent/action changes need real-LLM trajectories and UI changes need before/after screenshots + video. The create->verify->reload->broken-reload domain has only per-verification logs/screenshots; no captured scenario trajectory, run viewer report, video walkthrough, or before/after screenshot pair exists for it.",
        "suggestedWork": "Author a scenario-runner scenario (packages/scenario-runner) that drives VIEWS create -> coding agent -> verification -> live-load (pass) and a second variant that injects a build/import failure -> honest verdict, capturing JSON report + jsonl + run viewer. Add a test:e2e:record walkthrough showing the view appearing live after edit and the failure message on a broken reload."
      },
      {
        "title": "No git-revert / source snapshot / undo for code changes in the coding-orchestration context",
        "severity": "medium",
        "description": "There is no rollback of edited SOURCE when a verification or reload fails — the coding agent edits in place and a failed verify just prompts a retry; a failed live-load tells the user to 'reload the agent'. The only 'undo' in the searched code is the homescreen canvas history (client-side scene-document op stack in homescreen.ts), which is unrelated to plugin/view code. There is no git stash/branch/checkpoint per coding task to revert a bad edit set.",
        "suggestedWork": "Decide whether per-task source rollback is in scope; if so, add a git checkpoint (branch/stash or commit) before the coding agent edits and a revert path on a final verification/reload failure, surfaced as an orchestrator action. Add tests for checkpoint-create and revert-on-failure."
      },
      {
        "title": "No concurrency test for parallel plugin reloads (relevant to the 10-project concurrency goal)",
        "severity": "low",
        "description": "The plugin staging pruner and importedPluginPackageNames set guard concurrent imports, and the runtime-ops manager single-flights operations, but loadPluginFromDirectory (the live-load route) has no single-flight gate and no test for two reloads of the same/different plugins racing. The 10-concurrent-projects scenario could hit register/unregister interleaving.",
        "suggestedWork": "Add a concurrency test that fires N simultaneous loadPluginFromDirectory calls (same and distinct plugins) and asserts the final registry is consistent and no staging dir is corrupted; add a per-plugin single-flight lock to the live-load route if interleaving is observed."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/app-verification.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/verification-room-bridge.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/views-create.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/app-relaunch.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/actions/app-load-from-directory.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/load-plugin-from-directory.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/plugin-resolver.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/plugin-lifecycle.ts",
      "/Users/shawwalters/eliza/packages/agent/src/api/plugin-runtime-apply.ts",
      "/Users/shawwalters/eliza/packages/agent/src/api/server.ts",
      "/Users/shawwalters/eliza/packages/agent/src/api/views-routes.ts",
      "/Users/shawwalters/eliza/packages/agent/src/api/views-registry.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/operations/manager.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/operations/cold-strategy.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/operations/reload-hot.ts",
      "/Users/shawwalters/eliza/packages/app-core/scripts/dev-ui.mjs",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/task-coordinator-view-bundle.ts",
      "/Users/shawwalters/eliza/packages/agent/src/runtime/load-plugin-from-directory.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/verification-room-bridge.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-app-control/src/services/__tests__/app-verification.integration.test.ts"
    ]
  },
  {
    "domain": "Platform coverage for coding agents (cloud / desktop / AOSP / Debian / Google Play / iOS, plus remote-container delegation)",
    "summary": "elizaOS has two real, distinct coding-execution substrates and a clean per-platform gate between them. (1) Local subprocess/PTY execution via @elizaos/plugin-agent-orchestrator, which spawns ACP coding agents (claude/codex/opencode/elizaos/pi-agent) and is gated at plugin-construction time by isLocalCodeExecutionAllowed() (direct build only) AND detectOrchestratorTerminalSupport() (shell + runtime-mode checks). (2) Remote-container execution via E2BRemoteCapabilityRouterService (providers: e2b, eliza-cloud, home) which routes fs/pty/git capabilities over HTTP to a hosted runner (packages/cloud/services/coding-remote-runner), with the cloud control plane provisioning coding containers (packages/cloud/api/v1/coding-containers + cloud-shared coding-containers service + agent-sandboxes repo). Platform reality: desktop (Electrobun, direct build) and cloud both run local execution with fs access; STORE builds (Mac App Store/MS Store/Flathub) are hard-blocked to a TASKS stub; iOS is blocked entirely (no shell) and is expected to delegate to remote containers via the same agent runtime; Android/AOSP direct builds can run local execution only with local-yolo mode + a staged /system/bin/sh, otherwise stub. IMPORTANT framing correction: plugin-remote-manifest host-shim subpaths, plugin-worker-runtime, and plugin-remote-manifest are NOT coding-agent infrastructure — they are the wire bridge for remote-mode plugin VIEW bundles / remote plugin execution; they live under packages/ (not plugins/) and are unrelated to running coding agents. Maturity is partial-to-solid in source and unit/integration tests, but there is essentially NO captured cross-platform evidence (no trajectory/screenshot/video/timeline of a coding agent actually creating/modifying code on AOSP/Debian/iOS/Play, nor of the iOS->remote-container delegation end-to-end).",
    "maturity": "partial",
    "existing": [
      {
        "component": "Sandbox / local-code-execution policy (build-variant gate)",
        "path": "/Users/shawwalters/eliza/packages/core/src/sandbox-policy.ts + /Users/shawwalters/eliza/packages/core/src/build-variant.ts",
        "whatItDoes": "isLocalCodeExecutionAllowed() returns true only for the 'direct' build variant (ELIZA_BUILD_VARIANT); store builds get buildStoreVariantBlockedMessage(). Single source of truth consulted by orchestrator + actions.",
        "maturity": "solid"
      },
      {
        "component": "Orchestrator platform gate (per-platform coding support detection)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/terminal-capabilities.ts",
        "whatItDoes": "detectOrchestratorTerminalSupport(): store_build -> unsupported; iOS -> vanilla_mobile unsupported; Android -> requires ELIZA_RUNTIME_MODE=local-yolo AND an executable shell (resolveOrchestratorShell scans /system/bin etc.), else not_local_yolo/missing_shell. isAospTerminalRuntime() keys on ELIZA_AOSP_BUILD.",
        "maturity": "solid"
      },
      {
        "component": "Agent-orchestrator plugin factory (registers spawn services only when allowed)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/index.ts (createAgentOrchestratorPlugin, L89-215)",
        "whatItDoes": "When codeExecutionAllowed it registers AcpService/OrchestratorTaskService/SubAgentRouter/CodingWorkspaceService + TASKS subactions; otherwise registers ONLY a single stub action (tasksSandboxStubAction or createTerminalUnsupportedTasksAction). Node-only plugin.",
        "maturity": "solid"
      },
      {
        "component": "Remote-container coding capability router (e2b / eliza-cloud / home)",
        "path": "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.ts",
        "whatItDoes": "E2BRemoteCapabilityRouterService implements fs.list/readText/writeText, pty.runCommand, git status/diff/commandRun against a remote runner. DefaultSandboxFactory dispatches to E2B SDK, RemoteRunnerHttpFactory, or ElizaCloudCodingContainerFactory (POST /coding-containers, poll for URL). Workspace path mapping host<->container. agentRunners codex/claude-code/opencode.",
        "maturity": "solid"
      },
      {
        "component": "Cloud coding-remote-runner HTTP service (the container-side runner)",
        "path": "/Users/shawwalters/eliza/packages/cloud/services/coding-remote-runner/src/index.ts + Dockerfile",
        "whatItDoes": "Bun.serve runner exposing /v1/health, /v1/fs/entries, /v1/fs/file (GET/PUT), /v1/processes/run with token auth, workspace-root jail (realpath + ensureInsideRoot), symlink rejection, bounded output. This is what runs inside the remote container for Play/iOS/cloud delegation.",
        "maturity": "solid"
      },
      {
        "component": "Cloud control-plane coding-container provisioning",
        "path": "/Users/shawwalters/eliza/packages/cloud/api/v1/coding-containers/route.ts + /Users/shawwalters/eliza/packages/cloud/shared/src/lib/services/coding-containers.ts + .../db/repositories/agent-sandboxes.ts",
        "whatItDoes": "Cloud API endpoint + service that allocates a coding container (agent/image/env/workspace), image allowlist enforcement, returns a public bridge URL the E2B router treats as the remote runner. Backed by agent-sandboxes DB repo (warm pool, backups).",
        "maturity": "solid"
      },
      {
        "component": "Single shell-execution chokepoint with mode dispatch",
        "path": "/Users/shawwalters/eliza/packages/agent/src/services/shell-execution-router.ts",
        "whatItDoes": "runShell() routes one-shot exec by ELIZA_RUNTIME_MODE: cloud -> throws (no local exec), local-safe -> SandboxManager (Docker/Apple Container) and refuses to silently fall back to host, local-yolo -> host spawn. Also handles vfs:// materialization for VFS-backed workspaces.",
        "maturity": "solid"
      },
      {
        "component": "Desktop / mobile app shells + platform hosts",
        "path": "/Users/shawwalters/eliza/packages/app/electrobun + /Users/shawwalters/eliza/packages/app-core/platforms/{electrobun,ios,android} + /Users/shawwalters/eliza/packages/app/capacitor.config.ts",
        "whatItDoes": "Electrobun desktop shell (direct build -> full local coding), Capacitor iOS/Android shells. Desktop is the primary fs-access coding surface; mobile shells host the in-process agent that delegates coding to remote runners.",
        "maturity": "partial"
      },
      {
        "component": "Mobile agent bridge (phone <-> desktop relay tunnel)",
        "path": "/Users/shawwalters/eliza/plugins/plugin-native-mobile-agent-bridge/src/index.ts + web.ts + ios/android native",
        "whatItDoes": "Capacitor plugin opening an OUTBOUND WebSocket tunnel from a phone-hosted agent to a relay so a desktop client can reach it. Not coding-specific; enables cross-device access to the on-device agent. Web fallback returns state:error.",
        "maturity": "partial"
      },
      {
        "component": "AOSP Android OS distro (vendor overlay, build + boot-validate)",
        "path": "elizaOS/os:packages/os/android (Makefile, vendor/eliza, sepolicy/eliza_agent.te)",
        "whatItDoes": "AOSP fork producing Cuttlefish/Pixel images with the eliza launcher, init.eliza.rc, SELinux policy for eliza_agent. make build does build+launch+boot-validate. This is OS packaging only — it does not exercise coding-agent execution.",
        "maturity": "partial"
      },
      {
        "component": "elizaOS Live Debian distro",
        "path": "elizaOS/os:packages/os/linux (Justfile, build-iso.sh, elizaos overlay)",
        "whatItDoes": "Tails-derived Debian live-build ISO bundling the Electrobun app. just build (Docker) / just boot (QEMU) / just static-smoke. Direct-build app inside -> local coding capable, but no coding-agent run is part of the build/boot validation.",
        "maturity": "partial"
      },
      {
        "component": "Host-shim / worker-runtime / remote-manifest (plugin view + remote-plugin wire, NOT coding)",
        "path": "/Users/shawwalters/eliza/packages/plugin-remote-manifest/src/host-shim{,/android,/ios,/electrobun} + /Users/shawwalters/eliza/packages/plugin-worker-runtime + /Users/shawwalters/eliza/packages/plugin-remote-manifest",
        "whatItDoes": "PluginHostShim is the cross-platform bridge for remote-mode plugin VIEW bundles (Electrobun/iOS WKWebView/Android WebView/web iframe). worker-runtime bootstraps remote plugin execution in a Worker; remote-manifest defines the wire types. Mislabeled in the task as coding infra — these are plugin-UI/remote-plugin infra.",
        "maturity": "solid"
      }
    ],
    "tests": [
      {
        "path": "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/__tests__/sandbox-gating.test.ts",
        "kind": "unit",
        "covers": "Store-build gating (isLocalCodeExecutionAllowed false under store, true under direct), plugin registers 0 services + 1 TASKS stub under store, Android-without-shell -> AOSP_TERMINAL_MISSING_SHELL, stub handler returns STORE_BUILD_BLOCKED.",
        "gaps": "Pure construction-time checks; never spawns a real coding agent, never exercises iOS/AOSP-with-shell happy path, no Debian variant."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.test.ts",
        "kind": "unit",
        "covers": "Config resolution for e2b/eliza-cloud/home/disabled providers, default workspaces, structured unavailable, command run + host->container path mapping, file listing w/ hidden+ignore filters, git helpers, cloud-container provisioning poll, host-path-escape rejection (16 cases).",
        "gaps": "All against a fake E2BSandboxFactory; no real E2B/cloud container; no concurrency, no multi-account, no failure/rollback paths beyond unavailability."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.coding-remote-runner.test.ts",
        "kind": "integration",
        "covers": "Real in-process integration: E2B router 'home' provider driving the actual coding-remote-runner createHandler over a mocked global fetch — writes a file via pty, reads it back, verifies it landed on the real host workspace ('mobile evidence').",
        "gaps": "Single happy-path test; no auth-failure, no large output, no concurrent sessions, no iOS/Android process actually involved (fetch is mocked)."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/cloud/services/coding-remote-runner/__tests__/server.test.ts",
        "kind": "unit",
        "covers": "Runner HTTP server: health, fs entries/read/write, process run, auth token enforcement, workspace jail / path traversal / symlink rejection.",
        "gaps": "No live container, no coding-agent (claude/codex/opencode) actually invoked inside the runner."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/cloud/shared/src/lib/services/coding-containers.test.ts + /Users/shawwalters/eliza/packages/cloud/api/v1/coding-containers/route.test.ts",
        "kind": "unit",
        "covers": "Coding-container payload/image selection, request schema (default agent=claude, drop unknown fields), session response URL preference, image allowlist (fail-closed, wildcard, exact), custom-image as custom execution tier.",
        "gaps": "No end-to-end provision->run->return; no real container lifecycle; route test is schema/allowlist focused."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/agent/scripts/live-sandbox-smoke.ts (bun run --cwd packages/agent test:sandbox-live)",
        "kind": "live",
        "covers": "Optional live smoke: for e2b/eliza-cloud/home writes+reads a file, lists workspace, runs a shell command, git --version through the configured route; also codex-app-server local smoke. Skips without credentials unless --strict.",
        "gaps": "Manual/credential-gated, not in CI by default, produces only stdout PASS/SKIP lines — no trajectory/screenshot/video/timeline artifact, no per-platform (iOS/Android) run."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/ui-smoke/orchestrator-gui-workbench.spec.ts + remote-capability-view.spec.ts + terminal-plugin-view-command-contract.spec.ts",
        "kind": "e2e",
        "covers": "Playwright desktop-Chromium smoke of the orchestrator/task workbench UI and remote-capability view bundle loading, with all /api routes stubbed (mock task summaries, mock usage, local node http server for the view bundle).",
        "gaps": "Fully mocked backend — no real coding agent, no real remote container; not run on iOS/Android device; orchestrator surface is rendered against fixtures only."
      },
      {
        "path": "/Users/shawwalters/eliza/packages/app/test/android/{route-coverage,local-runtime,voice-selftest}.android.spec.ts (playwright.android.config.ts)",
        "kind": "e2e",
        "covers": "Real on-device Android WebView e2e against the on-device agent (route coverage, local runtime, voice). Captures screenshot+trace on failure over Android CDP.",
        "gaps": "No coding/orchestrator/terminal/sandbox spec among the Android device specs (grep for coding|orchestr|terminal|sandbox|TASKS|spawn returned nothing) — coding execution on Android/AOSP is never exercised on a device."
      },
      {
        "path": "elizaOS/os:packages/os/scripts/__tests__/*.test.mjs + elizaOS/os:packages/os/android/installer/tests/run-tests.sh",
        "kind": "unit",
        "covers": "OS release pipeline: manifest validation, checksums, image reproducibility, TEE/confidential checks, dstack pins; Android installer flasher tests.",
        "gaps": "Validates release/installer machinery, not coding-agent behavior on the built OS. No AOSP/Debian image is ever booted-then-asked-to-code in CI."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No scenario-runner trajectory exists for a coding agent running per-platform. e2b-capability-router.coding-remote-runner.test.ts proves the remote-runner path in-process but emits no trajectory/jsonl. No packages/scenario-runner scenario targets coding-across-platforms."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "No before/after screenshots of coding execution on any platform. orchestrator-gui-workbench.spec.ts renders the workbench from fixtures (could screenshot, but does not capture as platform evidence). .github/issue-evidence has only unrelated PNGs (8810/8812)."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of a coding agent creating/modifying code on desktop/AOSP/Debian/iOS/Play. Android device specs capture trace-on-failure only and have no coding spec."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured logs exist in source: E2BRemoteCapabilityRouter '[E2BRemoteCapabilityRouter] Service started', coding-remote-runner JSON '[CodingRemoteRunner] listening/request failed', orchestrator TASK_AUDIT log (ACP_AUDIT_LOG_PATH ~/.eliza/acp-audit.log). live-sandbox-smoke prints PASS/SKIP. But no captured/committed log artifact tying a coding run to a specific platform."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline capture for cross-platform coding. spawn-trajectory.ts in the orchestrator records session events, but no committed timeline artifact per platform exists."
      },
      {
        "type": "report",
        "exists": false,
        "where": "No platform-coverage report/matrix artifact (cloud/desktop/AOSP/Debian/Play/iOS) demonstrating coding works on each. The e2b-capability-routing.md doc describes the design but is not run evidence."
      }
    ],
    "gaps": [
      {
        "title": "No end-to-end evidence that a coding agent actually runs on iOS or Google Play via remote container",
        "severity": "critical",
        "description": "iOS is hard-blocked from local execution (terminal-capabilities.ts vanilla_mobile) and the stated design is to delegate coding to a remote container through the same agent runtime + E2BRemoteCapabilityRouterService (provider eliza-cloud -> /coding-containers -> coding-remote-runner). Every layer has unit/integration tests, but there is ZERO end-to-end proof of the full chain on a real iOS/Android (Play) build: phone agent -> cloud coding-container provision -> remote runner fs/pty/git -> result back to chat. The coding-remote-runner.test mocks fetch; the cloud route test is schema-only; no device run exists.",
        "suggestedWork": "Author a scenario-runner scenario 'coding-via-remote-container' that boots the agent with ELIZA_PLATFORM=ios and ELIZA_CODING_REMOTE_RUNNER=eliza-cloud against a live (or docker-compose) coding-remote-runner + a mock/real coding-containers endpoint, has the agent create+modify a small app, and emits a JSON trajectory + jsonl. Add a Capacitor/Playwright iOS-or-Android device test that drives the same flow and captures a video + before/after screenshots into .github/issue-evidence/."
      },
      {
        "title": "No coding/orchestrator e2e on Android or AOSP device",
        "severity": "high",
        "description": "playwright.android.config.ts drives a real on-device WebView against the on-device agent, but none of the android specs (route-coverage/local-runtime/voice-selftest) touch coding, orchestrator, terminal, TASKS, or sandbox. AOSP requires local-yolo + a staged /system/bin/sh (terminal-capabilities.ts), a path only covered by a negative unit test (missing-shell -> stub). The happy path (Android direct/AOSP with a staged shell actually spawning claude/codex/opencode) is never executed on a device or emulator.",
        "suggestedWork": "Add a *.android.spec.ts that, on an AOSP/Cuttlefish image with a staged shell, invokes TASKS spawn_agent (or a deterministic local runner) to create a file and asserts the workspace diff; capture trace+screenshot. Add a Cuttlefish CI lane that boots the elizaOS/os:packages/os/android image and runs this coding smoke as the boot-validate's final step."
      },
      {
        "title": "No Debian-live (elizaOS Live) coding-agent boot smoke",
        "severity": "high",
        "description": "elizaOS/os:packages/os/linux builds a Debian ISO bundling the direct-build Electrobun app (local-coding capable). just boot (QEMU) and just static-smoke validate the image, but nothing boots the ISO and then asks the bundled agent to write code. There is no proof the shipped Live image can actually run a coding agent.",
        "suggestedWork": "Extend the just boot path (or a new just coding-smoke) to boot the ISO in QEMU, bring up the bundled agent, run a minimal local coding task (create + run a script), and emit a log + screenshot artifact. Wire into the OS release verification (verify-release.sh)."
      },
      {
        "title": "Live remote-container smoke is credential-gated, not in CI, and emits no rich evidence",
        "severity": "high",
        "description": "live-sandbox-smoke.ts is the only thing that hits a real remote runner, but it skips without E2B/cloud credentials, is not part of default CI, and only prints PASS/SKIP/FAILED stdout lines — no trajectory, screenshot, video, or timeline. The repo's own Definition of Done (AGENTS.md) requires real-LLM trajectories + logs + video for agent/action changes; this domain has none committed.",
        "suggestedWork": "Add a CI lane (with secrets) that runs test:sandbox-live --strict against a docker-compose'd coding-remote-runner for the 'home' provider (no external creds needed), produce a structured JSON report + captured runner logs, and store artifacts under .github/issue-evidence/. Make the home-provider variant non-skippable so at least one real-container path is always proven."
      },
      {
        "title": "No concurrency / 10-project / rollback evidence for remote or local coding execution",
        "severity": "medium",
        "description": "The E2B router serializes one sandbox per service (sandboxPromise) and the orchestrator caps sessions (ELIZA_ACP_MAX_SESSIONS=8), but there is no test or evidence of N concurrent coding projects, container exhaustion, eviction, or rollback/cleanup (stop() keepAlive logic, scratch GC). The cloud warm-pool/agent-sandboxes repo has lifecycle code but no end-to-end concurrency proof tied to coding.",
        "suggestedWork": "Add an integration test that opens up to 10 concurrent coding sessions/containers (mixed local + remote), asserts queueing/cap behavior, kills/keepAlive semantics, and workspace cleanup; capture a timeline artifact of the concurrent runs."
      },
      {
        "title": "Desktop (Electrobun) coding path is only smoke-tested with mocked APIs",
        "severity": "medium",
        "description": "Desktop is the primary fs-access coding surface, yet orchestrator-gui-workbench.spec.ts and remote-capability-view.spec.ts stub all /api routes and never spawn a real coding agent. There is no packaged-Electrobun e2e that runs an actual local coding task and shows the diff in the workbench UI.",
        "suggestedWork": "Add a playwright.electrobun.packaged e2e that launches the packaged desktop app (direct build), runs a real local TASKS create against a deterministic/local runner, and captures a video + before/after screenshots of the workbench showing the produced diff."
      },
      {
        "title": "Apple-Container / Docker local-safe sandbox backend for coding is untested end-to-end",
        "severity": "medium",
        "description": "shell-execution-router.ts local-safe mode routes through SandboxManager (Docker / Apple Container) and refuses host fallback, and sandbox-engine.ts (753 lines) implements the backend, but I found no test that actually runs a coding command through a live Docker/Apple-Container sandbox; coverage of the local-safe coding path appears to be unit-level only.",
        "suggestedWork": "Add a gated integration test (docker available) that runs a coding command via runShell local-safe with a real Docker SandboxManager and asserts the host fs is untouched; capture logs showing sandbox backend = docker."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/packages/core/src/sandbox-policy.ts",
      "/Users/shawwalters/eliza/packages/core/src/build-variant.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/terminal-capabilities.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/index.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/__tests__/sandbox-gating.test.ts",
      "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.ts",
      "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.test.ts",
      "/Users/shawwalters/eliza/packages/agent/src/services/e2b-capability-router.coding-remote-runner.test.ts",
      "/Users/shawwalters/eliza/packages/agent/src/services/shell-execution-router.ts",
      "/Users/shawwalters/eliza/packages/agent/src/services/sandbox-engine.ts",
      "/Users/shawwalters/eliza/packages/agent/scripts/live-sandbox-smoke.ts",
      "/Users/shawwalters/eliza/packages/agent/docs/e2b-capability-routing.md",
      "/Users/shawwalters/eliza/packages/cloud/services/coding-remote-runner/src/index.ts",
      "/Users/shawwalters/eliza/packages/cloud/services/coding-remote-runner/__tests__/server.test.ts",
      "/Users/shawwalters/eliza/packages/cloud/shared/src/lib/services/coding-containers.ts",
      "/Users/shawwalters/eliza/packages/cloud/api/v1/coding-containers/route.ts",
      "/Users/shawwalters/eliza/packages/cloud/shared/src/db/repositories/agent-sandboxes.ts",
      "/Users/shawwalters/eliza/packages/app/playwright.android.config.ts",
      "/Users/shawwalters/eliza/packages/app/test/ui-smoke/orchestrator-gui-workbench.spec.ts",
      "/Users/shawwalters/eliza/packages/app/test/ui-smoke/remote-capability-view.spec.ts",
      "/Users/shawwalters/eliza/plugins/plugin-native-mobile-agent-bridge/src/index.ts",
      "elizaOS/os:packages/os/android/Makefile",
      "elizaOS/os:packages/os/linux/Justfile",
      "/Users/shawwalters/eliza/packages/plugin-remote-manifest/src/host-shim/index.ts",
      "/Users/shawwalters/eliza/packages/plugin-remote-manifest/src/worker-runtime/bootstrap.ts"
    ]
  },
  {
    "domain": "Domain J — Model backends for coding/orchestration (model routing for built-in vs sub-agent coding paths; gpt-oss-120b/Cerebras, claude, codex, z.ai wiring across cloud-routing, plugin-elizacloud, plugin-anthropic(+proxy), plugin-codex-cli, plugin-cli-inference, plugin-openai/openrouter, plugin-agent-orchestrator)",
    "summary": "The model-backend layer for coding/orchestration is mature and well-architected, with a clear separation between two paths. (1) The BUILT-IN path (agent answers/plans itself) routes through `runtime.useModel(ModelType.*)`; the eliza-cloud built-in coding model is gpt-oss-120b, wired as the default TEXT_SMALL via `DEFAULT_ELIZA_CLOUD_TEXT_MODEL = \"gpt-oss-120b\"` in packages/core/src/contracts/service-routing.ts and plugin-elizacloud's `getSmallModel`; the cloud gateway transparently routes gpt-oss to Cerebras (detected by `isCerebrasServedModel`). Large/planner tier defaults to `zai-glm-4.7` (z.ai). plugin-openai independently supports a direct Cerebras mode (CEREBRAS_API_KEY/base-url). (2) The SUB-AGENT path spawns external coding CLIs (elizaos, pi-agent, opencode, codex, claude) as ACP subprocesses; model selection is per-agent-type in AcpService.buildEnv (sets OPENAI_MODEL/ANTHROPIC_MODEL/OPENCODE_MODEL), opencode resolves provider+model via buildOpencodeSpawnConfig (Cerebras gpt-oss-120b default), and a multi-account credential bridge (coding-account-selection.ts) rotates pooled accounts for claude/codex/opencode. cloud/routing/resolve.ts is the local-key-vs-cloud-proxy-vs-disabled decider but is generic and NOT itself coding-aware. Unit coverage is strong (multi-account spawn rotation, opencode auto-detect, codex auth/backend, cerebras response_format gate, cloud-routing resolve). LIVE-model coverage exists but is narrow and entirely opt-in (gated by CEREBRAS_API_KEY+ELIZA_RUN_LIVE_TESTS, RUN_LIVE_NATIVE_ACP, RUN_LIVE_ACPX) — no committed trajectory/screenshot/video/timeline evidence ties a real coding task end-to-end to a chosen model, and there is no live test that asserts an actual sub-agent COMPLETES a coding task on gpt-oss-120b vs claude vs codex with captured artifacts.",
    "maturity": "partial",
    "existing": [
      {
        "component": "gpt-oss-120b as eliza-cloud built-in coding model (default TEXT_SMALL)",
        "path": "packages/core/src/contracts/service-routing.ts",
        "whatItDoes": "Defines DEFAULT_ELIZA_CLOUD_TEXT_MODEL = \"gpt-oss-120b\" (line 46), re-exported from core index.node.ts. This is the canonical default the elizacloud plugin's getSmallModel falls back to, making gpt-oss-120b the built-in small/nano/medium/response-handler model for cloud-connected agents.",
        "maturity": "solid"
      },
      {
        "component": "elizacloud text model handlers + Cerebras/gpt-oss detection",
        "path": "plugins/plugin-elizacloud/src/models/text.ts",
        "whatItDoes": "Implements TEXT_NANO/SMALL/MEDIUM/LARGE/MEGA/RESPONSE_HANDLER/ACTION_PLANNER handlers. isCerebrasServedModel() (line 359) detects gpt-oss by name and downgrades response_format json_schema->json_object since Cerebras 400s on json_schema. extractCostUsd reads gateway USD. Priority-50 plugin wins the default text slot.",
        "maturity": "solid"
      },
      {
        "component": "elizacloud model tier defaults",
        "path": "plugins/plugin-elizacloud/src/utils/config.ts",
        "whatItDoes": "getSmallModel falls back to gpt-oss-120b (via DEFAULT_ELIZA_CLOUD_TEXT_MODEL); getLargeModel/MegaModel/ActionPlanner fall back to zai-glm-4.7 (DEFAULT_ELIZA_CLOUD_LARGE_MODEL, line 5). nano/medium fall back to small. Reads ELIZAOS_CLOUD_*_MODEL then bare *_MODEL env aliases.",
        "maturity": "solid"
      },
      {
        "component": "cloud-routing resolver (local-key vs cloud-proxy vs disabled)",
        "path": "packages/cloud/routing/src/resolve.ts",
        "whatItDoes": "resolveCloudRoute/resolveFeatureCloudRoute pick a route source per RouteSpec; isCloudConnected gates on ELIZAOS_CLOUD_API_KEY + ELIZAOS_CLOUD_ENABLED. Generic per-feature policy (local/cloud/auto). NOTE: not coding-specific — it is the transport router, not the model chooser for sub-agents.",
        "maturity": "solid"
      },
      {
        "component": "Sub-agent model + auth selection per agent-type (buildEnv)",
        "path": "plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
        "whatItDoes": "buildEnv (~line 2063) sets OPENAI_MODEL/ANTHROPIC_MODEL/OPENCODE_MODEL from the spawn model param; drops ANTHROPIC_API_KEY/OPENAI_API_KEY when a subscription OAuth account is selected; drops API-tier OPENAI_MODEL for ChatGPT-auth codex; forwards CEREBRAS_API_KEY/BASE_URL/MODEL. ELIZA_CODEX_ACP_COMMAND/ELIZA_CLAUDE_ACP_COMMAND pin the CLI versions.",
        "maturity": "solid"
      },
      {
        "component": "OpenCode sub-agent provider+model config (Cerebras gpt-oss-120b)",
        "path": "plugins/plugin-agent-orchestrator/src/services/opencode-config.ts",
        "whatItDoes": "buildOpencodeSpawnConfig picks provider: cloud (Eliza Cloud, claude-opus-4-7/haiku), Cerebras (CEREBRAS_DEFAULT_MODEL = gpt-oss-120b, line 12), local (eliza-1-4b), or user opencode.json. Emits OPENCODE_CONFIG_CONTENT/OPENCODE_MODEL. This is the concrete place opencode sub-agents are wired to gpt-oss-120b on Cerebras.",
        "maturity": "solid"
      },
      {
        "component": "Multi-account coding credential bridge reader",
        "path": "plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
        "whatItDoes": "MULTI_ACCOUNT_AGENT_TYPES = {claude, codex, opencode}; selectCodingAccount reads the app-core AccountPool bridge via globalThis symbol and returns an envPatch (CLAUDE_CODE_OAUTH_TOKEN / CODEX_HOME / CEREBRAS_API_KEY) injected at spawn. No-ops to single-account when bridge absent.",
        "maturity": "solid"
      },
      {
        "component": "plugin-openai Cerebras mode (direct gpt-oss-120b / qwen)",
        "path": "plugins/plugin-openai/utils/config.ts",
        "whatItDoes": "Auto-detects Cerebras (ELIZA_PROVIDER=cerebras / *.cerebras.ai base / CEREBRAS_API_KEY-only), uses json_object structured output, low reasoning_effort, strips promptCacheRetention. Per-call params.model override used for isolated gpt-oss-120b workflow calls. Also EvoLink mode.",
        "maturity": "solid"
      },
      {
        "component": "plugin-codex-cli (ChatGPT Codex backend)",
        "path": "plugins/plugin-codex-cli/src/codex-backend.ts",
        "whatItDoes": "Registers TEXT_* + RESPONSE_HANDLER/ACTION_PLANNER handlers backed by the ChatGPT /responses SSE endpoint using the codex CLI OAuth cache; FIFO queue, 401 auto-refresh, supported models gpt-5..gpt-5.5-pro (default gpt-5.5). Built-in inference via a subscription, distinct from the codex ACP sub-agent.",
        "maturity": "solid"
      },
      {
        "component": "plugin-cli-inference (TOS-clean claude/codex CLI shell-out)",
        "path": "plugins/plugin-cli-inference/src/claude-cli.ts",
        "whatItDoes": "Gated by ELIZA_CHAT_VIA_CLI=claude|codex; registers ONLY large-tier handlers that spawn `claude --print` / `codex exec` which read their own on-disk creds (token never forwarded). Default models claude-opus-4-7 / gpt-5.5. Documented gap: no AccountPool failover (single on-disk cred set).",
        "maturity": "solid"
      },
      {
        "component": "plugin-anthropic-proxy (subscription routing for claude)",
        "path": "plugins/plugin-anthropic-proxy/src/proxy/server.ts",
        "whatItDoes": "7-layer bidirectional transform proxy so Anthropic traffic looks like the official Claude Code CLI, routing the agent's claude calls through a Claude Max/Pro subscription instead of per-token API. inline/shared/off modes. Relevant to the claude built-in + claude sub-agent coding paths.",
        "maturity": "solid"
      },
      {
        "component": "Cerebras-as-judge shared transport",
        "path": "packages/scenario-runner/src/cerebras-judge.ts",
        "whatItDoes": "Single CerebrasJudge class (default gpt-oss-120b) consumed by the scenario judge, lifeops live judge, training judge, and personality bench. Means gpt-oss-120b is also the default evaluation/judge model for coding/agent scenarios.",
        "maturity": "solid"
      },
      {
        "component": "Goal LLM verifier (task-completion check)",
        "path": "plugins/plugin-agent-orchestrator/src/services/goal-llm-verifier.ts",
        "whatItDoes": "Uses runtime.useModel(ModelType.TEXT_SMALL) (line 260) to verify a coding sub-agent actually met the task goal — i.e. the built-in (gpt-oss-120b on cloud) model judges the sub-agent's output. One useModel call per verification.",
        "maturity": "solid"
      }
    ],
    "tests": [
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/multi-account-spawn.test.ts",
        "kind": "unit",
        "covers": "Keystone: per-agent-type credential bridge injection at the real spawn layer — Claude OAuth token injected + ANTHROPIC_API_KEY dropped; Codex per-account CODEX_HOME + OPENAI_API_KEY/OPENAI_MODEL dropped; opencode pooled CEREBRAS_API_KEY injected; single-account fallback when no bridge; elizaos not consulted; least-used rotation across two spawns to distinct accounts; secrets never persisted to session metadata.",
        "gaps": "Mocks NativeAcpClient and git — no real subprocess, no real provider call. Does not assert the selected model actually reaches the model and produces output. z.ai/Kimi/GLM rotation not covered (no first-party CLI)."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/opencode-spawn-config-auto-detect.test.ts",
        "kind": "unit",
        "covers": "buildOpencodeSpawnConfig provider auto-detection (cloud/cerebras/local/user) and that Cerebras path defaults to gpt-oss-120b and emits correct OPENCODE_CONFIG_CONTENT/model.",
        "gaps": "No live opencode subprocess; does not verify the generated config is accepted by the real opencode binary."
      },
      {
        "path": "plugins/plugin-elizacloud/__tests__/unit/text-cerebras-response-format.test.ts",
        "kind": "unit",
        "covers": "Native /chat/completions response_format gate: gpt-oss models get json_object, all others keep json_schema. Fetch mocked, asserts outgoing body.",
        "gaps": "No live Cerebras call; the actual 400-on-json_schema behavior that motivates the gate is not regression-tested live."
      },
      {
        "path": "plugins/plugin-openai/__tests__/cerebras-spawn-subagent-refusal.live.test.ts",
        "kind": "live",
        "covers": "LIVE Cerebras gpt-oss-120b + qwen-3-235b: the #7620 Stage-1 'spawn sub-agent' refusal — runs the real user prompt N trials, asserts parser suppresses refusals on planning context (zero leaks), spawn action surfaces, adversarial variant. The single best live test tying gpt-oss to the orchestration decision.",
        "gaps": "Only the planner Stage-1 decision, not actual sub-agent execution. Skipped unless CEREBRAS_API_KEY+ELIZA_RUN_LIVE_TESTS=1 — not in default CI. Output is console-logged telemetry, not a saved trajectory artifact."
      },
      {
        "path": "plugins/plugin-openai/__tests__/cerebras-config.live.test.ts / cerebras-config.shape.test.ts",
        "kind": "live",
        "covers": "Cerebras provider-mode config (base URL, json_object, reasoning_effort, promptCacheRetention strip) at shape + live levels.",
        "gaps": "Provider plumbing only; no coding-task completion."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/native-acp-smoke.live.test.ts + live-native-acp-smoke.mjs",
        "kind": "live",
        "covers": "Drives a real native ACP adapter (codex/claude/opencode) through a tiny prompt and asserts PASSED/SKIPPED.",
        "gaps": "Gated by RUN_LIVE_NATIVE_ACP=1, not in CI. Tiny prompt, not a real coding task. Does not assert which model answered or capture a trajectory/timeline. gpt-oss/elizaos agent-type model path not exercised here."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
        "kind": "live",
        "covers": "Full loop with real acpx CLI: spawnSession -> codex subprocess -> task_complete -> SubAgentRouter -> synthetic Memory -> handleMessage.",
        "gaps": "Gated by RUN_LIVE_ACPX=1 + acpx on PATH + authenticated codex; not in CI. Single agent type; no model assertion or artifact capture."
      },
      {
        "path": "plugins/plugin-codex-cli/__tests__/codex-backend.test.ts",
        "kind": "unit",
        "covers": "Codex OAuth expiry/refresh, SSE parse, message translation, tool format, structured output wrapping. Mocked fetch.",
        "gaps": "No live ChatGPT /responses call; supported-model rejection not tested live."
      },
      {
        "path": "packages/cloud/routing/src/resolve.test.ts",
        "kind": "unit",
        "covers": "resolveCloudRoute/resolveFeatureCloudRoute local-key-vs-cloud-proxy-vs-disabled, per-feature policy, isCloudConnected.",
        "gaps": "Generic; no coding-specific routing scenarios (it is not coding-aware)."
      },
      {
        "path": "plugins/plugin-anthropic/__tests__/native-plumbing.live.test.ts + anthropic-drift.real.test.ts",
        "kind": "live",
        "covers": "Live Anthropic native tool plumbing + model-drift detection for claude.",
        "gaps": "Provider-level; not tied to a coding sub-agent task."
      },
      {
        "path": "packages/scenario-runner/test/scenarios/deterministic-coding-tools-actions.scenario.ts",
        "kind": "scenario",
        "covers": "Deterministic coding-tools action coverage (file edits, git worktree) with strict LLM action fixtures — no live model.",
        "gaps": "Uses fixtures, not a live model; does not exercise model selection or sub-agent spawn across gpt-oss/claude/codex. No end-to-end coding task on a real backend."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No committed real-LLM trajectory for a coding task tied to a chosen backend. plugin-openai/__tests__/trajectory.live.test.ts exists for provider trajectory logging but is generic and opt-in; no .github/issue-evidence/*.jsonl run-viewer report for gpt-oss/claude/codex coding completion."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured [ClassName] logs are emitted at runtime (e.g. acp-service buildEnv 'OpenCode ACP provider configured' with provider/model, sub-agent-router 'router bound to AcpService', coding-account-selection). Live tests console-log Cerebras refusal telemetry buckets. No captured/committed log artifacts for a coding-model run."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline capture for model routing or a multi-step coding turn across backends. Round-trip/respawn caps are logged but not assembled into a timeline artifact."
      },
      {
        "type": "report",
        "exists": true,
        "where": ".github/workflows/cerebras-nightly.yml produces nightly benchmark + delta reports against gpt-oss-120b (lifeops/personality suites) uploaded as artifacts (90-day retention). These are bench reports, not coding/orchestration completion evidence, and gpt-oss is judge+agent here, not the sub-agent coder."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "No screenshots for the orchestrator/task views showing which model a sub-agent used."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of a coding task running on a selected model backend."
      }
    ],
    "gaps": [
      {
        "title": "No end-to-end live test that a sub-agent COMPLETES a coding task per backend (gpt-oss-120b vs claude vs codex vs z.ai) with captured artifacts",
        "severity": "high",
        "description": "All live coverage is either provider plumbing (cerebras-config), the planner Stage-1 decision (cerebras-spawn-subagent-refusal), or a tiny smoke prompt (native-acp-smoke / sub-agent-router.live). None asserts that an opencode/codex/claude sub-agent actually writes correct code AND that the chosen model is the one that ran, with a saved trajectory + diff + timeline. The 'which model coded this' linkage is untested end-to-end.",
        "suggestedWork": "Add a gated live scenario (scenario-runner) that, per agent-type/model matrix [opencode/gpt-oss-120b-Cerebras, claude/opus, codex/gpt-5.5, opencode/zai-glm-4.7], spawns a sub-agent to build a tiny app, captures the JSON report + native jsonl trajectory + git changeset, and asserts the session metadata.account/model matches the requested backend. Land artifacts under .github/issue-evidence/."
      },
      {
        "title": "gpt-oss-120b is the documented built-in coding model but has no built-in-path coding trajectory evidence",
        "severity": "high",
        "description": "gpt-oss-120b is wired as the default cloud TEXT_SMALL (built-in answer/plan path) and as the default judge, but there is no committed real-LLM trajectory showing the built-in agent planning+executing a coding/orchestration turn on gpt-oss-120b. Per the repo Definition of Done (AGENTS.md), agent/action/prompt/model changes require real-LLM trajectories; this domain has none committed for the coding path.",
        "suggestedWork": "Run packages/scenario-runner/bin/eliza-scenarios against a live elizacloud gpt-oss-120b for a coding-delegation scenario (TASKS_SPAWN_AGENT) and commit the JSON report + run viewer + jsonl. Wire it as a nightly (extend cerebras-nightly.yml) so drift surfaces."
      },
      {
        "title": "Sub-agent model selection is dispersed and not centrally validated",
        "severity": "medium",
        "description": "Which model a sub-agent uses is decided across buildEnv (OPENAI_MODEL/ANTHROPIC_MODEL/OPENCODE_MODEL), buildOpencodeSpawnConfig (provider/model defaults), the multi-account bridge (envPatch), and the spawn-agent action's model param — with subtle drops (API key/model stripping for OAuth accounts). There is no single test that, given a spawn request + settings, asserts the FINAL resolved (provider, model, auth) tuple for each agent type. Regressions in one layer can silently change the backend.",
        "suggestedWork": "Add a unit test matrix in plugin-agent-orchestrator that calls the real buildEnv+buildOpencodeSpawnConfig (no mock) for {claude,codex,opencode,elizaos,pi-agent} x {api-key, subscription-account, cloud, cerebras, local} and snapshots the resolved env (OPENCODE_MODEL/OPENCODE_CONFIG_CONTENT, ANTHROPIC_MODEL, dropped keys). This is the missing 'model chooser' contract test."
      },
      {
        "title": "z.ai / GLM / Kimi have no first-party coding-CLI path and no rotation test",
        "severity": "medium",
        "description": "zai-glm-4.7 is the cloud LARGE/ACTION_PLANNER default (built-in path) but coding-account-selection.ts explicitly excludes z.ai/Kimi/GLM from MULTI_ACCOUNT_AGENT_TYPES — they have no first-party coding CLI. Task #4 calls for validating z.ai multi-account switching, but the only z.ai coding route is opencode pointed at a z.ai base URL, which is untested. The gap between 'z.ai is a default model' and 'z.ai cannot be a pooled coding sub-agent' is undocumented in tests.",
        "suggestedWork": "Either add a test proving opencode can target z.ai (via ELIZA_OPENCODE_BASE_URL + key) and document it as the z.ai coding path, or add an explicit assertion/test that z.ai is intentionally built-in-only for coding, so the limitation is captured."
      },
      {
        "title": "Live model tests are all opt-in and absent from default CI; only cerebras-nightly runs on schedule",
        "severity": "medium",
        "description": "Every model-backend live test is gated (ELIZA_RUN_LIVE_TESTS, RUN_LIVE_NATIVE_ACP, RUN_LIVE_ACPX, CEREBRAS_API_KEY). Only cerebras-nightly.yml runs automatically, and it benchmarks lifeops/personality, not coding/orchestration sub-agent completion. A model/provider drift in the coding path (e.g. Cerebras json_schema behavior, codex supported-model change, claude OAuth handling) would not be caught until manual invocation.",
        "suggestedWork": "Add a scheduled (nightly/weekly) CI lane that runs the gated coding-model live tests (cerebras-spawn-subagent-refusal, native-acp-smoke per agent type, a minimal coding-completion scenario) when the relevant secrets are configured, skipping cleanly otherwise — mirroring cerebras-nightly's secret-gate pattern."
      },
      {
        "title": "plugin-cli-inference single-token limitation (no AccountPool failover) is a documented but untested risk",
        "severity": "low",
        "description": "plugin-cli-inference (the TOS-clean claude/codex shell-out used as a built-in inference route) explicitly has no multi-account failover — it owns one on-disk cred set. If that subscription is rate-limited/expired there is no failover, unlike the sub-agent path which has the AccountPool bridge. No test asserts the failure surfaces as a provider error for useModel failover.",
        "suggestedWork": "Add a unit test confirming the handlers THROW on auth failure / rate-limit (non-zero exit) so runtime useModel failover engages, and document the single-token constraint in evidence for any deployment relying on cli-inference as the coding model."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/packages/core/src/contracts/service-routing.ts",
      "/Users/shawwalters/eliza/plugins/plugin-elizacloud/src/models/text.ts",
      "/Users/shawwalters/eliza/plugins/plugin-elizacloud/src/utils/config.ts",
      "/Users/shawwalters/eliza/packages/cloud/routing/src/resolve.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/opencode-config.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/coding-account-selection.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/task-agent-routing.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/goal-llm-verifier.ts",
      "/Users/shawwalters/eliza/plugins/plugin-openai/utils/config.ts",
      "/Users/shawwalters/eliza/plugins/plugin-codex-cli/src/codex-backend.ts",
      "/Users/shawwalters/eliza/plugins/plugin-cli-inference/src/claude-cli.ts",
      "/Users/shawwalters/eliza/plugins/plugin-anthropic-proxy/src/proxy/server.ts",
      "/Users/shawwalters/eliza/packages/scenario-runner/src/cerebras-judge.ts",
      "/Users/shawwalters/eliza/plugins/plugin-openai/__tests__/cerebras-spawn-subagent-refusal.live.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/multi-account-spawn.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-elizacloud/__tests__/unit/text-cerebras-response-format.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/live/native-acp-smoke.live.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
      "/Users/shawwalters/eliza/.github/workflows/cerebras-nightly.yml"
    ]
  },
  {
    "domain": "Domain K — Surfacing coding tasks to users + connectors (home-screen widget + notifications, shortcuts, slash commands, actions surfaced, rollout to Telegram/Discord/social connectors; update cadence / progress modes)",
    "summary": "Coding-task surfacing is implemented almost entirely inside plugin-agent-orchestrator. registerProgressHook (src/index.ts) is the single hot path: it subscribes to AcpService session events and posts narration/heartbeat to the sub-agent's origin room (Discord channel, Telegram chat/thread, etc.). The \"don't be annoying\" cadence is real and centralized — resolveSubAgentProgressPolicy parses ACPX_PROGRESS_MODE (compact|threaded|silent, plus an undocumented \"ack\" mode) and ACPX_PROGRESS_DELAY_MS (default 15s, capped 120s) with ACPX_PROGRESS_REACTIONS, falling back to process.env when getSetting misses; a capability-aware routing ladder (thread → edit-in-place → fresh send) plus an LLM heartbeat (10s fast / 30s slow) with dedupe keeps the channel clean. On task_complete, sub-agent-router.ts also fans the result out to the cross-platform AgentNotification surface (category \"agent\", deepLink \"/orchestrator\"), which the UI notification-store delivers to in-app toast, desktop OS, and mobile local notifications. Connectors expose the needed primitives via MessageConnector capabilities: Discord has all five (send/react/edit/create_thread/post_to_thread), Telegram has send/create_thread/post_to_thread but not edit/react. Slash commands/shortcuts (plugin-commands) surface /subagents but no coding-task-specific shortcut, and client commands are filtered off chat connectors by design. Maturity is partial: the policy logic is well unit-tested and the production code is thorough, but the actual posting/heartbeat/notification surfacing is largely untested and has zero trajectory/screenshot/video/timeline evidence. The native plugins (mobile-signals, eliza-tasks, reminders) are generic device bridges, not coding-task surfaces.",
    "maturity": "partial",
    "existing": [
      {
        "component": "Sub-agent progress hook (cadence engine + connector posting)",
        "path": "plugins/plugin-agent-orchestrator/src/index.ts",
        "whatItDoes": "registerProgressHook (L729) subscribes to AcpService events and posts progress to the origin room. emitProgress (L1131) is the routing ladder: per-session thread → edit-in-place main message → fresh send, all capability-gated via hasCap/getMessageConnectors. Adds 🚀/✅/❌ reactions when supported, caches one main message + one thread per (source,roomId,label), dedupes spawn ACKs per session and per room (ACK_ROOM_DEDUP_MS=60s), and wraps runtime.sendMessageToTarget to redirect planner chatter into the per-task thread.",
        "maturity": "solid"
      },
      {
        "component": "Progress-mode policy resolution (the 'don't be annoying' control)",
        "path": "plugins/plugin-agent-orchestrator/src/index.ts",
        "whatItDoes": "resolveSubAgentProgressPolicy (L532) + parseProgressMode/parseProgressDelayMs/parseProgressReactions (L499-529) read ACPX_PROGRESS_MODE / ELIZA_SUB_AGENT_PROGRESS_MODE (compact|threaded|silent|ack), ACPX_PROGRESS_DELAY_MS (default 15000, clamped 0..120000), ACPX_PROGRESS_REACTIONS. 'silent' suppresses all posts; 'ack' posts one spawn ACK and forces delayMs=0; default 'compact' debounces 15s and strips emoji/labels. Falls back to process.env when runtime.getSetting misses (env-only config bug fix).",
        "maturity": "solid"
      },
      {
        "component": "LLM heartbeat summarizer",
        "path": "plugins/plugin-agent-orchestrator/src/index.ts",
        "whatItDoes": "startHeartbeat (L972) runs every 10s (edit-capable surfaces) or 30s (post-only) while a session is non-terminal, summarizing recent narration+tool-calls via ModelType.TEXT_SMALL (HEARTBEAT_SUMMARY_PROMPT, L649), skipping when there is nothing concrete and deduping identical consecutive lines — explicitly preferring silence over a repeated 'still working' line.",
        "maturity": "solid"
      },
      {
        "component": "Coding-task completion → OS/in-app notification",
        "path": "plugins/plugin-agent-orchestrator/src/services/sub-agent-router.ts",
        "whatItDoes": "On task_complete (L831-854) calls getNotifier(runtime).notify({title:'<label> finished', body:<preview>, category:'agent', priority:'normal', source:'orchestrator', deepLink:'/orchestrator', groupKey:'orchestrator:<sessionId>'}). getNotifier (L1375) resolves ServiceType.NOTIFICATION. This is the push-to-user path for notifications/home surfaces.",
        "maturity": "solid"
      },
      {
        "component": "Cross-platform AgentNotification contract + delivery",
        "path": "packages/core/src/types/notification.ts",
        "whatItDoes": "Defines AgentNotification with categories including 'task' (task-coordinator/scheduled completion) and 'agent' (background/coding agent finished), priority, deepLink, groupKey. notification-store.ts (packages/ui) ingests and fans out to in-app toast, desktop OS (Electrobun), and mobile local notifications; native-notifications.ts bridges to @capacitor/local-notifications, ElizaIntent, or web Notification.",
        "maturity": "solid"
      },
      {
        "component": "Chat-surface TASK widget block",
        "path": "plugins/plugin-agent-orchestrator/src/actions/tasks.ts",
        "whatItDoes": "TASKS:create mints a durable task thread and emits [TASK:<threadId>]<title>[/TASK] in the callback text (L724-727) so the chat surface renders a TaskWidget linking back to the workbench.",
        "maturity": "solid"
      },
      {
        "component": "Dashboard widgets + orchestrator workbench surface",
        "path": "plugins/plugin-task-coordinator/src/OrchestratorWorkbench.tsx",
        "whatItDoes": "Orchestrator declares three chat-sidebar widgets (agent-orchestrator.apps/accounts/activity, src/index.ts L183-211). plugin-task-coordinator renders OrchestratorWorkbench, CodingAgentTasksPanel, and a view 'interact' capability handler (CodingAgentTasksPanel.interact.ts) that calls client.getCodingAgentStatus / listCodingAgentTaskThreads.",
        "maturity": "solid"
      },
      {
        "component": "Telegram connector capabilities + thread support",
        "path": "plugins/plugin-telegram/src/service.ts",
        "whatItDoes": "Registers MessageConnector with capabilities send_message/resolve_targets/list_rooms/chat_context/user_context/create_thread/post_to_thread (L54-62). Notably lacks edit_message and react_message, so compact-mode progress falls to fresh-send and reactions are unavailable on Telegram.",
        "maturity": "solid"
      },
      {
        "component": "Discord connector capabilities (full set)",
        "path": "plugins/plugin-discord/service.ts",
        "whatItDoes": "Declares send_message/react_message/edit_message/create_thread/post_to_thread (L318-335), so the orchestrator's full routing ladder (edit-in-place, reactions, threaded narration) works on Discord.",
        "maturity": "solid"
      },
      {
        "component": "Slash commands + shortcuts surface",
        "path": "plugins/plugin-commands/src/actions/shortcuts.ts",
        "whatItDoes": "Builds explicit slash shortcuts for gate-safe commands; registry includes /subagents (auth-gated, management category). connector-catalog.ts filters client commands off chat connectors. No coding-task-specific shortcut exists; coding tasks are surfaced via the planner action TASKS, not a dedicated slash command.",
        "maturity": "partial"
      },
      {
        "component": "Native device bridges (NOT coding-task specific)",
        "path": "plugins/plugin-native-eliza-tasks/src/definitions.ts",
        "whatItDoes": "capacitor-eliza-tasks bridges iOS BGTaskScheduler/APNs wakes to a uniform 'wake' event; capacitor-mobile-signals bridges wake/lock/battery/health; plugin-native-reminders is a macOS Reminders bridge. These are generic infrastructure, not channels that surface coding-task progress.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/strip-progress-label-prefix.test.ts",
        "kind": "unit",
        "covers": "resolveSubAgentProgressPolicy (compact default, threaded opt-in, ack mode, ack forces delayMs=0, process.env fallback), stripProgressLabelPrefix (all emoji prefixes incl. variation-selector composites), compactProgressText, plannerAlreadyAckedSpawn (spawn-ACK dedup logic).",
        "gaps": "Pure functions only. Does NOT test emitProgress routing (thread vs edit vs send), the delay/debounce timer behavior, the heartbeat loop, dedupe/eviction caches, or the sendMessageToTarget thread-redirect wrapper."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/sub-agent-router.test.ts",
        "kind": "unit",
        "covers": "task_complete posts a synthetic memory to the origin room; workdir route metadata; deduped swarm rooms; threading planner replies through sendMessageToTarget; routing QUESTION_FOR_TASK_CREATOR / AGENT_COORDINATION; not injecting for streaming events; dedupe of duplicate task_complete.",
        "gaps": "Does NOT assert the AgentNotification emission on task_complete (getNotifier(...).notify with category 'agent' / deepLink '/orchestrator') — confirmed zero references to notify/Notifier in this test. The notification surfacing path is unverified."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/create-task-emits-widget-block.test.ts",
        "kind": "unit",
        "covers": "TASKS:create emits the [TASK:<id>]<title>[/TASK] chat widget block on success and omits it (still succeeding) when createTask throws.",
        "gaps": "Only the chat-text widget marker; does not test the sidebar dashboard widgets or how a connector renders the marker."
      },
      {
        "path": "plugins/plugin-telegram/src/messageConnector.test.ts",
        "kind": "unit",
        "covers": "Telegram connector registers chat+thread capabilities, forum-topic channel parsing, account-scoped send routing, target resolution.",
        "gaps": "Does not test orchestrator progress posting through Telegram (compact fresh-send vs thread routing), nor the absence of edit/react capability paths under load."
      },
      {
        "path": "plugins/plugin-discord/actions/messageConnector.test.ts",
        "kind": "unit",
        "covers": "Discord connector metadata/capabilities registration, target resolution, account scoping, reply-reference on send.",
        "gaps": "No coverage of orchestrator edit-in-place progress, reaction stamping (🚀/✅), or thread narration routing actually wired through Discord."
      },
      {
        "path": "plugins/plugin-commands/__tests__/connector-catalog.test.ts",
        "kind": "unit",
        "covers": "Navigation/command catalog projection, client commands filtered off chat connectors (discord/telegram), view-scoped visibility.",
        "gaps": "No coding-task / orchestrator command surfacing test; /subagents handler behavior on a connector is untested."
      },
      {
        "path": "packages/ui/src/state/notifications/notification-store.test.ts",
        "kind": "unit",
        "covers": "Notification inbox ingest/unread count, desktop+native sinks fire when unfocused, interrupt sinks for high/urgent, toast tone, WS stream subscription, mark-read APIs.",
        "gaps": "Tests the store generically; does NOT verify an orchestrator-produced 'agent' notification (from a coding task) flows end-to-end into the store and the OS/mobile sinks with the /orchestrator deepLink."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
        "kind": "live",
        "covers": "Gated (RUN_LIVE_ACPX=1) full loop: real acpx subprocess → task_complete → SubAgentRouter → synthetic memory → fake messageService. Verifies routing, not user-facing surfacing.",
        "gaps": "Uses a fake message service; does not drive a real connector (Discord/Telegram) or assert a real progress message / notification appears to a user. No cadence (delay/heartbeat) assertions."
      },
      {
        "path": "plugins/plugin-task-coordinator/test/orchestrator-workbench.live.e2e.test.ts",
        "kind": "e2e",
        "covers": "Live Playwright drive of the /orchestrator workbench dev stack: asserts no raw ACP JSON leaks, no horizontal overflow, user message appears, no console errors.",
        "gaps": "UI workbench only; gated on a running stack (skipped otherwise). Does not cover connector surfacing, notifications, widgets, or progress cadence. No screenshots/video captured as evidence."
      },
      {
        "path": "plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs",
        "kind": "e2e",
        "covers": "Headless-chromium screenshot harness for the orchestrator dashboard cards (populated + empty, desktop + mobile) with stubbed agent-surface hook.",
        "gaps": "Renders card components in isolation against stubbed data; not a real coding-task run, no connector/notification surfacing, output is not wired into issue-evidence."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No scenario-runner scenario or trajectory captures coding-task progress surfacing to a connector or notification. grep of packages/scenario-runner for ACPX_PROGRESS/sub_agent_progress/task_complete-notif returned nothing."
      },
      {
        "type": "screenshot",
        "exists": true,
        "where": "plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs produces dashboard-shots/ (isolated cards, stubbed data) — not a real surfacing flow. .github/issue-evidence/ contains only 8810/8812 PNGs, unrelated to Domain K."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of a coding task surfacing progress to Discord/Telegram/notification. No test:e2e:record artifact for this domain."
      },
      {
        "type": "log",
        "exists": false,
        "where": "No captured structured [ClassName] logs demonstrating emitProgress/heartbeat/notify on a real run are stored as evidence. The code logs via runtime.logger but nothing is persisted for review."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline artifact showing progress cadence (delay → heartbeat ticks → completion) across a session, despite cadence being the core 'don't be annoying' feature."
      },
      {
        "type": "report",
        "exists": false,
        "where": "No JSON/report evidence for the surfacing path. orchestrator-workbench.live.e2e asserts via expect() but emits no report file."
      },
      {
        "type": "none",
        "exists": true,
        "where": "Net: for Domain K the only concrete artifact is the isolated dashboard screenshot harness; the connector/notification/cadence surfacing has no real evidence."
      }
    ],
    "gaps": [
      {
        "title": "No test asserts coding-task completion emits an AgentNotification",
        "severity": "high",
        "description": "sub-agent-router.ts:831-854 fires getNotifier(runtime).notify({category:'agent', deepLink:'/orchestrator', groupKey:'orchestrator:<sessionId>'}) on task_complete, but sub-agent-router.test.ts has ZERO references to notify/Notifier (confirmed by grep). A regression that drops or mis-shapes the notification — the primary push-to-user/home-surface signal for finished coding tasks — would pass CI silently.",
        "suggestedWork": "Add a unit test in sub-agent-router.test.ts that registers a mock ServiceType.NOTIFICATION service and asserts notify is called once on task_complete with title containing the label, category 'agent', source 'orchestrator', deepLink '/orchestrator', and groupKey 'orchestrator:<sessionId>'; assert it is NOT called for error/blocked/streaming events."
      },
      {
        "title": "emitProgress routing ladder + cadence is untested",
        "severity": "high",
        "description": "The thread→edit→send ladder, ACPX_PROGRESS_DELAY_MS debounce, ack-mode one-shot suppression, per-room ACK dedup, heartbeat 10s/30s loop, and the sendMessageToTarget thread-redirect wrapper are ~600 lines of intricate, regression-prone logic in index.ts with no direct tests (only the pure policy parser is covered). The 'don't be annoying' guarantees (no duplicate ACKs, silence over repetition, delay-then-post) are unverified.",
        "suggestedWork": "Add a unit test that constructs registerProgressHook against a mock runtime exposing getMessageConnectors with controllable capabilities and spy send/edit/createThread/postToThread/addReaction. Drive synthetic AcpService events and assert: (a) compact mode posts one debounced fresh message after delayMs; (b) ack mode posts exactly one ACK per room within ACK_ROOM_DEDUP_MS and never edits; (c) silent mode posts nothing; (d) threaded mode creates one thread per label and routes narration there; (e) edit-capable surface edits one message in place; (f) heartbeat skips when no content and dedupes identical lines."
      },
      {
        "title": "No end-to-end surfacing test through a real connector",
        "severity": "high",
        "description": "There is no integration/e2e test that runs a coding task and asserts a progress message and a completion summary actually arrive on Discord and Telegram (the two connectors with differing capability sets — Discord edits/reacts, Telegram only threads/sends). The capability fork (edit_message present vs absent) is the highest-risk surface and is exercised only by isolated connector-registration tests.",
        "suggestedWork": "Add a scenario-runner scenario (or connector live-smoke) that spawns a short sub-agent task and verifies, against Discord (full caps) and Telegram (no edit/react), that exactly one spawn ACK + one completion summary land, threads are created only where supported, and no duplicate/empty messages appear. Capture the trajectory + connector message log as evidence."
      },
      {
        "title": "Zero trajectory/log/video/timeline evidence for the surfacing domain",
        "severity": "high",
        "description": "Per AGENTS.md, agent/action/prompt changes require real-LLM trajectories, connector logs, before/after screenshots, and a video walkthrough. For coding-task surfacing none exist: no scenario trajectory, no captured progress timeline, no video of progress arriving in a chat or as a notification, no issue-evidence artifacts for this domain.",
        "suggestedWork": "Build a recorded walkthrough harness that runs a real coding task, captures (1) the structured [orchestrator] progress/heartbeat/notify logs, (2) a timeline of cadence events (spawn ACK at T0, heartbeat ticks, completion), (3) screenshots/video of the message in Discord/Telegram and the in-app/OS notification, and lands them under .github/issue-evidence/."
      },
      {
        "title": "AgentNotification fan-out to OS/mobile not verified for orchestrator-sourced notifications",
        "severity": "medium",
        "description": "notification-store.test.ts verifies the store generically and native-notifications.ts has a documented but untested 3-tier fallback (LocalNotifications → ElizaIntent → web Notification). No test confirms an orchestrator 'agent' notification with deepLink '/orchestrator' actually reaches a desktop OS or mobile local notification and deep-links correctly on tap.",
        "suggestedWork": "Add an integration test that ingests an orchestrator-shaped AgentNotification into the notification store while window unfocused and asserts the desktop+native sinks fire with the correct title/body/deepLink; add a mobile bridge test stubbing Capacitor.Plugins.LocalNotifications and asserting schedule() is called with the derived id and the /orchestrator deep link."
      },
      {
        "title": "No coding-task slash command / shortcut / home-screen widget on connectors",
        "severity": "medium",
        "description": "Coding tasks are surfaced through the planner TASKS action and a chat [TASK:..] marker, plus auth-gated /subagents. There is no first-class slash command to start/check a coding task from a connector, no home-screen widget specifically for coding-task status (the native plugins are generic device bridges), and client/view commands are deliberately filtered off chat connectors. Users on Discord/Telegram cannot deterministically query coding-task status without the LLM routing.",
        "suggestedWork": "Decide whether to add a connector-safe agent-target command (e.g. /tasks or /coding) that lists active sub-agent sessions and progress via TASKS_LIST_AGENTS, register it as an explicit shortcut, and test it through connector-catalog (must appear on chat connectors, unlike client commands). If a mobile home-screen widget for coding-task status is intended, add it to plugin-native-* with a status feed; currently none exists."
      },
      {
        "title": "ACK mode and ELIZA_SUB_AGENT_PROGRESS_* aliases are under-documented vs implemented",
        "severity": "low",
        "description": "The code supports an 'ack' progress mode and ELIZA_SUB_AGENT_PROGRESS_MODE/_DELAY_MS/_REACTIONS aliases plus values off/disabled/none/0/false→silent, but the package CLAUDE.md documents only compact/threaded/silent for ACPX_PROGRESS_MODE. Operators tuning the 'don't be annoying' behavior may not discover ack mode (clean ack+final UX with no in-place edits).",
        "suggestedWork": "Update plugins/plugin-agent-orchestrator/CLAUDE.md (and AGENTS.md copy) to document the 'ack' mode, the ELIZA_SUB_AGENT_PROGRESS_* aliases, and the accepted silent synonyms, matching parseProgressMode in src/index.ts."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/index.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/sub-agent-router.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/actions/tasks.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/strip-progress-label-prefix.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/sub-agent-router.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/create-task-emits-widget-block.test.ts",
      "/Users/shawwalters/eliza/packages/core/src/types/notification.ts",
      "/Users/shawwalters/eliza/packages/core/src/services/notification.ts",
      "/Users/shawwalters/eliza/packages/ui/src/state/notifications/notification-store.ts",
      "/Users/shawwalters/eliza/packages/ui/src/bridge/native-notifications.ts",
      "/Users/shawwalters/eliza/plugins/plugin-telegram/src/service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-discord/service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-commands/src/actions/shortcuts.ts",
      "/Users/shawwalters/eliza/plugins/plugin-commands/src/connector-catalog.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/CodingAgentTasksPanel.interact.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/test/orchestrator-workbench.live.e2e.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-task-coordinator/src/__e2e__/run-dashboard-shot.mjs",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/CLAUDE.md"
    ]
  },
  {
    "domain": "L — Multiple simultaneous tasks/projects (concurrency, session/workspace isolation, scheduling, stress to 10)",
    "summary": "Concurrency for multiple simultaneous coding sub-agents lives almost entirely in plugins/plugin-agent-orchestrator. There are two distinct limiters: a hard session cap ELIZA_ACP_MAX_SESSIONS (default 8) enforced atomically via a promise-chain mutex (reserveSessionSlot + enforceSessionLimit in acp-service.ts), and a soft spawn-gate ELIZA_MAX_CONCURRENT_SPAWNS (default 2, despite README/CLAUDE.md saying 'unset') implemented as a polling wait in actions/common.ts:waitForSpawnSlot. Session state is persisted through a tiered SessionStore (runtime-db / file-with-lock / in-memory) with a WriteQueue that serializes writes, plus startup orphan reconciliation, a 60s health-check sweep, retention GC, and reverse-orphan acpx file cleanup. The hard-cap atomicity is genuinely well-tested (a 6-spawn race against cap=2), but the system is NOT configured to handle 10 simultaneous projects out of the box: maxSessions defaults to 8 (<10) and the spawn-gate serializes to 2-at-a-time. The most serious correctness issue is that default workspace isolation does not exist — when a task matches no route/convention/explicit workdir, every concurrent session collapses to the SAME directory (process.cwd() or ELIZA_ACP_WORKSPACE_ROOT); git-worktree isolation is opt-in via useWorktree. There is no end-to-end/stress test (and no trajectory/log/screenshot/timeline evidence) for N independent simultaneous projects; SessionEventQueue is defined but completely unwired.",
    "maturity": "partial",
    "existing": [
      {
        "component": "AcpService session cap (ELIZA_ACP_MAX_SESSIONS, default 8) + atomic reserveSessionSlot/enforceSessionLimit mutex",
        "path": "plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
        "whatItDoes": "Caps concurrent non-terminal sessions at maxSessions (default 8). reserveSessionSlot (lines ~2000-2014) is a promise-chain mutex that wraps enforceSessionLimit (list+count) and store.create into one critical section so N concurrent spawns cannot overshoot the cap. Also installs one process-wide SIGTERM/SIGINT shutdown hook via a static liveInstances registry, a 60s health-check, orphan reconciliation, and retention sweep.",
        "maturity": "solid"
      },
      {
        "component": "waitForSpawnSlot soft spawn-gate (ELIZA_MAX_CONCURRENT_SPAWNS, default 2)",
        "path": "plugins/plugin-agent-orchestrator/src/actions/common.ts",
        "whatItDoes": "Lines 343-379. Before each orchestrated spawn (called from actions/tasks.ts:876), polls real session state every 3s and blocks until active sessions < limit (default 2), giving up after 8min and proceeding anyway. Exists because gpt-oss-class models on OpenAI-compatible providers silently skip tool calls under concurrent load. Default is 2, contradicting the README/CLAUDE.md which list it as 'unset'.",
        "maturity": "partial"
      },
      {
        "component": "Tiered SessionStore (RuntimeDb / File-with-lock / InMemory) + WriteQueue",
        "path": "plugins/plugin-agent-orchestrator/src/services/session-store.ts",
        "whatItDoes": "AcpSessionStore selects runtime-db > file > memory. InMemorySessionStore serializes mutations through a WriteQueue promise chain; FileSessionStore adds an atomic temp+rename write under a stale-aware file lock (FILE_LOCK_ACQUIRE_TIMEOUT_MS=30s); RuntimeDbSessionStore uses INSERT OR REPLACE with its own WriteQueue. sweepStale removes terminal sessions past retention.",
        "maturity": "solid"
      },
      {
        "component": "CodingWorkspaceService — worktree/clone provisioning + scratch GC/retention",
        "path": "plugins/plugin-agent-orchestrator/src/services/workspace-service.ts",
        "whatItDoes": "provisionWorkspace picks strategy 'worktree' vs 'clone' from options.useWorktree (line 439). Tracks workspaces in a Map, schedules per-session scratch cleanup timers (scheduleScratchCleanup), reads ELIZA_SCRATCH_RETENTION / ELIZA_SCRATCH_DECISION_TTL_MS, defaults workspaceTtlMs to 24h, and runs startup orphan-workspace GC. Worktree isolation is OPT-IN only.",
        "maturity": "partial"
      },
      {
        "component": "resolveSpawnWorkdir — default workspace resolution (the isolation gap)",
        "path": "plugins/plugin-agent-orchestrator/src/services/task-agent-routing.ts",
        "whatItDoes": "Resolves spawn cwd via route > convention > explicit > default. resolveDefaultSpawnWorkdir (lines 142-156) falls back to a SINGLE shared dir: ELIZA_ACP_WORKSPACE_ROOT / ACPX_DEFAULT_CWD, or process.cwd() when unset. No per-session subdir is created, so multiple concurrent ad-hoc tasks with no route/worktree all write into the same directory.",
        "maturity": "partial"
      },
      {
        "component": "gcOrphanedWorkspaces / removeScratchDir (workspace lifecycle GC)",
        "path": "plugins/plugin-agent-orchestrator/src/services/workspace-lifecycle.ts",
        "whatItDoes": "removeScratchDir refuses to rm outside allowed base dirs; gcOrphanedWorkspaces removes untracked dirs older than workspaceTtlMs (TTL=0 disables). Keeps tracked workspace IDs safe.",
        "maturity": "solid"
      },
      {
        "component": "smithers-task-runner parallelAgents (intra-task fan-out)",
        "path": "plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
        "whatItDoes": "Builds a Smithers workflow that runs `agents = max(1, parallelAgents)` agent loops; uses wf.parallel(...loops) when agents>1 (line 191). This is N agents on ONE task, NOT N independent projects. Durable resume across crash is supported.",
        "maturity": "solid"
      },
      {
        "component": "SessionEventQueue (per-session ordered event delivery)",
        "path": "plugins/plugin-agent-orchestrator/src/services/session-event-queue.ts",
        "whatItDoes": "Implements per-session FIFO queues with a processing Set so events for one session are delivered in order without head-of-line blocking across sessions. However grep shows ZERO importers in src — it is defined and exported but completely unwired; ordered delivery is not actually used by AcpService/SubAgentRouter.",
        "maturity": "stub"
      },
      {
        "component": "OrchestratorTaskService — concurrent sub-agents per task + distinct naming",
        "path": "plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
        "whatItDoes": "Durable task store and lifecycle bridge from ACP events to task records; spawnAgentForTask assigns distinct pooled names so two concurrent sub-agents on one task don't collide.",
        "maturity": "solid"
      },
      {
        "component": "GET /api/orchestrator/rooms — workbench surfacing of concurrent sessions",
        "path": "plugins/plugin-agent-orchestrator/src/api/orchestrator-routes.ts",
        "whatItDoes": "Lines ~214-303. Returns a per-room participant roster grouping live sessions by room (orchestrator + user + sub-agents), omitting rooms with no sub-agents; emits widget refresh on status/usage changes so the workbench reflects multiple live sessions.",
        "maturity": "partial"
      },
      {
        "component": "plugin-background-runner BgTaskSchedulerService",
        "path": "plugins/plugin-background-runner/src/services/BgTaskSchedulerService.ts",
        "whatItDoes": "Bridges OS background wake-ups (Capacitor BGTaskScheduler/WorkManager) or a setInterval fallback to core TaskService.runDueTasks(); single periodic wake, not a concurrent sub-agent scheduler. Unrelated to ACP session concurrency despite being in scope; no overlap/concurrency handling for simultaneous wakes.",
        "maturity": "partial"
      }
    ],
    "tests": [
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/acp-service.test.ts",
        "kind": "unit",
        "covers": "The headline concurrency test: 'enforces ELIZA_ACP_MAX_SESSIONS atomically under concurrent spawns' (line 1528) fires 6 simultaneous spawnSession calls with cap=2 via Promise.allSettled and asserts exactly 2 fulfilled, 4 rejected with 'max session limit reached', and store agrees only 2 active. Proves reserveSessionSlot is race-free. Also covers cancel SIGTERM/SIGKILL, orphan reconcile windows.",
        "gaps": "Only tests the hard cap atomicity with the native fast path. Does NOT test cap at the documented default (8) or near 10. No test that the cap and the spawn-gate interact. No test of mid-flight session limit + retention sweep interplay under load."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/session-store.test.ts",
        "kind": "unit",
        "covers": "'serializes concurrent writes' for InMemorySessionStore (25 parallel creates) and FileSessionStore (20 parallel creates) both assert full count survives; stale-lock recovery; sweepStale selects only old stopped/errored; lastActivityAt transitions; findByScope.",
        "gaps": "No cross-process file-lock contention test (two FileSessionStore instances racing on the same file). No RuntimeDbSessionStore concurrency test. No test that 10 sessions persist+reload correctly across a simulated restart."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/orchestrator-task-service.test.ts",
        "kind": "unit",
        "covers": "'assigns distinct names to two concurrent sub-agents on one task' (line 245) — two spawnAgentForTask on one task yield 2 sessions with distinct non-empty labels.",
        "gaps": "Only 2 sub-agents, on a single task, via FakeAcp. No test of many tasks each with sub-agents concurrently; no real subprocess; no workspace isolation assertion."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-runner.test.ts",
        "kind": "unit",
        "covers": "'fans out parallel agents and completes when all are done' (line 144) with parallelAgents:2 asserts 2 agents x 2 turns; plus durable crash-resume skipping completed turns.",
        "gaps": "parallelAgents tested only at 2 and with a FakeExecutor (no real ACP turns). This is intra-task fan-out, not N independent projects. No stress beyond 2."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/resolve-spawn-workdir.test.ts",
        "kind": "unit",
        "covers": "Explicit workdir fallback, ELIZA_ACP_WORKSPACE_ROOT / ACPX_DEFAULT_CWD precedence, convention auto-detect, self-checkout default to process.cwd(), malformed route guard.",
        "gaps": "Crucially does NOT assert that two concurrent tasks get DISTINCT isolated workdirs — it actually documents/locks-in the shared-dir default. No test for collision when N sessions land on the same fallback dir."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/unit/orchestrator-rooms-route.test.ts",
        "kind": "unit",
        "covers": "GET /api/orchestrator/rooms groups live sessions by room with orchestrator+user+sub-agents, omits empty rooms, serves roster over the route.",
        "gaps": "No assertion of correct grouping/ordering under many (e.g. 10) concurrent rooms/sessions; no large-roster or churn-under-load test."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/__tests__/live/sub-agent-router.live.test.ts",
        "kind": "live",
        "covers": "One real acpx subprocess runs codex and emits task_complete routed back as a synthetic memory (single session).",
        "gaps": "Single session only. No live multi-session/concurrent stress; gated on acpx being installed so usually skipped in CI."
      },
      {
        "path": "plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
        "kind": "e2e",
        "covers": "Composed offline pipeline: AccountPool -> selector bridge -> AcpService.spawnSession -> real (fake) subprocess receiving injected per-account creds; proves multi-account selection end-to-end.",
        "gaps": "Spawns are SEQUENTIAL (spawn 1 then spawn 2), not concurrent. Does not exercise the session cap, spawn-gate, or workspace isolation under simultaneous load."
      },
      {
        "path": "plugins/plugin-background-runner/__tests__/unit/bg-scheduler.test.ts",
        "kind": "unit",
        "covers": "BgTaskSchedulerService scheduler selection, serverless flag, wake->runDueTasks wiring; IntervalBgScheduler fallback.",
        "gaps": "No overlapping-wake / re-entrancy / concurrent runDueTasks test."
      }
    ],
    "evidence": [
      {
        "type": "trajectory",
        "exists": false,
        "where": "No real-LLM trajectory exists for N simultaneous coding tasks. scenario-runner has no concurrency scenario referencing ELIZA_ACP_MAX_SESSIONS; the only multi-agent harness (smithers parallelAgents) is unit-tested with FakeExecutor."
      },
      {
        "type": "log",
        "exists": true,
        "where": "Structured spawn-gate/health-check/reconcile logs exist in code ([spawn-gate], 'health-check self-healed sessions', 'reconcile: ...', 'coding account selected for spawn') but no captured log artifact of a concurrent N-session run is committed."
      },
      {
        "type": "screenshot",
        "exists": false,
        "where": "No screenshot of the orchestrator workbench/rooms view with multiple (let alone 10) concurrent sessions. .github/issue-evidence/ contains only unrelated PNGs (8810 banner, 8812 boot KPI)."
      },
      {
        "type": "video",
        "exists": false,
        "where": "No video walkthrough of running multiple simultaneous projects."
      },
      {
        "type": "timeline",
        "exists": false,
        "where": "No timeline/Gantt capture of concurrent session lifecycles (spawn/queue/run/complete) exists."
      },
      {
        "type": "report",
        "exists": true,
        "where": "(elizaOS/benchmarks) ORCHESTRATOR_SUBAGENT_BENCHMARK_RUNBOOK.md documents an orchestrator sub-agent benchmark but contains NO concurrency/N-simultaneous-tasks/stress section."
      }
    ],
    "gaps": [
      {
        "title": "System cannot handle 10 simultaneous projects with default config",
        "severity": "high",
        "description": "Two defaults block 10-way concurrency out of the box: ELIZA_ACP_MAX_SESSIONS defaults to 8 (acp-service.ts line 244), which is < 10, so the 9th/10th spawn rejects with 'max session limit reached'; and ELIZA_MAX_CONCURRENT_SPAWNS defaults to 2 (common.ts line 354), so the spawn-gate serializes orchestrated spawns to 2-at-a-time. Reaching 10 concurrent requires raising both, but there is no validated profile proving the runtime + model providers survive 10 concurrent sub-agents.",
        "suggestedWork": "Add a documented 'high-concurrency' config profile (ELIZA_ACP_MAX_SESSIONS>=10, ELIZA_MAX_CONCURRENT_SPAWNS tuned) and a stress test that spawns 10 sessions and asserts all reach ready/complete. Reconcile the README/CLAUDE.md claim that ELIZA_MAX_CONCURRENT_SPAWNS is 'unset' with the actual default of 2."
      },
      {
        "title": "No default per-session workspace isolation — concurrent ad-hoc tasks collide in one directory",
        "severity": "critical",
        "description": "resolveDefaultSpawnWorkdir (task-agent-routing.ts lines 142-156) collapses any task without a route/convention/explicit workdir to a SINGLE shared dir (ELIZA_ACP_WORKSPACE_ROOT/ACPX_DEFAULT_CWD or process.cwd()). Git-worktree isolation is opt-in (useWorktree). Running multiple simultaneous projects without explicit routing means all sub-agents read/write the same files, corrupting each other's work — exactly the failure mode for '10 simultaneous projects'. resolve-spawn-workdir.test.ts locks in this shared-dir behavior rather than guarding against it.",
        "suggestedWork": "Default to a per-session isolated subdir (e.g. <root>/<sessionId>) when no explicit workdir/route is given, OR auto-enable a worktree per concurrent session. Add a test asserting two concurrent spawns with no route get distinct workdirs, and a stress test asserting no cross-session file writes."
      },
      {
        "title": "No end-to-end / stress test for N simultaneous coding tasks",
        "severity": "high",
        "description": "The only concurrency test is the unit-level cap-atomicity test (6 spawns, cap 2, native fast path). There is no test that spins up N (e.g. 8-10) real or realistically-faked sub-agent sessions across distinct tasks, drives them through provision->turn->complete, and asserts isolation, correct workbench/rooms surfacing, retention sweep, and no slot leak. The multi-account e2e spawns sequentially.",
        "suggestedWork": "Author a scenario-runner scenario (and a composed offline harness like compose-multi-account-e2e but concurrent) that launches 10 tasks via TASKS_CREATE, captures a real-LLM trajectory per session, asserts all complete in isolated workspaces, and emits a timeline. Lower ACPX_SUB_AGENT_ROUND_TRIP_CAP for the test env."
      },
      {
        "title": "SessionEventQueue is dead/unwired code",
        "severity": "medium",
        "description": "session-event-queue.ts implements per-session ordered, non-head-of-line-blocking event delivery, but grep shows zero importers in src. AcpService/SubAgentRouter route events without it, so under concurrent load there is no guarantee that blocked/turn_complete events for a session are delivered in order, and a slow handler for one session can block others.",
        "suggestedWork": "Either wire SessionEventQueue into SubAgentRouter/AcpService event emission (and test ordered delivery + cross-session isolation under interleaved events) or delete it as dead code."
      },
      {
        "title": "No cross-process / multi-instance file-lock contention test",
        "severity": "medium",
        "description": "FileSessionStore uses an O_EXCL lock + stale-lock recovery to coordinate writes, but tests only cover single-instance in-process serialization. Two AcpService instances (multi-tenant, hot-reload overlap) hitting the same sessions.json concurrently is untested, and a lost-write or stale-lock thrash there would silently drop sessions.",
        "suggestedWork": "Add a test that creates two FileSessionStore instances on the same file and races creates/updates, asserting no lost writes and correct stale-lock takeover."
      },
      {
        "title": "Workbench surfacing of many concurrent sessions is unverified",
        "severity": "medium",
        "description": "GET /api/orchestrator/rooms grouping is tested only with a handful of sessions; there is no test or screenshot proving the rooms roster + widget refresh remain correct and performant with 8-10 live sessions churning status/usage. No frontend (packages/app) source was found wiring ELIZA_ACP_MAX_SESSIONS or a multi-session board.",
        "suggestedWork": "Add a route test with 10 concurrent rooms/sessions asserting correct grouping/ordering, and capture before/after screenshots of the workbench with 10 live sessions for evidence."
      },
      {
        "title": "Spawn-gate (waitForSpawnSlot) has no unit test",
        "severity": "medium",
        "description": "waitForSpawnSlot (common.ts 343-379) is the primary throttle that prevents concurrent-load model degradation, but it has no test for: limit<=0 disabling the gate, the 8min maxWaitMs giveup-and-proceed path, or correct active-session counting that excludes terminal statuses.",
        "suggestedWork": "Add unit tests for waitForSpawnSlot covering disabled (limit 0/negative), blocking-then-releasing as sessions terminate, and the giveup-after-maxWaitMs warn-and-proceed path."
      }
    ],
    "keyFiles": [
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/acp-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/session-store.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/session-event-queue.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/actions/common.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/task-agent-routing.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/workspace-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/workspace-lifecycle.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/smithers-task-runner.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/services/orchestrator-task-service.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/src/api/orchestrator-routes.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/acp-service.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/session-store.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/orchestrator-task-service.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/smithers-task-runner.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/__tests__/unit/resolve-spawn-workdir.test.ts",
      "/Users/shawwalters/eliza/plugins/plugin-agent-orchestrator/scripts/compose-multi-account-e2e.ts",
      "/Users/shawwalters/eliza/plugins/plugin-background-runner/src/services/BgTaskSchedulerService.ts",
      "/Users/shawwalters/eliza/(elizaOS/benchmarks) ORCHESTRATOR_SUBAGENT_BENCHMARK_RUNBOOK.md"
    ]
  }
]
