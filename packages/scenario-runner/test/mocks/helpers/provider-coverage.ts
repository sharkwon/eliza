/** Provides provider coverage helper utilities shared by package tests and scenario harnesses. */
import type { MockEnvironmentName } from "../scripts/start-mocks.ts";

export type LifeOpsProviderMockMode =
  | "stateful-http"
  | "static-http"
  | "dependency-seam"
  | "browser-workspace";

export interface LifeOpsProviderMockCoverage {
  id: string;
  label: string;
  mode: LifeOpsProviderMockMode;
  environment: MockEnvironmentName | null;
  envVars: readonly string[];
  surfaces: readonly string[];
  knownGaps: readonly string[];
  validation: readonly string[];
  rationale?: string;
}

export const REQUIRED_LIFEOPS_PROVIDER_IDS = [
  "google-calendar",
  "gmail",
  "github",
  "x",
  "whatsapp",
  "telegram",
  "signal",
  "discord",
  "imessage-bluebubbles",
  "twilio",
  "calendly",
  "eliza-cloud-managed-google",
  "discord-rest",
  "slack",
  "telegram-bot-http",
  "linear",
  "shopify",
  "payments",
  "anthropic",
  "openai",
  "vision-analysis",
] as const;

export const LIFEOPS_PROVIDER_MOCK_COVERAGE = [
  {
    id: "google-calendar",
    label: "Google Calendar",
    mode: "stateful-http",
    environment: "google",
    envVars: ["ELIZA_MOCK_GOOGLE_BASE"],
    surfaces: [
      "OAuth token and userinfo rewrite",
      "calendar list",
      "event list/get/search",
      "event create/patch/update/move/delete",
      "request ledger metadata",
    ],
    knownGaps: [
      "No recurring-event expansion beyond single synthetic events",
      "No freebusy, ACL, attachment, or conference-data surfaces",
      "No Google rate-limit or partial-failure variants",
    ],
    validation: [
      "test/mocks/__tests__/google-calendar-mock.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-personal-assistant/test/scenarios/calendar-llm-eval-mutations.scenario.ts",
    ],
  },
  {
    id: "gmail",
    label: "Gmail",
    mode: "stateful-http",
    environment: "google",
    envVars: ["ELIZA_MOCK_GOOGLE_BASE", "ELIZA_BLOCK_REAL_GMAIL_WRITES"],
    surfaces: [
      "work/home account fixture data",
      "message list/get/search/send/modify/delete",
      "thread list/get/modify/trash/untrash",
      "draft create/list/get/send/delete",
      "attachment metadata and download",
      "labels, history, watch, filters",
      "priority, vague, multi-search, and cross-account query fixtures",
      "write request ledger metadata",
    ],
    knownGaps: [
      "Search is deterministic fixture matching, not the full Gmail query grammar",
      "No attachment upload or full multipart MIME fidelity",
      "No delegated mailbox, push-notification, quota, or rate-limit variants",
    ],
    validation: [
      "test/mocks/__tests__/google-mock-fidelity.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-personal-assistant/test/scenarios/gmail-llm-eval-search-priority.scenario.ts",
    ],
  },
  {
    id: "github",
    label: "GitHub",
    mode: "stateful-http",
    environment: "github",
    envVars: ["ELIZA_MOCK_GITHUB_BASE", "GITHUB_API_URL"],
    surfaces: [
      "REST pull request list/review",
      "issue creation and assignment fixtures",
      "issue/PR search",
      "notification list",
      "Octokit-shaped unit-test fixture",
      "request ledger metadata",
    ],
    knownGaps: [
      "No GraphQL API coverage",
      "No checks, statuses, contents, branch protection, or workflow endpoints",
      "No webhook delivery simulation",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "test/mocks/helpers/github-octokit-fixture.ts",
    ],
  },
  {
    id: "x",
    label: "X",
    mode: "stateful-http",
    environment: "x-twitter",
    envVars: ["ELIZA_MOCK_X_BASE"],
    surfaces: [
      "home timeline",
      "mentions",
      "recent search",
      "DM list",
      "tweet create",
      "DM send",
      "request ledger metadata",
    ],
    knownGaps: [
      "No streaming API, OAuth handshake, media upload, or delete/like/repost surfaces",
      "No rate-limit, partial response, or protected-account variants",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "plugins/plugin-personal-assistant/src/actions/search-across-channels.test.ts",
    ],
  },
  {
    id: "whatsapp",
    label: "WhatsApp Business Cloud",
    mode: "stateful-http",
    environment: "whatsapp",
    envVars: ["ELIZA_MOCK_WHATSAPP_BASE"],
    surfaces: [
      "text message send",
      "inbound webhook ingestion",
      "Cloud API webhook metadata and contact mapping in simulator seed",
      "test-only inbound buffer route",
      "request ledger metadata",
    ],
    knownGaps: [
      "No media upload/download, templates, reactions, or message status lifecycle",
      "No webhook signature validation or delivery retry simulation",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "test/mocks/__tests__/mock-runtime-seeding.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-personal-assistant/test/whatsapp.test.ts",
    ],
  },
  {
    id: "telegram",
    label: "Telegram",
    mode: "dependency-seam",
    environment: null,
    envVars: [],
    surfaces: [
      "MTProto local-client dependency injection",
      "encoded local mock session for full LifeOps simulator runs",
      "auth retry state",
      "connector service status",
      "send/search/read-receipt calls through mocked client deps",
    ],
    knownGaps: [
      "No central HTTP mock because LifeOps does not consume Telegram through HTTP",
      "No MTProto protocol simulator, media fixture, or group-admin fixture",
    ],
    rationale:
      "LifeOps delegates Telegram transport to plugin-telegram; an HTTP Mockoon facade would test a path the product does not call.",
    validation: [
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-telegram/src/local-client.test.ts",
      "plugins/plugin-personal-assistant/src/lifeops/service-mixin-telegram.test.ts",
      "plugins/plugin-personal-assistant/test/cross-channel-send.test.ts",
    ],
  },
  {
    id: "signal",
    label: "Signal",
    mode: "stateful-http",
    environment: "signal",
    envVars: ["SIGNAL_HTTP_URL", "SIGNAL_ACCOUNT_NUMBER"],
    surfaces: [
      "signal-cli health check",
      "REST receive",
      "REST send",
      "JSON-RPC send",
      "connected service read path backed by signal-cli receive in simulator runs",
      "request ledger metadata",
    ],
    knownGaps: [
      "No attachment, group-management, profile, registration, or safety-number surfaces",
      "No daemon restart, backfill, or malformed-envelope variants",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "test/mocks/__tests__/mock-runtime-seeding.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-signal/src/local-client.test.ts",
      "plugins/plugin-personal-assistant/src/lifeops/service-mixin-signal.test.ts",
    ],
  },
  {
    id: "discord",
    label: "Discord",
    mode: "browser-workspace",
    environment: "browser-workspace",
    envVars: [
      "ELIZA_BROWSER_WORKSPACE_URL",
      "ELIZA_BROWSER_WORKSPACE_TOKEN",
      "ELIZA_DISABLE_DISCORD_DESKTOP_CDP",
    ],
    surfaces: [
      "desktop browser workspace tab lifecycle",
      "navigation",
      "script evaluation",
      "LifeOps outbound send handler through browser workspace eval",
      "snapshot",
      "request ledger metadata",
    ],
    knownGaps: [
      "No Discord REST or Gateway mock",
      "DOM fixture cannot prove Discord production layout compatibility",
      "No attachment, reaction, edit, or thread lifecycle coverage",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "test/mocks/__tests__/mock-runtime-seeding.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-personal-assistant/test/discord-browser-scraper.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-discord-browser-companion.test.ts",
    ],
  },
  {
    id: "imessage-bluebubbles",
    label: "iMessage / BlueBubbles",
    mode: "stateful-http",
    environment: "bluebubbles",
    envVars: [
      "ELIZA_IMESSAGE_BACKEND",
      "ELIZA_BLUEBUBBLES_URL",
      "BLUEBUBBLES_SERVER_URL",
      "ELIZA_BLUEBUBBLES_PASSWORD",
      "BLUEBUBBLES_PASSWORD",
    ],
    surfaces: [
      "server info",
      "chat query",
      "message query/search",
      "text send",
      "message detail/delivery metadata",
      "request ledger metadata",
    ],
    knownGaps: [
      "No attachment, tapback/reaction, edit, unsend, or read-receipt lifecycle",
      "No macOS Messages database fallback fixture in the central mock runner",
    ],
    validation: [
      "test/mocks/__tests__/non-google-provider-mocks.test.ts",
      "plugins/plugin-personal-assistant/test/lifeops-simulator.test.ts",
      "plugins/plugin-personal-assistant/test/imessage.test.ts",
      "plugins/plugin-personal-assistant/src/lifeops/imessage-bridge.test.ts",
    ],
  },
  {
    id: "twilio",
    label: "Twilio",
    mode: "static-http",
    environment: "twilio",
    envVars: ["ELIZA_MOCK_TWILIO_BASE"],
    surfaces: [
      "Programmable Messaging send",
      "Programmable Voice call create",
      "Mockoon template request echo",
    ],
    knownGaps: [
      "No delivery status callbacks, recordings, media, incoming call webhooks, or error variants",
    ],
    validation: [
      "test/mocks/__tests__/mock-runtime.smoke.test.ts",
      "plugins/plugin-personal-assistant/test/twilio-sms.test.ts",
      "plugins/plugin-personal-assistant/test/twilio-call.test.ts",
    ],
  },
  {
    id: "calendly",
    label: "Calendly",
    mode: "static-http",
    environment: "calendly",
    envVars: ["ELIZA_MOCK_CALENDLY_BASE"],
    surfaces: [
      "current user",
      "event types",
      "available times",
      "scheduling links",
      "scheduled events",
    ],
    knownGaps: [
      "No webhooks, invitee cancellation/reschedule, organization/team scope, or OAuth refresh variants",
    ],
    validation: [
      "test/mocks/__tests__/mock-runtime.smoke.test.ts",
      "plugins/plugin-personal-assistant/test/calendly.test.ts",
    ],
  },
  {
    id: "eliza-cloud-managed-google",
    label: "Eliza Cloud managed Google",
    mode: "static-http",
    environment: "cloud-managed",
    envVars: ["ELIZA_CLOUD_BASE_URL"],
    surfaces: ["managed Google status", "managed Google account list"],
    knownGaps: [
      "No managed mutation routes, cloud auth failure matrix, billing limits, or account relink flows",
    ],
    validation: [
      "test/mocks/__tests__/mock-runtime.smoke.test.ts",
      "test/mocks/__tests__/mock-runtime-seeding.test.ts",
    ],
  },
  {
    id: "discord-rest",
    label: "Discord REST API",
    mode: "stateful-http",
    environment: "discord",
    envVars: ["ELIZA_MOCK_DISCORD_BASE"],
    surfaces: [
      "POST message to channel (stateful message store)",
      "GET channel messages (merged sent + inbound history)",
      "GET single message in channel",
      "GET channel info",
      "GET guild info",
      "GET guild channels",
      "GET guild member",
      "GET /users/@me (bot identity)",
      "GET /applications/@me",
      "POST interaction callback (204)",
      "GET gateway/bot",
      "Test-only peek at /__mock/discord/sent",
      "Test-only inject at /__mock/discord/inbound",
    ],
    knownGaps: [
      "No Discord Gateway / WebSocket mock",
      "No voice channel, thread, forum, or stage surfaces",
      "No attachment upload, reaction, edit, or delete endpoints",
      "No rate-limit, retry-after, or permission-error variants",
    ],
    validation: [],
  },
  {
    id: "slack",
    label: "Slack Web API",
    mode: "stateful-http",
    environment: "slack",
    envVars: ["ELIZA_MOCK_SLACK_BASE"],
    surfaces: [
      "POST chat.postMessage (stateful per-channel)",
      "chat.update",
      "chat.delete",
      "GET conversations.list (derived from sent-message channels)",
      "GET conversations.history",
      "GET users.info",
      "GET users.list",
      "POST auth.test (bot identity)",
      "POST oauth.v2.access (token exchange)",
      "POST files.upload",
      "POST /events (URL verification challenge echo)",
      "Test-only peek at /__mock/slack/sent",
      "Test-only inject at /__mock/slack/inbound (also /__mock/slack/inbound-event alias)",
    ],
    knownGaps: [
      "No Events API signing verification",
      "No Block Kit interactive components",
      "No real file content storage; files.upload returns metadata only",
      "No RTM or Socket Mode",
    ],
    validation: [],
  },
  {
    id: "telegram-bot-http",
    label: "Telegram Bot API (HTTP)",
    mode: "stateful-http",
    environment: "telegram",
    envVars: ["ELIZA_MOCK_TELEGRAM_BASE"],
    surfaces: [
      "GET /bot<token>/getMe",
      "POST /bot<token>/sendMessage",
      "POST /bot<token>/sendPhoto",
      "POST /bot<token>/editMessageText",
      "POST /bot<token>/sendChatAction",
      "GET /bot<token>/getFile",
      "POST /bot<token>/answerCallbackQuery",
      "GET /bot<token>/getUpdates (drains pending queue)",
      "POST /bot<token>/setWebhook",
      "POST /bot<token>/deleteWebhook",
      "POST /bot<token>/answerInlineQuery",
      "Test-only inject at /__mock/telegram/inbound (also /__mock/telegram/inbound-update alias)",
    ],
    knownGaps: [
      "No MTProto; only Bot API HTTP surface",
      "No media types beyond photo (audio, video, document, sticker)",
      "No inline keyboards or reply markup payload validation",
      "No chat/group admin endpoints",
    ],
    rationale:
      "The LifeOps Telegram integration uses MTProto. This mock covers the Bot API HTTP path for plugins that consume the Bot API directly.",
    validation: [],
  },
  {
    id: "linear",
    label: "Linear",
    mode: "stateful-http",
    environment: "linear",
    envVars: ["ELIZA_MOCK_LINEAR_BASE"],
    surfaces: [
      "POST /graphql — Viewer query",
      "POST /graphql — Issues query",
      "POST /graphql — Teams query",
      "POST /graphql — Team query",
      "POST /graphql — Users query",
      "POST /graphql — Projects query",
      "POST /graphql — IssueCreate mutation (stateful issue store)",
      "POST /graphql — IssueUpdate mutation",
      "POST /graphql — IssueDelete mutation",
    ],
    knownGaps: [
      "No webhook delivery simulation",
      "No full GraphQL grammar (fragments, aliases, pagination cursors)",
      "No attachment, comment, cycle, or project-planning surfaces",
      "No rate-limit or permission-error variants",
    ],
    validation: [],
  },
  {
    id: "shopify",
    label: "Shopify Admin API",
    mode: "stateful-http",
    environment: "shopify",
    envVars: ["ELIZA_MOCK_SHOPIFY_BASE"],
    surfaces: [
      "GET /admin/api/2024-10/products.json",
      "POST /admin/api/2024-10/products.json",
      "GET /admin/api/2024-10/products/:id.json",
      "GET /admin/api/2024-10/orders.json",
      "GET /admin/api/2024-10/orders/:id.json",
      "GET /admin/api/2024-10/customers.json",
      "GET /admin/api/2024-10/customers/:id.json",
      "GET /admin/api/2024-10/inventory_levels.json",
      "GET /admin/api/2024-10/shop.json",
    ],
    knownGaps: [
      "No cart, checkout, or fulfillment surfaces",
      "No webhook delivery or signature verification",
      "No metafields, collections, discounts, or gift cards",
      "No pagination, filter query params, or error variants",
    ],
    validation: [],
  },
  {
    id: "payments",
    label: "Payments API",
    mode: "stateful-http",
    environment: "payments",
    envVars: ["ELIZA_MOCK_PAYMENT_BASE", "ELIZA_MOCK_PAYMENTS_BASE"],
    surfaces: [
      "POST /v1/payment-requests — create a dollar-denominated payment request",
      "GET /v1/payment-requests/:id — status lookup",
      "POST /v1/payment-requests/:id/pay — mark paid/accepted with transaction hash",
      "POST /v1/payment-requests/:id/fail — mark failed and attach a reason",
      "POST /api/v1/apps/:appId/charges — create a Cloud-style app charge request",
      "GET /api/v1/apps/:appId/charges/:chargeId — public app charge status lookup",
      "POST /api/v1/apps/:appId/charges/:chargeId/checkout — create a mock Stripe/OxaPay checkout",
      "GET /__mock/payments/requests — inspect payment request, app charge, and callback ledger",
      "DELETE /__mock/payments/requests and POST /__mock/payments/reset — reset state",
      "Signed HTTP callbacks for paid and failed transitions",
      "request ledger metadata",
    ],
    knownGaps: [
      "No card-network, wallet, OxaPay, Stripe, or x402 protocol validation",
      "No real settlement, exchange-rate, dispute, refund, or chargeback lifecycle",
      "No delayed webhook retries; callback delivery is synchronous in the fixture",
    ],
    validation: ["test/mocks/__tests__/payment-provider-mock.test.ts"],
  },
  {
    id: "anthropic",
    label: "Anthropic Messages API",
    mode: "stateful-http",
    environment: "anthropic",
    envVars: ["ELIZA_MOCK_ANTHROPIC_BASE"],
    surfaces: [
      "POST /v1/messages — text response (static baseline)",
      "POST /v1/messages — prompt-prefix-keyed text response (ping/echo/summarize/explain)",
      "POST /v1/messages — tool_use response (when tools present)",
      "POST /v1/messages — computer-use tool_use (screenshot first turn)",
      "POST /v1/messages — text after tool_result (computer-use follow-up)",
      "GET /v1/models",
    ],
    knownGaps: [
      "No streaming (Server-Sent Events) support",
      "No message batches API",
      "No Files API or vision image uploads",
      "No prompt caching or token-budget headers",
    ],
    validation: [
      "test/harness/__tests__/provider-wire-mock.test.ts",
      "../plugins/plugin-anthropic/__tests__/anthropic-drift.real.test.ts",
    ],
  },
  {
    id: "openai",
    label: "OpenAI API",
    mode: "stateful-http",
    environment: "openai",
    envVars: ["ELIZA_MOCK_OPENAI_BASE"],
    surfaces: [
      "POST /v1/chat/completions",
      "POST /v1/embeddings (deterministic 1536-dim vector seeded by sha256 of input)",
      "POST /v1/images/generations (placeholder image URL)",
      "GET /v1/models",
    ],
    knownGaps: [
      "No streaming (Server-Sent Events) support",
      "No vision image uploads or image-in-content",
      "No function-calling streaming or parallel tool calls",
      "No Assistants v2 (threads, runs, vector stores)",
      "No fine-tuning, moderation, or audio endpoints",
    ],
    validation: [
      "test/harness/__tests__/provider-wire-mock.test.ts",
      "../plugins/plugin-openai/__tests__/openai-drift.real.test.ts",
    ],
  },
  {
    id: "vision-analysis",
    label: "Vision Analysis API",
    mode: "stateful-http",
    environment: "vision",
    envVars: ["ELIZA_MOCK_VISION_BASE"],
    surfaces: [
      "POST /v1/vision/analyze — cat-fixture (default; legacy path)",
      "POST /v1/analyze — full analyze; sha256 image-bytes hash → fixture, fallback to image_hint, fallback to generic",
      "POST /v1/describe — caption only",
      "POST /v1/objects — object detection only",
      "POST /v1/text — OCR only",
      "Three deterministic fixture hints: cat-fixture, document-fixture, street-fixture",
      "Generic 'no recognizable content' response for unknown image hashes",
    ],
    knownGaps: [
      "No real computer vision; responses are deterministic fixture data",
      "No OCR confidence scores or word-level bounding boxes",
      "No multi-image batch analysis",
      "No streaming or async result polling",
    ],
    validation: [],
  },
] as const satisfies readonly LifeOpsProviderMockCoverage[];
